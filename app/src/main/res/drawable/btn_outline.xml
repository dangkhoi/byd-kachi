<?xml version="1.0" encoding="utf-8"?>
<!--
  btn_outline — DARK cockpit outline button (L2 Stage 1). Same resource name + selector structure.
  Transparent fill + brand stroke + ~12dp corners. Pressed overlay switched from black (invisible on
  dark) to a translucent blue glow. Strokes reference @color/brand + @color/divider (auto-reskinned).
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="12dp" />
            <stroke android:width="1dp" android:color="@color/divider" />
        </shape>
    </item>
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/press_blue" />
            <corners android:radius="12dp" />
            <stroke android:width="1.5dp" android:color="@color/brand" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="12dp" />
            <stroke android:width="1.5dp" android:color="@color/brand" />
        </shape>
    </item>
</selector>
