# Project Context — ClusterNav 2.0 (luôn-bật)

> **Trạng thái**: Current · **Cập nhật**: 2026-09-14 · **Mục đích**: Tóm tắt luôn-bật (kiến trúc + bản đồ nguồn nav + HAL/CAN + map file + trạng thái) để phiên sau KHÔNG phải đọc lại toàn code.
> *Derived* từ repo (nguồn-sự-thật). Chi tiết: `docs/README.md` (INDEX) · `docs/PROJECT-BACKLOG.md` (task) · `docs/diagnostics/*`. App standalone `com.byd.clusternav2`, JDK 17, compile/target SDK 37, minSdk 29.

## 1. Kiến trúc — ĐÚNG 2 nhánh (không chia sẻ runtime/state/executor/journal)
- **Navigation + HUD**: 1 nguồn nav (SourceArbiter chọn) → `NavigationSessionCoordinator` (authoritative) → **`Maneuver` trung lập** (1 quyết định hướng rẽ) → 3 đầu ra encode ĐỘC LẬP:
  - **cluster-lane** (làn zin) qua broadcast AMAP — `toAmapIcon()`.
  - **cluster-centre "Giữa + ETA"** qua HAL `BydHal.writeNavFrame` (owner DUY NHẤT = `NavigationHudOwner`) — `toHudIcon()`. Chỉ ghi khi `navOnlyMode` (Cast master OFF).
  - **windshield HUD** (kính lái) — cùng họ HAL guidance; bị gate bởi coding xe (xem §3).
- **Cluster Cast**: state/journal/cast ĐỘC LẬP (`SimpleCastRuntime`, 4-state IDLE→PROJECTING→CASTING→RETURNING, 1 nút nổi cast/return). KHÔNG dùng chung state/executor với Nav.
- **Home = renderer/dispatcher**, KHÔNG orchestrator.

## 2. Bản đồ NGUỒN NAV (quan trọng — tham chiếu khi làm B3 screen-capture)
- **GMaps** (`com.google.android.apps.maps` + `app.revanced.android.apps.maps`): **notif = nav ĐẦY ĐỦ, chạy NỀN** + arrow bitmap. Nguồn chính + **ground-truth cự ly** (nội suy bám notif).
- **VietMap** (`vn.vietmap.live`): (a) **widget RemoteViews = speed/giới-hạn/giới-hạn-sắp-tới + cự ly, chạy NỀN** (slot `SPEED_LIMIT`/`ALERTS`/`ALERT_FULL`=VMAlertWidgetProvider); (b) **a11y content-desc = turn/đường/ETA nhưng FOREGROUND-only** — Flutter ⇒ KHÔNG có resource-id, parse ở `VietMapDescParser` (:core) → publish `NavViewIdSource` (B3.40); (c) **mũi tên = screen-capture** qua `NavGlyphLocator` (dò glyph động, bất biến dpi/size/vị-trí) + registry riêng `WazeArrowRegistry.VIETMAP_INK` 40 template sinh từ asset APK 3.3.4 (34) + fixture-runtime (6, B3.52) (B3.43). **B3.47 vòng 3 (08-23) đã thay cổng cỡ tuyệt đối bằng tiêu chí TỈ SỐ** — gỡ `MAX_DP`, nới `MAX_FILL` 0.35→0.60, thêm `MIN/MAX_ASPECT` [0.35, 1.45] + `MAX_LEFT_ANCHOR` 2.0 (khoảng cách mép-trái đo bằng chính cỡ đảo). [ĐO] bằng locator Kotlin thật trên 87 khung: **dò đúng 27→86**, **đảo mồi 60→0**, tới mã AMAP **15→39**; Waze bbox không đổi một pixel. Đảo mồi = icon POI xe buýt TRÊN BẢN ĐỒ (603,78)-(642,117) qua sạch cả 4 cổng cũ ⇒ giả định "không dò được ⇒ im lặng" từng SAI. Khoá: `VietMapGlyphGateTest` + `NavGlyphLocatorTest.bbox Waze khong doi mot pixel nao`; (d) camera = map trên SurfaceView → screen-capture. ⚠ **KHÔNG còn ở roster kênh notification** từ B3.44 (`NavApps.NOTIFICATION` = chỉ GMaps) — chính noti của nó từng đóng mốc DATA và khoá mất kênh ảnh của chính nó.
- **Waze / WazeMod** (`com.waze` / `com.chisadin.wazemod`): **KHÔNG có kênh data nền** (đo 08-22: noti chỉ có `tickerText=Waze`) ⇒ cũng KHÔNG ở roster notification (B3.44). Hai kênh THẬT, cả hai đều **cần app visible**: (a) **view-id a11y** (clone OpenBYD) — `navBarDistance`/`navBarStreetLine`/`lblArrivalTime`… , ⚠ tiền tố id là tên package trong `resources.arsc` = **`com.waze` cho CẢ bản mod** (WazeMod DUAL manifest `com.chisadin.wazemod` nhưng arsc `com.waze`) ⇒ một tiền tố phủ cả hai (B3.33); (b) **screen-capture mũi tên** → `WazeArrowRegistry.WAZE_INK` (4 template). HLP-logcat đã **gỡ hẳn** khỏi code+UI (B3.21/B3.30: 0 dòng log khi đang dẫn, hardware-gated).

## 3. HAL / CAN facts (đã proven on-car / RE)
- **HUD kính ZIN nav = gate firmware của XE, KHÔNG phải app** (ADR 0002, **sửa ×2 2026-08-19**). **App ĐẨY nav lên bus ĐÚNG** — proven vì **HUD Taobao aftermarket** trên xe anh em hiện nav bằng **chính app này + GMaps** (đường render ĐỘC LẬP, đọc bus tự vẽ; **cụm cũng hiện nav**). ⚠ **HUD Taobao ≠ HUD zin**: nó KHÔNG bác được cờ gate zin. **`0x38B00030` (INSTRUMENT_HUD_NAVIGATION_MAP_CONFIG, consumer `readSelfLearnState()==1`) VẪN là nghi phạm gate HUD-ZIN, CHƯA bị bác.** `40d` 138(owner) vs 162(anh em) chỉ **tình cờ** — khác biệt THẬT = HUD zin vs HUD Taobao, không phải con số 40d. **Hai đường ra:** (a) HUD Taobao = đã chạy; (b) HUD zin = provisioning firmware (`0x38B00030` ± cờ khác) qua tool coding. Đừng "fix" trong code. Chi tiết + xếp hạng đường: `docs/diagnostics/factory-hud-nav-RE-avenues-2026-08-19.md`.
- **`NOT_PROVISIONED_RC = -2147482648`** (= `Int.MIN_VALUE + 1000` = `0x800003E8`; **KHÁC** `Int.MIN_VALUE`). BydHal cache per-feature-id gặp rc này → skip frame sau (hết spam `no permission device 1007`). Xe provision oversea (Sealion 6) không bao giờ nhận sentinel → vẫn ghi.
- **guide** `INSTRUMENT_GUIDE_INFO_SIMPLE_SET`: **`0x43F01010` (domestic)** / **`0x1F701010` (oversea)** — cụm-centre + HUD Taobao đọc (HUD zin chỉ đọc khi lớp nav provisioned); app ghi CẢ 2 họ.
- **`SET_NAVI_SCREEN_STATUS_SET = 0x4C10E015`** (BYDAutoSettingDevice); `NAV_SCREEN_MODE_ON = 3`.
- **`Maneuver.toHudIcon()` vòng xuyến** (CAN ghi-thẳng, OpenBYD `w40`+`HudController`): **15=trái · 18=phải · 20=thẳng/generic · 22=u-turn** (CCW/VN); CW=16/17/19/21; có số lối ra → **24+N** (25..34). `ROUNDABOUT_EXIT`→HUD **24**. `toAmapIcon()` vòng xuyến (mọi hướng) = **11 generic** (cụm-strip không có glyph hướng).
- **Nav-guide registers (43E/43F: NAVI_STATUS/PATHNAME/TRIP) ghi rc=0** → nuôi cụm-centre + **HUD Taobao aftermarket** (đọc bus tự vẽ). Đây là **đường content** (đúng, đã chạy). Với **HUD zin**, content 43E/43F chỉ render khi **lớp nav được provision** — nghi phạm gate = `0x38B00030` (INSTRUMENT_HUD_NAVIGATION_MAP_CONFIG, `readSelfLearnState()==1`), **ghi thẳng bị reject** (đọc-only, set qua UDS coding). ⚠ đừng kết luận "38B00030 không liên quan": nó CHƯA bị bác cho HUD zin (bằng chứng anh em là HUD Taobao, đường render khác).
- **Bug owner "vòng xuyến generic"** = **OEM RENDER-side**, đặc thù variant xe owner (`vehicle_40d` owner=138 vs bạn=162). App gửi **ĐÚNG CAN 18** (data owner + bạn xác nhận). → glyph-test cần data owner on-car (C2).

## 4. Map file chính
- `core/.../navigation/Maneuver.kt` — enum maneuver trung lập + `toAmapIcon`(cụm)/`toHudIcon`(HUD)/`fromAmapIcon`.
- `core/.../navigation/ManeuverSignature.kt` — phân loại GMaps large-icon (chữ ký) → maneuver/mã AMAP.
- `app/.../NavNotificationListener.kt` — listener notif 5 gói nav; gate+parse+fan-out; sở hữu speed-sign + VietMap bridge + Waze HUD.
- `app/.../NavRepository.kt` — facade runtime nav authoritative; `ingest`→coordinator→lane + centre(HAL) + HUD.
- `app/.../NavigationHudOwner.kt` — owner DUY NHẤT ghi cụm-centre HAL (Giữa+ETA) + keep-alive.
- `app/.../modules/hal/BydHal.kt` — hạ tầng HAL reflection: `writeNavFrame` (icon/cự ly/đường/ETA domestic+oversea) + rejection cache + register consts.
- `core/.../vietmapwidget/VietMapWidgetModels.kt` — model snapshot VietMap widget (speed/limit/upcoming, per-slot freshness).
- `app/.../speedbadge/SpeedBadgeOverlay.kt` — overlay badge tốc-độ/giới-hạn trên cụm (display 1), lifecycle event-driven + badge "sắp tới".
- `app/.../modules/clustercast/simplified/SimpleCastRuntime.kt` — runtime Cast 4-state.
- `core/.../navigation/NavApps.kt` — **một nguồn sự thật** cho roster app nav: `ALL` (cổng a11y, khớp XML `nav_accessibility_config.xml`) · `NOTIFICATION` (chỉ GMaps) · `DESC_ONLY` (VietMap) · `GMAPS`/`WAZE`/`VIETMAP`.
- `core/.../navigation/screencapture/NavGlyphLocator.kt` — dò bbox mực của glyph mũi tên: đảo sáng + vành tối 4 phía + **sàn** dp + **hai tỉ số hình dạng** + **neo trái** + trái-nhất. Bất biến với dpi/size/vị-trí cast (B3.47 vòng 3 gỡ hẳn trần dp).
- `core/.../navigation/WazeArrowRegistry.kt` — registry quy ước **bbox-mực** (4 Waze + 40 VietMap: 34 SVG-asset + 6 fixture-runtime B3.52). TÁCH CỨNG khỏi 38 mục GMaps quy ước *khung-vẽ* của `ManeuverSignature` (B3.46).
- `core/.../navigation/NavViewIdSource.kt` — holder dữ liệu a11y (Waze view-id + VietMap content-desc); `Reading` composite chống đọc-rách.
- `core/.../navigation/{NavFrameIdentity,NavSourceDwell,TurnDistancePlausibility}.kt` — 3 guard scope B: một-package-một-khung · dwell 3 nhịp chống nhảy nguồn · chặn cự ly nhảy phi lý.
- `core/.../navigation/screencapture/NavWindowPump.kt` — **nhịp định kỳ** cho vòng enum cửa sổ a11y (B3.13r): một throttle 800 ms dùng chung cho CẢ đường event LẪN đường nhịp (**trần đỉnh** enum/giây không đổi; **trung bình thì tăng** — xem B3.13r-cost) · tự tắt sau 4 nhịp không thấy cửa sổ nav · mồi lại bằng `arm()` từ event a11y **và** `onServiceConnected` (hai đường độc lập vòng enum). Bệnh nó chữa KHÔNG phải "hết tươi 3 s ⇒ cụm trống" (nhịp chụp tự nuôi qua `SourceArbiter.shouldFeed(…, IMAGE)`) mà là **VÒNG TRÒN**: kênh ảnh im > `STALE_MS` ⇒ cổng `ScreenCaptureNavSource.tick` đóng, mà thứ duy nhất mở lại cổng nằm SAU chính nó.
- `app/.../NavOutputOwner.kt` — owner đường ẢNH: quyết định push + tự assert op-39 dựng bề mặt cụm (B3.45).

## 5. Trạng thái (2026-09-12)

- **2026-09-14 · T-BRIDGE — CẦU KIỂM THỬ QUA adb. DONE off-car (5 module 0 đỏ), spec `docs/specs/kachi-test-bridge.html`.** Owner: *"sẽ adb vào xe, nên chuẩn bị toàn bộ script test automation trên xe thông qua adb, log liếc các loại"*. `BroadcastReceiver` **exported** (`.launcher.testbridge.KachiTestBridge`, action `${applicationId}.TEST`) nhận **13 lệnh** — `say` · `wav` · `listen` · `state` · `profiles` · `profile` · `preset` · `slot` · `slot_clear` · `open` · `prefs` · `reapply` · `diag` — và trả **JSON** ở ba nơi cùng lúc: `setResultData` (⇒ `am broadcast` in thẳng ra terminal), tệp `files/test/<mốc>-<lệnh>.json`, một dòng `logcat -s KachiTest`. **[ĐO] vì sao phải `exported`**: uid shell (2000) không gửi được vào receiver non-exported — cùng họ với ca `.ClusterNavActivity` đã ghi trong manifest từ 2026-07-20; mà `onReceive` **không có `getCallingUid`** ⇒ không phân biệt được shell với app khác. **Cổng thật = công tắc *Chế độ kiểm thử qua adb*** (Cài đặt › Hệ thống & quyền › Nâng cao): chỉ bật **bằng tay trong xe** (không có lệnh `enable` — một cửa mở được từ xa thì chủ xe không biết mình đã mở), **tự tắt sau 60 phút**, **chết theo lần nổ máy**. Cơ chế ở `TestBridgeWindow` (`:core`, thuần): MỘT khoá `test_bridge_until` = `"<mốc nổ máy>:<hạn dùng>"` với *mốc nổ máy* = `currentTimeMillis − elapsedRealtime` — khác lần nổ máy ⇒ đóng; hạn xa hơn cả cửa sổ (đồng hồ bị vặn lùi) ⇒ đóng; giá trị hỏng ⇒ đóng. Tệp prefs **riêng** `kachi_test_bridge` (khai ở `SettingsCatalog.PREFS_FILES`), khoá xếp `TRANSIENT` — cố ý KHÔNG nằm trong `kachi_workspace` vì tệp đó đi theo hồ sơ (chụp–áp/nhân bản/xoá) còn một cửa mở-60-phút thì không được đi đâu cả. **Ba ràng buộc còn lại**: (1) mọi lệnh đi qua **đúng đường một cú chạm đi** (`VoiceDispatcher` · `KachiHomeSlots.assignApp/clearSlot/openAppFullscreen` · `HomeViewModel` · `ClusterNavBridge.reapplyAll` · `ClusterDiag`) — không đường thứ hai, không lệnh shell thô, không chạm display ≥ 1; (2) cổng CONFIRM **mặc định TỪ CHỐI** và trả nguyên văn câu hỏi trong `needs_confirm` (broadcast không có ai ngồi trước màn để bấm; hộp thoại sẽ hết giờ và lượt đo báo `timeout` thay vì báo đúng việc), `--ez auto_confirm true` mới chạy và **luôn** ghi `AUTO-CONFIRM: <câu hỏi>` vào nhật ký; (3) mọi lượt chạy để lại dấu — không lệnh nào chạy im lặng. **Móc của màn chính** (`TestBridgeHooks` + `KachiTestHooks`, một bản cho cả tiến trình như `SlotVdOwner`): gắn ở `onCreate` bằng **một dòng**, lượt tháo nối vào `ActivityLifecycleCallbacks` chứ không phải `onDestroy` (cặp gắn/tháo hai chỗ là một nửa sẽ bị quên — CLAUDE.md §8). Màn chưa lên ⇒ `home_not_running`, trừ `prefs` (cố ý chạy được để chẩn đoán đúng ca *launcher không lên*). **[ĐO] máy ảo `emulator-5554` (1.53/54, `install -r`)**: 13 lệnh + 6 ca lỗi chạy thật — `state` ra đủ 11 nhóm trường (gồm `slot_displays:["kachi-slot-1-…","kachi-slot-3-…"]` đọc từ `DisplayManager`, không từ cờ RAM), `say "bật đèn đọc"` → ý định `Control`, `say "mở khoá cửa"` → `needs_confirm` (không chạy) và với `auto_confirm` thì chạy + có dòng `AUTO-CONFIRM`, `slot 9` → `slot_out_of_range slot_count:4`, `listen` → tấm chữ *Đang chuẩn bị…* hiện thật, tắt công tắc → `test_mode_off`. ⚠ **[ĐO] bẫy dấu nháy**: `adb shell` cắt chuỗi theo khoảng trắng ⇒ `--es text "bật đèn đọc"` chỉ tới `"bật"` mà lời đáp **vẫn `ok:true`** — phải nháy hai lớp `"'bật đèn đọc'"` (helper `k_shq` trong `scripts/vehicle/kachi/_common.sh` đã làm đúng). **Sửa kèm**: `k_test_alive` trước đây chỉ hỏi *có trả dữ liệu không* ⇒ báo "cầu SỐNG" cả khi công tắc đang TẮT (cầu vẫn trả `test_mode_off`); nay đọc `"ok":true`. **Còn nợ xe**: (a) shell của ROM BYD có gửi vào được receiver `exported` không; (b) reboot bằng **nút nguồn vật lý** rồi bắn lại phải ra `test_mode_off`; (c) một lệnh xe THẬT trả `✓`. **Còn mở**: `listen` chưa ghi kết quả phiên ra tệp ([CHƯA BIẾT] `VoiceSession` không phơi sự kiện kết thúc); tiền tố `kachi-slot-` còn là bản sao giữa `VdAppHost` và `TestBridgeState` (có bài canh giữ hai bên khớp). Files: `core/…/launcher/testbridge/{TestBridgeCommand,TestBridgeJson,TestBridgeWindow}.kt` · `app/…/launcher/testbridge/{KachiTestBridge,TestBridgeHooks,TestBridgeReply,TestBridgeState,TestBridgeStore}.kt`.

- **2026-09-14 · V1 pha NGHE — nhận dạng tiếng nói TẠI MÁY (Vosk), bản 1.49/50. DONE off-car (2500 test/0 đỏ), spec `docs/specs/kachi-voice-command.html` R9–R14.** ASR cắm vào **TRƯỚC** bộ hiểu ý của pha chữ ⇒ `:core` pha chữ **không sửa một dòng**; ranh giới vẫn là *"một chuỗi chữ"*. **Thư viện** (version kiểm Context7 + Maven Central, API kiểm bằng `javap` trên chính AAR): `com.alphacephei:vosk-android:0.3.47@aar` + `net.java.dev.jna:jna:5.17.0@aar`; **KHÔNG** dùng `SpeechService`/`SpeechStreamService` của gói (chúng tự dựng `AudioRecord` ngoài tầm bài canh mạng + trần 8 s). **Mô hình** `vosk-model-small-vn-0.4` (33.656.337 B, sha256 `efe5c849…d622e5`, Apache-2.0) **TẢI RIÊNG** vào `filesDir/vosk/` — ghim sha+cỡ, chống zip-slip, tiến trình trong Cài đặt, có đường **gỡ**; APK 9,06 → **26,8 MB** (`abiFilters` arm64-v8a + armeabi-v7a). **Hai sự thật ĐO được định hình thiết kế**: (a) gói **không có `graph/words.txt`** — từ điển **nhúng trong `graph/Gr.fst`** (flags=3, **19.529 mục**) ⇒ `VoskWordList` đọc bảng ký hiệu OpenFst (`model.cc:306-307` lường sẵn ca này); (b) từ điển là chữ **CÓ DẤU**, mà `VoiceSynonyms`/bảng động từ khai **không dấu** ⇒ `VoicePhrases` **tra ngược qua chính từ điển** và **nở mọi thanh điệu** của một âm (chứ không chép một bảng có-dấu thứ hai) ⇒ nghe sai thanh hết làm hỏng lệnh. Vosk **bỏ lặng lẽ** từ ngoài từ điển (`recognizer.cc:340-347`) nên cụm nào không đủ từ bị loại **cả cụm** và con số bị ghim trong test. **[ĐO] trước khi vá: 11 khả năng gõ được mà KHÔNG nói được** (nhãn có `EV/HEV`·`iTAC`·`AVH`·`PM2.5`·`SOH`·`MCU`·`12V`·`%`) ⇒ thêm cách gọi thuần Việt, nay **0 câm**. **Phiên bấm-để-nói** (`VoiceCapture`+`VoiceSession`+`VoiceOverlay`): `AudioRecord` 16 kHz mono (`VOICE_RECOGNITION`→`MIC`), **trần cứng 8 s**, tấm chữ góc màn, âm báo `ToneGenerator` (**không TTS**), focus `MAY_DUCK` (**không tắt nhạc**), cổng CONFIRM nhận *"đồng ý"/"huỷ"* bằng lời **mặc định KHÔNG**. **Ba lối vào, MỘT bộ dây** (`VoiceWiring`): `launcher_voice` *Nói với xe* · nút mic thanh trên (chỉ hiện khi mô hình đã tải; công tắc ở *Cài đặt › Thanh trạng thái*) · đích phím `__KACHI_VOICE__` *Kachi nghe* — **không đổi mặc định nào**, nút 328 vẫn thuộc Kiki (§6). `RECORD_AUDIO` vào `LauncherRequirements` (**không** `coreFeature`) + **tự cấp** `pm grant`. **[ĐO] máy ảo 1.49**: tải+sha+giải nén+rút từ điển chạy thật (19529 từ · 51 MB), nạp mô hình **114 ms**, ngữ pháp **2069 mục**, WAV *"đặt nhiệt độ hai mươi hai"* → nghe *"…hái mươi hai"* → **Đặt Nhiệt độ = 22**, phiên nghe thật mở micro nguồn 6 + tấm chữ + tự đóng. **Hai bug tự bắt**: hằng magic bảng ký hiệu FST **đoán sai** mà 3 bài kiểm vẫn xanh (chúng dựng dữ liệu bằng chính hằng sai) ⇒ thêm bài hồi quy đọc **64 KiB byte THẬT** của `Gr.fst`; `InputStream.readNBytes` là **API Java 9**, biên dịch sạch rồi ngã trên minSdk 29. **Bài canh phạm vi đổi CÂU HỎI, không mất phạm vi**: `VoiceCommandWiringContractTest` nay cấm `SpeechRecognizer`/`TextToSpeech`/`MediaRecorder(`/**mọi đường ra mạng trong tệp `Voice*`**/hơn một tệp mở micro, và **đòi** manifest có `RECORD_AUDIO`. **SOÁT Pass 2 (2026-09-14)** — 7 vá [P1]–[P2]: phiên nghe **chết theo màn chính** (`VoiceSession.stop()` gọi trong `onDestroy` — cửa sổ overlay + micro từng sống lâu hơn activity) · **chốt một-lượt-tải** ở `VoiceModelStore` · **trần 500 dòng đã trả nợ** (`KachiHomeActivity` 523→**478** nhờ tách `KachiHomeSlots.kt` + `collectHome` sang `KachiHomeWiring`; `ClusterNavBridge` 506→**491**) · nút mic **hiện ngay** sau khi tải mô hình/gạt công tắc (`KachiTopStrip.refreshVoicePill`) · **kiểm chỗ trống** trước khi tải · **chốt HTTPS sau chuyển hướng** (`conn.url.protocol`) · từ điển 19.529 dòng có bộ nhớ đệm theo dấu vết tệp. **Còn mở**: độ chính xác giọng thật + CPU/RAM trên xe (T18) · TTS · wake word · nút *Dừng tải* giữa chừng · [CHƯA BIẾT] phím vô-lăng kéo màn chính lên trước có đẩy app đang chiếu trong ô xuống không. Files: `core/…/voice/{VoiceModelManifest,VoskWordList,VoicePhrases}.kt` · `app/…/voice/{VoiceModelStore,VoiceRecognizer,VoiceCapture,VoiceSession,VoiceOverlay,VoiceWavProbe,VoiceWiring,VoiceModelSettings}.kt` · `app/…/net/HttpConn.kt` · `res/drawable/ic_mic.xml`.

- **2026-09-14 (khuya) · V1 pha CHỮ — điều khiển bằng lời, tầng `chữ → hành động`. DONE off-car (**2471 test/0 đỏ**, senior review Pass 1 APPROVED), spec `docs/specs/kachi-voice-command.html`.** `:core/launcher/voice/` = `VoiceLexicon` (bỏ dấu + số bằng chữ VI/EN) · `VoiceGrammar` (**từ vựng SINH từ 4 bộ đăng ký**, `VoiceSynonyms` khai một chỗ) · `VoiceIntentParser` (động từ × **KIỂU ĐỐI TƯỢNG** — giải "Mở" quá tải theo RE Kiki §7c; ba luật: dãy dài nhất thắng ⇒ L-RE2 · loại động từ chọn ứng viên ⇒ 18 nhãn trùng · cả câu là TÊN việc ⇒ gói lệnh thắng nút cùng tên) · `VoiceRisk` (4 việc CONFIRM: mở khoá · hạ hết kính · dừng chiếu cụm · đổi hồ sơ) · `VoiceReply` (song ngữ, gọi tên bằng `displayLabel`). `:app` = `VoiceDispatcher` (CẦU, đi lại **đúng** đường của một cú chạm) + hàng **“Gõ lệnh chữ (thử giọng nói)”** ở *Cài đặt › Hệ thống & quyền › Nâng cao*. **Phủ 194 ca sinh từ registry** + 44 mẫu câu Kiki chuyển thể. **R8: KHÔNG mic/ASR/TTS/wake word/LLM** — tầng 1 §14 chưa xanh (playbook §2.14 + K1–K3), có bài canh quét chặn (`VoiceCommandWiringContractTest`) + manifest không xin `RECORD_AUDIO`. **Pass 1 (§10 spec)**: câu ghép có vế CONFIRM ⇒ **dừng cả chuỗi** tới khi đồng ý (OQ4 đã chốt) · `VoiceLexicon` đọc số rút gọn *“hai lăm/hăm bốn”* (trước đó rơi về `min`) · `MediaBridge.tx` tự `read()` + trả `Boolean` (hết “✓” khống) · từ vựng `by lazy` · `VoiceReply` gắn đuôi *“chưa kiểm trên xe”* theo `CarCapabilities.needsBadge` · thêm `VoiceDispatcherSafetyTest`.
- **2026-09-14 (tối) · ONCAR-1.53 — BỘ TEST LÊN XE, NAY TỰ CHỤP BẰNG CHỨNG (chưa chạy trên xe).** Playbook một tệp `docs/diagnostics/oncar-playbook-kachi-1.53.md` (**17 mục** rủi ro tăng dần; thêm **§2.16 giọng nói thật T18/T25** — 25 câu ở 3 mức ồn, độ trễ đo bằng mốc logcat, CPU/RAM, VietMap `vietmaplive://` + GMaps `google.navigation:ll=`; **§2.17 H2** — số `kachi-slot-*` phải bằng số ô App, nhãn "App đã đóng" ≤10 s = `SlotLiveness` 5 s × 2 nhịp; **§0.5** có gì mới 1.47→1.53) + `scripts/vehicle/kachi/` (thêm **`70-voice.sh`**; `bash -n` sạch cả 11 tệp). **Tự động hoá** (owner: *"toàn bộ script test automation qua adb, log liếc các loại"*): mỗi bước tự mở logcat nền `logcat-<bước>.txt`, tự chụp `shot-<bước>-NN.png` sau mỗi thao tác, tự `diff-<bước>.txt` từ dumpsys trước/sau, tự ghi ms từng lệnh vào `session-notes.txt`; helper `k_test` gọi **`KachiTestBridge`** qua `am broadcast -a com.byd.launcher.TEST` (bật ở Cài đặt › Hệ thống › Nâng cao, tự tắt sau 60 phút) — **[CHƯA BIẾT] cầu chưa có trong bản ở `apk/`, mọi bước có đường tay đi kèm**. **CẤM `input tap` toạ độ** (xe 1920×720 ≠ máy ảo 1080×2340). `40-ota.sh` đọc bản đích từ tên tệp `apk/Kachi-*-release.apk` (§9, hết hằng số). **Ba sự thật [ĐO từ source]**: (a) giá trị datum chỉ đọc được trong tiến trình app (`BydHalGateway`→`BydHal`); (b) `ClusterNavReapply` chỉ ghi khi applier NÉM ⇒ im lặng ≠ không chạy; (c) mô hình giọng 32 MB **chỉ tải được qua HTTPS** (`VoiceModelStore`→`HttpConn` chốt `https://`) — không có đường cài từ tệp cục bộ, và `filesDir` của bản release không `adb push` vào được. Build type **`vehicleTest`** = release + `isDebuggable=true` + cùng khoá ký ⇒ cài đè không mất prefs khi cần `run-as` (nhớ cài lại release trước khi rời xe).
- **2026-09-13 (trưa) · S3 GỠ HẲN MÀN CLUSTERNAV CŨ + U7 BỘ ICON v2. DONE off-car (đang senior review), APK 1.41 (42).** S3: MainActivity + 12 lớp + 8 test + layout-w960dp xoá; 4 tính năng còn thiếu chuyển vào Kachi (khung/DPI khi chiếu = `ClusterNavBridgeGeometry` + stepper/chip ở Chiếu cụm; `ClusterPreviewView` nhúng; disclaimer lần đầu ở KachiHomeWiring; op39 statusRow) + 3 hành vi vòng đời (bóng nổi khi onResume · ép tắt HUD ở BootSetupService · re-apply Gemini ở nút Kiểm tra/Sửa ngay); intent cũ → KachiHomeActivity + extra `open_settings_group`; **2 tệp seal T11 (`res/layout/activity_main.xml`, `res/values/strings.xml`) ở lại NGUYÊN BYTE, mồ côi** (`LegacyScreenAbsenceContractTest` canh cả hai chiều). U7: 3 khung xe dùng chung (top/front/rear), 168 icon (64 mới `ic_car_*`), NÉT = vật thể / VÙNG TÔ = bộ phận, hình phân biệt 65 → 117, trần ≤3 mọi nhóm; `PickerBadge` đảo dấu "chưa kiểm". Còn: U9 hình xe lớn cho widget board · U10 chấm thanh nút · W5 chờ đo phím trên xe · đăng `apk/Kachi-1.41-release.apk`.
- **2026-09-13 (chiều) · L2 KHOÁ KÝ + OTA RIÊNG. DONE — [ĐO] OTA end-to-end máy ảo 1.41→1.42 OK (cần `adb tcpip 5555` + `adb reverse tcp:5555 tcp:5555`); U7 bài mù độc lập 100% (1.42 đã đăng).** Keystore `~/.kachi/kachi-release.keystore` (RSA-4096, alias `kachi`, SHA-256 `92:57:49:9B…99:17`) + `keystore.properties` ở gốc repo (gitignored; bản sao `~/.kachi/`). `assembleRelease` KHÔNG có keystore.properties ⇒ fail có chủ ý. OTA: `UpdateChecker` REPO = `dangkhoi/byd-kachi`, tên `apk/Kachi-<ver>-release.apk` trên `main` (nhận cả tên cũ), `.gitignore` mở `apk/Kachi-*`, hướng dẫn `apk/README.md`. ⚠ `.kiro/` bị gitignore cho tệp MỚI ⇒ `conversation-protocol.md` + `trace-den-tan-cung.md` là CỤC BỘ (máy khác chép từ `../byd-cluster-2/.kiro/steering/`); file này và 2 steering cũ vẫn được track. Owner chốt cùng ngày: S3 gỡ hẳn MainActivity (2 tệp seal T11 để nguyên đĩa) · U7 làm lại TOÀN BỘ icon đồng bộ + hình xe theo vị trí · lượt sau: hình xe cho widget board (Tyre/Radar/SideBoardView).
- **2026-09-13 (tối) · W5 (camera 360 + phím vô-lăng theo ngữ cảnh): SPEC `docs/specs/kachi-camera-context-keys.html`, KHÔNG code** — [CHƯA BIẾT] mã phím tăng/giảm âm + trái/phải khi app cam tiền cảnh, phím có tới `NavAccessibilityService.onKeyEvent` không, lệnh `camera_view` có ăn khi app cam mở không ⇒ CLAUDE.md §14 tầng 1: kịch bản đo ở spec §6 (getevent -lt + Học phím). U9 (3 bảng widget trên khung xe icon v2, spec `kachi-car-boards.html`) + U10/U11 đang làm off-car.
- **2026-09-14 · U12 — bộ chỉnh vị trí bong bóng VietMap luôn hiện** (owner: "mất chỗ cấu hình vị trí trên cụm"). [ĐO] IA v2 ẩn stepper + kéo-thả khi chưa bật chiếu/công tắc (return sớm) ⇒ khôi phục hành vi màn cũ: luôn dựng, mờ 0.4 + khoá + 3 câu nhắc (`SettingsNavSection.applyBubbleGate`). Bài học: **cắt vì R4 (nhóm dài) không được biến thành ẩn tính năng** — mờ + nói lý do, không xoá khỏi mắt.
- **2026-09-14 · S4 — HỒ SƠ LÀ TẤT CẢ: T1 (`:core`) + T2 (`:app` dữ liệu + gỡ cảnh + nối dây) + T3 (UI) xong off-car, 5 module 0 đỏ.** Spec `docs/specs/kachi-profiles-are-everything.html`. Owner 2026-09-14: *"có cảnh rồi có hồ sơ nữa hơi khó hiểu… profile cover bố cục, các cấu hình tất cả mọi thứ… bỏ luôn các nút đổi bố cục trên header"*.
  - **Khái niệm "cảnh" XOÁ HẲN** — `SceneBook`/`SceneBookTest` (`:core`) · `SceneController`/`SettingsSceneSection`/`SceneWiringContractTest` (`:app`) · 4 intent cảnh ở `HomeViewModel` · 23 chuỗi `kachi_scene*`/`kachi_cancel` (cả hai tệp strings) · 5 icon `ic_layout_*`. Dữ liệu chuyển **một lần** qua `ScenesMigration` (`:core`, thuần, có test) + `WorkspacePrefs.migrateScenesOnce()` — chạy ở `init` của `PrefsWorkspaceRepository`, tức **trước** lượt dọn rác widget đầu tiên (nếu không, chính lượt nâng cấp xoá vĩnh viễn widget nằm trong cảnh).
  - **Phạm vi khoá khai MỘT chỗ ở `:core`**: `ProfileScope` (`LAUNCHER_SUFFIXES` / `DEVICE_KEYS` / `TRANSIENT_KEYS`, sinh bằng mã từ `SettingsCatalog`). `WorkspacePrefs.PROFILE_SUFFIXES` nay chỉ là `= ProfileScope.LAUNCHER_SUFFIXES`. `theme_mode`/`unit_prefs`/`wallpaper_prefs`/`launcher_autostart`/`lang` chuyển từ **chung cả máy → theo hồ sơ**, có đường **lùi khoá cũ một lần** để máy đang chạy không mất lựa chọn.
  - **Cấu hình ClusterNav theo hồ sơ bằng CHỤP–ÁP**: `PrefSnapshot` (`:core`, giữ kiểu) + `snapshotClusterNav`/`applyClusterNav` (`WorkspacePrefsProfile.kt`). Thứ tự đổi hồ sơ **là hợp đồng**: chụp A → đặt con trỏ → áp B → `broadcastLang` → `bridge.reapplyAll()`.
  - ⚠⚠ **[ĐO] 0 chỗ nào trong toàn repo đăng ký `registerOnSharedPreferenceChangeListener`** (`app/src/main` · `core/` · `car-integration/` · `vehicle-contracts/` · `offcar-planner/`) ⇒ ghi prefs xong là **đúng trên đĩa mà không có gì đang chạy biết**. Đó là lý do `ClusterNavBridgeReapply.reapplyAll()` tồn tại — nó **chỉ gọi lại applier đã có** (`speedSign.*` · `NavRepository.reapplyClusterMode` · `VmOverlayPosition.applyOnOpen` · `SeatComfortApplier.applyNow` · `Pm25FilterApplier.enable/disable`). Bảng đầy đủ ở §9 T2 của spec.
  - **Cố ý KHÔNG áp lại** (mỗi cái một lý do, không phải chỗ quên): `cast_enabled` (bật/tắt phiên chiếu = cụm trước mặt người lái tối đi/sáng lên vì một cú chạm chip → **backlog S4-OQ2**, owner chốt) · 5 khoá `autostart_*` của cast + `headless_autostart` + `recirc_on_start_enabled` (nghĩa của khoá là *"khi nổ máy"*) · `theme_choice` (`attachBaseContext`, lần mở sau đã đúng). **[CHƯA BIẾT]/thiếu**: `seat_level_1..3` chưa theo hồ sơ vì `SettingsCatalogClusterNav.KEYS` chỉ khai `seat_level_0` → **backlog S4-SEAT**.
  - **Chưa đo trên máy/xe**: mọi kết luận trên là [ĐO] **từ source**, không phải từ một lượt chạy thật. V-switch + V-mig chưa chạy (owner đang dùng máy ảo) — đó là việc của T4.
- **2026-09-14 · U13 — hồ sơ tài xế nói rõ nó giữ bố cục gì.** Cơ chế: `WorkspacePrefs.key(suffix) = "<hồ sơ đang dùng>__<suffix>"` cho preset/slot_0..5/dock/top_strip/grid/scenes/boot_scene; chung cả máy: theme/unit/wallpaper/recent/autostart + toàn bộ prefs ClusterNav. UI nay nói ra ở đầu nhóm Màn hình chính + Thanh nút và trên từng thẻ hồ sơ (`ProfileNames.summary`). Bài học: **cơ chế đúng mà UI không nói = với owner là "không có"** (cùng họ U12).
- **2026-09-14 (chiều) · S4 HỒ SƠ LÀ TẤT CẢ — DONE off-car, 1.47 (48) đăng OTA.** Cảnh gỡ hẳn (migration một lần → hồ sơ cùng tên + hồ sơ nổ máy; [ĐO] chạy đúng trên prefs máy ảo của owner). Hồ sơ giữ cả theme/units/wallpaper/lang/autostart + ảnh chụp ClusterNav (`WorkspacePrefsProfile.snapshot/apply` + `ClusterNavBridge.reapplyAll`); `cast_enabled` theo XE (senior: đổi hồ sơ không được tắt/bật phiên chiếu). Thanh trên: chip hồ sơ (bộ chọn) + Apps/Cài đặt chỉ icon (`ic_grid`/`ic_gear` — `ic_sys_g` là mặt trời, đừng dùng cho Cài đặt) + tối đa 8 chip; `CapabilityKind.LAUNCHER` (`launcher_apps`/`launcher_settings`) lên được thanh nút. Nợ: S4-SEAT (seat_level_1..3 chưa theo hồ sơ), V-switch trên xe.
- **2026-09-13 (tối) · U9 BA BẢNG WIDGET TRÊN KHUNG XE CỦA BỘ ICON v2. DONE off-car (lốp · cảm biến đỗ · ADAS hai bên), APK release 1.42/43 KHÔNG bump.** Spec `docs/specs/kachi-car-boards.html`. **Kiến trúc cần nhớ**: `app/.../launcher/CarFrames.kt` (`internal object`) là **nguồn hình xe DUY NHẤT** của tầng vẽ — giữ chuỗi path SVG **chép nguyên văn** từ script sinh icon U7 (`TOP_BODY`/`TOP_GLASS`/`WHEEL_FILL` của `gen_car4.py`, cũng là `android:pathData` trong `res/drawable/ic_car_top_*.xml`), đọc **một lần** (`by lazy`) qua `androidx.core.graphics.PathParser` (**dependency mới `androidx.core:core:1.19.0`**; `android.util.PathParser` của nền tảng là @hide). `TyreBoardView`/`RadarBoardView`/`SideBoardView` bỏ hẳn `drawRoundRect`+`drawLine` tự vẽ, phóng bằng `Matrix.ScaleToFit.CENTER` (**giữ tỉ lệ**) vào đúng khung cũ; bảng lốp canh ô giá trị theo **tâm bánh đã vẽ** (`mapRect`), không suy từ tên góc. Khoá: `CarFramesSourceContractTest` so path **hai chiều** với `res/drawable` (lệch 1 ký tự ⇒ đỏ) + cấm tệp khác giữ chuỗi path. [ĐO] app+core **2355/0**; 5 ảnh máy ảo (1 ô · 2 cột): tỉ lệ thân xe **0.609–0.623** ở 4 cỡ vs **0.611** đường tâm icon ⇒ một chiếc xe; ô giá trị lệch tâm bánh **0–0.5 px**; nét khung 12 px (sàn `KachiSpace.STROKE` 2dp). **Còn nợ**: vùng tô **cửa/cốp/ca-pô** (owner nêu "mở cửa, mở cốp" — chưa bảng nào vẽ; `CarFrames` cố ý chưa giữ path đó vì hằng không ai gọi thì không bài canh nào thấy nó trôi) · màu trạng thái bánh **[CHƯA BIẾT]** off-car (mọi bánh `UNKNOWN`, dự án cấm dữ liệu giả) · chưa có ảnh chủ đề SÁNG. **Soát viên cấp cao 2026-09-13 — APPROVED** (spec §10): 2 vá — [P1] dấu "chưa kiểm" còn **bề mặt thứ ba** (góc tiêu đề ô nhóm `GroupTileView.header` vẫn hổ phách sau khi U10 đã hạ thanh nút) ⇒ gom nốt về `PickerBadge.dot`; [P2] U11 gộp cả 5 `LocalShellFailure` vào một câu ⇒ thêm `UpdateChecker.channelRemedy` (ca `AWAITING_APPROVAL` phải nói "bấm Cho phép" chứ không đẩy vào Cài đặt). **U10 + U11 DONE off-car cùng ngày.** [ĐO] 2522 bài/0 đỏ (`--rerun-tasks`, 5 module). **Cần nhớ**: dấu *"chưa kiểm trên xe"* nay chỉ còn MỘT hàm vẽ = `PickerBadge.dot` (bộ chọn · thanh nút · đầu ô nhóm); bề mặt thứ tư `WidgetViews.badgeView` (chip CHỮ) cố ý giữ hổ phách, chờ owner. `LocalDeviceShell.installApk` trả `LocalInstallOutcome` (Ok/NoShellChannel/PmRejected) — [ĐO] bytecode dadb: `install` trả `InstallResult` và KHÔNG ném khi pm từ chối, `supportsFeature` chỉ ép chính cái bắt tay lười mà `install` vốn gọi (không thêm bước trên dây).
- **2026-09-13 (tối, pha 2) · U9 BẢNG THỨ TƯ: *Cửa & khoang* (`g_doors`) — DONE off-car, APK 1.43 (44) KHÔNG bump.** `g_doors` đổi **STRIP → BOARD**. `:core` `GroupBoard.doorPlan` gộp **10 datum ⇒ 8 bộ phận** (cốp/nóc mỗi thứ có trạng-thái + phần-trăm; sắc thái lấy cái NẶNG hơn) + dựng câu kết luận phân biệt *"đã đóng"* / *"còn n chưa đọc"* / *"chưa đọc được"*; chỗ nối `:core`↔`:app` là enum **`CarPart`** (tầng vẽ bị cấm nhắc mã `door_`). `:app` `DoorBoardView` + `GroupBoardBinder` (tách khỏi `GroupTileViews.kt`, 492→464, phải khai vào `tileFiles` của bài canh). `CarFrames` thêm 9 chuỗi path bộ phận + `openFrameBounds` (vạt cửa mở ra `x 3.9…20.1`, phóng theo thân sẽ cắt cụt). **Ba bất biến mới cần nhớ**: (1) `CapabilityGroups.SHAPES_WITH_ACTIONS` = `{STRIP, BOARD}` — BOARD-có-nút thì thân ô phải lấy `weight` (ô vẽ Canvas `MATCH_PARENT` nuốt trọn chỗ ⇒ hàng nút bị cắt IM LẶNG); (2) vùng tô phải có **đường tách màu nền thẻ** (nóc + rèm chồng nhau, cùng ACTIVE ⇒ dính thành một khối); (3) nhãn `%` vẽ ở **lượt cuối** và mực theo **thứ nằm dưới** (khoét khi nằm trong vùng tô). [ĐO] 6 ảnh máy ảo: tỉ lệ hộp mực **0.613** (thân) / **1.027–1.029** (thân+bộ phận) vs **0.611 / 1.032** của icon; hàng nút còn **18 px** lề dưới; **ảnh chủ đề SÁNG khớp từng pixel** (trả nợ (c) của pha 1). app+core **2368/0**. **Còn nợ**: **ca-pô KHÔNG có datum nào đọc** (`CarStatus.Body` không có field, `hood` chỉ là nút `NEEDS_CAR` ngoài nhóm) ⇒ cố ý không vẽ, chờ owner (spec OQ2) · [CHƯA BIẾT] thang 0–100 của `tailgate_position`/`sunroof_pos`/`sunshade_pct` + `mirror_fold` trên xe thật.
- **2026-09-13 (sáng) · D2 + U8(b) + U6 — dọn nợ + icon/nhãn đợt 2. DONE off-car, APK 1.40 (41).** D2: token chết gỡ (`HEAD_BG`/`SCRIM_BTN2`/`HEAD_BAR`); `queryIntentActivities` [ĐO] 7 chỗ → `system/PackageQueries.queryActivities` (rẽ nhánh API 33, bài canh cấm gọi thẳng, quét 3 module); 2 test file dài tách; `SplitDelegate.getTouchDelegateInfo()` (a11y). U8(b): toast thiếu kênh shell một lần/phiên (`AtomicBoolean`, RAM có chủ ý). U6: 23 icon mới + `ic_motor` vẽ lại, `displayLabel` = tên / `displaySub` = gợi ý loại, `HIDDEN_FROM_PICKER` (ẩn khỏi bộ chọn, `pick()` vẫn tra), icon phân biệt 34 → 56, `CapabilityIconsDiversityTest` ghim trần ≤3 ô/hình + trần đo được 5 nhóm chưa chạm (U7 chờ owner: icon theo góc/vị trí?).
- **2026-09-13 (đêm→sáng) · S2 SETTINGS IA v2 + GỘP CLUSTERNAV THÀNH MỘT APP. DONE off-car, senior APPROVED, 4 pass soát ảnh, APK 1.39 (40), ĐÃ PUSH.** Spec `docs/specs/kachi-settings-ia-v2.html`. **Kiến trúc mới cần nhớ**: (1) Settings 10 nhóm (`SettingsCatalogGroups` :core: home · bars · display · profiles · nav · cast · keys · car · system · about), 56 mục, mỗi mục ↔ ≥1 control (`SettingsScreenWiringContractTest` hai chiều). (2) **`ClusterNavBridge`** (:app, 3 tệp + `BridgeMsg`) = cầu DUY NHẤT cho Settings đọc/ghi khoá ClusterNav (`Prefs`, `SimpleCastRuntime.coordinator().prefs`, `NavConnect`, applier ghế/PM2.5, `VmOverlayPosition`); section KHÔNG được gọi thẳng (`ClusterNavSettingsWiringContractTest`); cầu KHÔNG mang chữ (toast qua `BridgeMsg` → `KachiHomeWiring.bridgeMsgRes`). (3) **Màn ClusterNav cũ (`MainActivity` + `activity_main.xml` byte-seal T11) KHÔNG sửa** — nay là "màn nâng cao" mở từ Hệ thống › Nâng cao; **một icon** LAUNCHER = `KachiHomeActivity` (`SingleLauncherIconContractTest`). (4) Chủ đề: `PrefsWorkspaceRepository.persist` gương sang `clusternav_theme` (chỉ khi đổi). (5) Lưới 123 ô rời Settings: `DrawerController.openDockPicker` + `DockSelection` (:core); `CapabilityTileGrid.UniformRow` đo 2 pha (ô chèn cao 0 + `forceUniformHeight` = bẫy đã chứng minh từ source AOSP). (6) Nút ⇄ ô: `SlotSwapButton` dùng chung 2 đường (nhúng `centered` trong suốt; freeform `strip` dải khung ô che caption hệ, `Sp.CAPTION_COVER = 44`); ẩn khi Cài đặt/bảng vẽ mở. Còn: V-oncar; OQ1 gỡ màn cũ; D2; U8.
- **2026-09-12 (tối) · DESIGN SYSTEM PHA 2 — lan type scale ra 5 bề mặt ngoài Settings (OQ1-a). DONE off-car, senior APPROVED, soát ảnh ĐẠT CÓ ĐIỀU KIỆN.** AppDrawer · KachiTopStrip · OverlayHeads · WorkspaceView · LayoutEditorPanel → `KachiType.apply`; ngoại lệ cỡ chữ khai marker `[type scale]` + lý do ≥12 ký tự tại CHÍNH dòng mã, số dòng miễn ghim = 3. Vá lẻ: ô preset sáng lấy từ `EffectiveLayout.highlightedPreset` (:core, hỏi `usable` — bố cục tự vẽ hỏng ⇒ sáng lại preset); nút ⇄ viền HAIRLINE. **Owner chốt chip giữ 44.** [ĐO] app+core 2251/0. Còn: lưới ô nhóm trong NGĂN KÉO dính 0px (P1, cùng bệnh pha 1 nhưng bề mặt khác) · −/+ thanh nút 14–22dp · 4 chiều cao nút · OQ1-b/c/d · OQ4. Spec `kachi-design-system.html` §10 Pass 2.
- **2026-09-12 (tối) · OWNER GIAO (chạy tự chủ qua đêm, duyệt sau)**: (1) Settings "còn lổn nhổn" — soát kỹ + vá hết; (2) **gộp settings ClusterNav (MainActivity 1386 dòng) vào Kachi Settings thành MỘT app**, đồng bộ, sắp xếp, category lại. Đảo quyết định R5 của `kachi-settings-screen.html` ("không đụng màn ClusterNav cũ"). Spec mới `kachi-settings-ia-v2.html`. (3) Xong thì làm tiếp backlog tới hết, không dừng hỏi.
- **2026-09-12 (chiều-3) · DESIGN SYSTEM + IA THẬT (owner: "Settings vẫn lộn xộn, nút dính nhau, checkbox/button sai style; có IA/design system thật chưa?"). DONE off-car, senior APPROVED, ĐÃ PUSH.** Spec `docs/specs/kachi-design-system.html`. 3 commit `07ca70c`+`6050d3a`+`328f16e`. **[ĐO] 3255 công bố / 0 đỏ** (73/73 rerun).
  - **TRẢ LỜI THẲNG (đo được): CHƯA có design system thật** — có tokens rời (màu `KachiPalette`/`KachiTheme`, khoảng cách `KachiSpace`) + khung IA (`SettingsCatalog` 7 nhóm), nhưng thiếu 3 thứ nền: (1) **type scale** — [ĐO] 18 cỡ chữ tay (9.5→32sp); (2) **component library** — nút/chip/nền dựng tay ở 14 tệp; (3) **quy tắc khoảng cách giữa phần tử** — `checkRow` là thẻ không có lề ngoài ⇒ dính. Các fix trước chỉ chữa triệu chứng.
  - **Nền dựng**: `KachiType` (:app) 5 bậc DISPLAY28/TITLE20/SECTION16/BODY13.5/CAPTION12 + `apply()`. `SettingsRows` nâng thành **bộ component chuẩn**: mọi cỡ chữ qua KachiType; mọi hàng tự mang **LỀ NGOÀI stack** (`stackLp`: gap Sp.S, section header topMargin Sp.L) ⇒ hết dính; `addChip` dùng chung; checkbox/button/chip style CỐ ĐỊNH (chỉ đổi tô). Chuyển HẾT setTextSize số tay ở 5 tệp Settings.
  - **Lưới ô nhóm (`CapabilityGridSection`) — CHÍNH "nút widget dính nhau" owner báo**: [ĐO] dính 0-4px ⇒ thêm khe dọc Sp.S + ngang Sp.XS; ô cao MATCH_PARENT (đồng cao hàng) + căn TRÊN ⇒ ô nhãn ngắn/đang-chọn hết lệch (verify: 0-1px). Chip ngắn thành hình tròn ⇒ minWidth Sp.TOUCH (pill); chip chọn/tắt lệch 4px cao ⇒ minHeight Sp.ICON_XL (đồng cao 0px).
  - **Test khoá**: `TypeScaleContractTest` (cấm cỡ chữ tay trong bề mặt Settings + khoá đúng 5 bậc bằng so-tập-tên) + `SettingsStackMarginContractTest` (lề stack 2 chiều: hàm dựng phải tự đặt layoutParams, chỗ gọi không truyền lp). Senior review củng cố cả hai (bài đầu bỏ qua `textSize=`/`setTextSize(Nf)`/vắt-dòng; bài "5 bậc" không đỏ được khi thêm bậc).
  - ⚠⚠ **Bài học lặp lại — soát viên độc lập (3 vòng đọc ảnh) tìm ra cái mã+test không thấy**: sau khi áp type scale + component, hàng Settings hết dính (đạt) NHƯNG **lưới ô nhóm vẫn dính** (component khác chưa áp) — đúng "nút widget dính" owner báo. Phải mở ảnh xem, và để LUỒNG ĐỘC LẬP xem, mới ra.
  - **CÒN TỒN (OQ/pha sau)**: (c) màn Home-settings 2 cấp tiêu đề (nhóm cha "Individual items" vs lĩnh vực con "Energy & charging") **cùng cỡ SECTION** — chưa có cấp sub-header nhỏ hơn; 31 chỗ `setTextSize(KachiType.X)`+typeface tay chưa gộp `apply()`; `CapabilityGridSection` còn 2 cỡ tay 11.5/10 (ô lưới mật độ cao, ngoài 5 bậc); chip cao 44<48 (owner quyết); **lan design system ra ngăn kéo/thanh nút/widget (14 tệp) = pha sau (OQ1)**; chưa đo trên xe.

- **2026-09-12 (chiều-2) · SỬA GIAO DIỆN owner báo lộn xộn — 12 lỗi (10 + 2 senior). DONE off-car, senior APPROVED, ĐÃ PUSH.** Owner: *"UIUX chạy lộn xộn, đặc biệt Settings; chọn app chiếu vào ô lệch loạn = regression"*. Soát ảnh emulator qua luồng ĐỘC LẬP + tự kiểm gốc bằng code trước khi vá. Commit `375d44f` + `95c2f28`. **[ĐO] 3247 công bố / 0 đỏ** (`--rerun-tasks`, 73/73).
  - ⚠⚠ **HỒI QUY chọn-app-vào-ô = xác nhận + gốc rõ**: màn "Đặt widget/app vào ô" nhảy cột **3→4→6** giữa CÙNG vùng cuộn (G1 chèn phần Nhóm 3 cột lên đầu, phần cũ 4/6) ⇒ cuộn xuống tâm cột lệch trái-phải = "lệch loạn". Vá: **một vùng cuộn = MỘT lưới cột** — mọi phần ô-khả-năng về 4 cột, app 6; số cột đưa về **`:core CapabilityPicker.COLS`** (trước nằm 3 chỗ ⇒ đặt lại `cols=3` là hồi quy mọc lại IM LẶNG) + `PickGridColumnContractTest` **2 lớp** (chặn số trần + chặn tập token). [ĐO] verify ảnh độc lập: cột **303/741/1179/1617 bước 438px** thẳng đều mọi vị trí cuộn.
  - **Settings lộn xộn** (verify ảnh ĐẠT): tiêu đề nhóm mờ (`MUT2`) + nhỏ hơn body ⇒ đổi **sáng `INK` + đậm** (AppDrawer · SettingsRows · TopStripPicker — 3 bộ dựng, sót 1 chỗ phát hiện khi verify) · nền bảng bán trong suốt lộ Home (ghost "Cài đặt"/avatar) ⇒ **scrim đục hẳn `#ff…`** (owner chọn gọn) · nút bật hình nền TẮT mà 3 dãy pill dưới vẫn bấm được (nút chết) ⇒ **ẩn khi tắt** · dùng bố cục tự vẽ mà vẫn sáng preset "1 ô" ⇒ không tô preset khi custom · nút Xoá hồ sơ luôn hiện (mời xoá hồ sơ đang dùng) ⇒ **chỉ hiện khi xoá được** (+sửa test khoá giao kèo mới).
  - **Ô Home** (verify code+test, CHƯA verify ảnh — off-car nhóm render mini-card, không dựng được board lớn): nhãn ADAS dính sát hình xe ~5-7px ⇒ **khoảng cách có SÀN** (SideBoardView) · câu "chưa đọc được" (verdict lốp) phình ~43px = TO NHẤT màn ⇒ **cap trần 16sp**, và senior bắt thêm: **dòng phụ từng bánh cũng phải cap** (nếu không sub 37.8px > verdict 24px, đảo thứ bậc NGƯỢC lại) · ô Mức xăng chỉ-đọc nhồi vỏ stepper, đơn vị % xuống dòng bị cắt ⇒ **gộp giá trị+đơn vị một hàng** + ellipsize.
  - ⚠ **CHỈ đo được TRÊN XE** (reviewer ghi rõ): đơn vị ô dock hẹp có thể bóp về 0 khi giá trị dài (off-car mọi giá trị "—"); scrim bảng SÁNG nay gần đen quanh viền (hệ quả owner chọn đục hẳn).
  - ⚠ **Bài học lặp lại**: các fix Home tiles là cơ học + test xanh nhưng **verify ảnh bị chặn off-car** (nhóm chỉ render board lớn khi ô đủ to; Home mặc định dùng mini-card). Cụm Settings + chọn-app-vào-ô thì verify ảnh ĐẠT. ⇒ owner-primary đã chứng bằng ảnh, phần Home tiles chờ xe.

- **2026-09-12 (chiều) · LƯỢT SOÁT ĐỘC LẬP sau P7/P6 — vá 20 lỗi. DONE off-car, đã ghi + đang commit.** Bản đầy đủ `docs/_handoff/session-2026-09-12-audit-after-p7-p6.md` · Reviewer Log spec `kachi-scenes-and-widgets.html` §9. **[ĐO] 3239 công bố / 0 đỏ** (`--rerun-tasks`, 73/73 tác vụ chạy thật) ⇒ **+48** so mốc P7/P6 (3191).
  - ⚠⚠ **P0 duy nhất — GIAO ĐIỂM P7×P6, đúng "chỗ không ai thử"**: đổi hồ sơ tài xế **xoá vĩnh viễn widget của hồ sơ kia** ([ĐO] id 654 biến khỏi `dumpsys`, ô báo sai "app đã bị gỡ" mà app vẫn cài). Gốc ở **KIỂU DỮ LIỆU**: id widget do nền tảng cấp cho một HOST (mọi hồ sơ), nhưng `HomeUiState` chỉ mang dữ liệu **hồ sơ đang dùng** ⇒ lượt dọn rác tính "còn ai dùng" hẹp hơn sự thật. Vá: `AppWidgetIds` thu id qua **hợp** id mọi hồ sơ. **Lần nữa: hai tính năng mới cùng phiên phải đo GIAO ĐIỂM.**
  - **P1 ×4**: `headl`/`headlight_mode` cùng nhóm gửi y hệt một byte (bỏ `headl` khỏi nhóm — G1 làm miễn-trừ "hai mục rời" hết đúng) · hình nền mở đầu giải mã cỡ view=0 · tắt hình nền ảnh quay lại · đổi hồ sơ không nạp lại bố cục. **P2 ×9** hiệu năng/nền (nhịp chạy khi HOME bị che · giải mã ảnh trên thread chính · luật "không hex" phủ màu-dựng-bằng-số · huỷ giải mã đang bay · tách I/O ảnh khỏi dadb · đọc nhạc một lần/lượt · thanh trên chỉ đổi chữ…). **P3 ×6**: **tên hồ sơ "Mặc định" lọt màn EN** → tách **khoá** không dịch ↔ **nhãn** dịch (`ProfileNames.kt` mới) · nhãn widget bên thứ ba trùng tên (`AppWidgetLabels.kt` mới) · dòng phụ nhóm Đèn nói sai · số "4 lớp"→"5 lớp".
  - ⚠ Lượt soát **KHÔNG đụng** `AppDrawer`/`WorkspaceView`/`LauncherWindows`/`AppOpener` ⇒ **hồi quy "chọn app chiếu vào ô"** (owner báo 09-12) + giao diện Settings lộn xộn nằm ở mốc **đã lưu** → **việc TIẾP THEO** (soát ảnh độc lập vs prototype + bisect).
- **2026-09-12 (trưa) · P7 CẢNH KHỞI ĐỘNG + P6 WIDGET BÊN THỨ BA. DONE off-car.** Spec `docs/specs/kachi-scenes-and-widgets.html`. **[ĐO] 3191 test / 0 đỏ** (trước phiên: 3059 ⇒ **+132**), APK sạch, niêm phong khớp hằng ghim.
  - **Hai mục backlog hoá ra là MỘT khái niệm**: "cấu hình khởi động" của P7 và "cảnh" của P6 cùng là *một bộ (bố cục + nội dung từng ô + thanh nút) có tên, gọi lại được*. Làm một lần dùng cho cả hai ⇒ tránh hai bộ lưu song song (bẫy hai-bản-sao).
  - ⚠⚠ **HAI LỖI P0 NẰM Ở GIAO ĐIỂM HAI TÍNH NĂNG — chỗ không ai thử.** Cảnh đúng khi đứng riêng, widget đúng khi đứng riêng, nhưng lưu một cảnh **có widget bên thứ ba** rồi gọi lại thì vỡ hoàn toàn, và cả hai đều **IM LẶNG**: (1) `SlotCodec` dùng `|` ngăn id với nhà cung cấp — **trùng dấu ngăn TRƯỜNG** của chuỗi cảnh ⇒ bản ghi ra 8 trường/cần 7 ⇒ `decode` bỏ cả cảnh; **23 bài `SceneBookTest` đã có không bài nào đi qua loại ô thứ tư**; (2) lượt dọn rác id chỉ xét bố cục đang dùng, không xét **cảnh đã lưu** ⇒ gọi lại cảnh ra thẻ *"widget không còn — app đã bị gỡ"* trong khi app **vẫn còn nguyên trên máy** (còn **báo sai nguyên nhân**). Vá: đổi dấu ngăn + `SceneBook.RESERVED` công khai + bài quét **mọi loại ô** bằng `when` không `else` ⇒ thêm loại ô thứ năm mà quên là **không biên dịch được** (chốt bằng KIỂU, không bằng lời nhắc). **LUẬT: hai tính năng mới trong cùng phiên thì phải đo GIAO ĐIỂM của chúng, không chỉ đo từng cái.**
  - ⚠⚠ **Ô widget TRỐNG dù ba nguồn đều nói OK** (`dumpsys` ghi `views=RemoteViews@…`, cây view có `LinearLayout` đồng hồ kèm `text="12:47"`, prefs đã lưu): vùng ô 1242×684 có **99.84% một màu nền**, `TextView` giờ đo được **4×1 px**. Hai nguyên nhân xếp lớp: `AppWidgetHostView` **không tự khai cỡ** nên nhà cung cấp dựng RemoteViews ở cỡ 0; và bản vá đầu truyền `Bundle.EMPTY` — **bundle không ghi được** mà `updateAppWidgetSize` lại GHI khoá cỡ vào chính nó ⇒ ném `UnsupportedOperationException` ⇒ ô trống **y như chưa vá**, đội lốt "bản vá không ăn". Tìm ra chỉ vì nhánh lỗi có ghi log kèm tên ngoại lệ. Sau vá: TextView **503×243px** + **194×33px**, 3276 điểm sáng > 220 (trước **0**). **Lần thứ TƯ của dự án: mã đúng + test xanh ≠ đã giao được.**
  - **Việc đầu tiên của widget bên thứ ba là ĐO xem có làm được không, không phải code**: `BIND_APPWIDGET` mức *signature|privileged* ⇒ `pm grant` trả nguyên văn `not a changeable permission type` (exit 255). Nhưng **đường thứ hai mở được**: `appwidget grantbind --package … --user 0` (exit 0, `dumpsys appwidget` chuyển `Grants:` rỗng → có) — và đó **chính là kênh dự án đã proven TRÊN XE** cho badge tốc-độ VietMap (bản 1.13) ⇒ không vướng tiền lệ hộp thoại hệ thống bị khoá trên IVI (màn Notification access). Cấp lúc người dùng chọn, KHÔNG thêm điều kiện thứ 7 vào vòng kiểm P8.
  - **Host id RIÊNG `0x4B41`** tách hẳn host VietMap `0x564D` (đang giữ 3 widget badge **đã chạy trên xe**) — `appWidgetIds` là per-host nên lượt dọn rác không thấy id của VietMap; [ĐO] 3 widget đó **alive mọi lần kiểm** suốt cả lượt đo. Vẫn bịt lỗ lý thuyết: `deleteAppWidgetId` của nền tảng chỉ kiểm uid/gói **chứ không kiểm id thuộc đúng host**, mà hai host cùng gói ⇒ id đọc từ đĩa nay bị **giao với danh sách của host này** trước khi xoá.
  - **Ngữ nghĩa cảnh khởi động: khởi động NGUỘI tiến trình**, một lần mỗi lần tiến trình sống. Đã dựng xong đường "đúng lúc máy nổ" (móc `KachiAutostart.runBoot`, dự án phân biệt được thật vì có `BOOT_COMPLETED`) rồi **BỎ** vì ba lỗ không vá được ở tầng đó: `runBoot` **thoát ngay dòng đầu** khi người dùng tắt `launcher_autostart` ⇒ cảnh chết theo một công tắc **không liên quan**, im lặng · nó cũng chạy khi **cài bản mới** (≠ nổ máy) ⇒ ngữ nghĩa đã sai ngay trong chính đường đóng dấu · cổng nguội 30s có thể bỏ lượt ⇒ cảnh lên thất thường. Trả giá bằng: hàng cảnh **nói thẳng** hợp đồng · chạm lại "Nổ máy" = bỏ dấu · cảnh **chỉ chạm vùng làm việc** (không kéo hình nền/chip/đơn vị/giao diện/ngôn ngữ/hồ sơ vào — test khoá cả 7).
  - **[ĐO] R4 (C5 của dự án) đạt**: gọi cảnh có cùng nội dung ô App ⇒ **PID app 21156 → 21156** (không mở lại), mà ô khác vẫn đổi thật (15.8% pixel). Đo đối chứng còn **bác** một quy kết sai: ô app rơi từ *đang chiếu sống* về *thẻ app* sau khi gọi cảnh — nhưng mở Cài đặt rồi bấm Xong **không chạm cảnh nào** cũng rơi y hệt ⇒ hành vi CÓ TRƯỚC của bảng Cài đặt che bề mặt ô.
  - **Widget bên thứ ba mất chữ trên bảng SÁNG**: RemoteViews của app khác dùng chữ trắng theo quy ước launcher-nền-tối, launcher không sửa được nội dung đó ⇒ [ĐO] bảng sáng còn **0.15%** mực tối = chữ biến mất. Vá bằng **nền tối cố định** sau widget, đúng tiền lệ nút ⇄ của U5 (vai không được theo chủ đề vì nằm trên pixel của người khác); sau vá **77.84%**.
  - **3 loại ô cũ giữ đúng từng byte** dạng lưu (`app:`/`widget:`/rỗng) ⇒ cấu hình đã lưu trên xe đọc lên nguyên vẹn.
  - ⚠ **TAI NẠN CỦA LƯỢT SOÁT — thành LUẬT**: script thử phá hoàn nguyên bằng `git checkout -- <tệp>` trên **cây chưa commit** ⇒ lệnh trả về **HEAD**, không trả về "bản trước khi thử phá" ⇒ **xoá sạch phần việc của phiên ở 4 tệp** (với tệp MỚI chưa track thì lệnh thất bại nên còn để lại phép phá trong mã sản phẩm). Phục hồi được và **kiểm được** (diffstat khớp từng con số). **LUẬT: cây chưa commit thì TUYỆT ĐỐI không hoàn nguyên bằng git — sao lưu theo tệp.**
  - ⚠ **Không gõ được tên cảnh khi có app đang chiếu trong ô**: [ĐO] `dumpsys window` cho `mCurrentFocus = <app>`, và chạm vào ô nhập **vẫn không chuyển focus**. Hộp thoại tên hồ sơ có trước dùng **cùng khuôn `AlertDialog`** nên gần như chắc chắn cũng bị. Sửa việc này chạm đúng lớp chiếu-app (nơi sinh P-bug1/P-bug2) ⇒ cố ý không tự làm.
  - **Một bài canh là TRANG TRÍ**: đổi `if (coldStartDone)` thành `if (false)` mà **vẫn xanh** vì tên cờ còn ở dòng gán bên dưới ⇒ đã đòi đúng phép gác.
  - **Còn tồn**: **chưa đo gì trên xe** (`appwidget grantbind` trên IVI khoá · `updateAppWidgetSize` với cỡ ô thật · **reboot vật lý**) · chỉ đo 1 nhà cung cấp (widget dạng collection như Gmail/Calendar là đường khác) · gọi cảnh thay app bằng widget **không gỡ sổ vị trí app** (off-car không lộ, trên xe có thể để lại cửa sổ treo) · OQ2: nếu trên xe bind bị từ chối thì ẩn hẳn mục hay giữ câu nói thật.

- **2026-09-12 (sáng) · U5 ĐA NGÔN NGỮ + BẢNG MÀU SÁNG. DONE off-car. Đây là mục off-car CUỐI CÙNG có quy mô.** Spec `docs/specs/kachi-i18n-and-light-theme.html`. **[ĐO] 3059 test / 0 đỏ** (trước phiên: 2960 ⇒ **+99**), APK sạch, niêm phong nguyên.
  - **Hai tầng, hai cách, MỘT công tắc**: `:core` **262 nhãn EN** dạng **tham số mặc định** (`labelEn`/`subEn`/`shortEn` + `interface Localized`) ⇒ 0 churn trên 123 dòng registry, và **hàng trăm test đang assert chuỗi tiếng Việt vẫn xanh** (đúng khuôn `TelemetrySpec.short` của RW0). `:app` **123 khoá tài nguyên** ở **TỆP MỚI** `strings_kachi.xml` + `values-en/` — Android gộp mọi tệp trong `values/` nên hoạt động y hệt mà **niêm phong `strings.xml` KHÔNG bị đụng** (khỏi xin ghim lại).
  - ⚠ **SAI LỆCH SPEC CÓ CHỦ Ý — chỗ lưu ngôn ngữ**: spec ghi tạo khoá mới ở `WorkspacePrefs`, nhưng làm vậy thì một APK có **HAI** công tắc ngôn ngữ (launcher + màn ClusterNav đang đọc `seg_language`) = đúng **bẫy hai-bản-sao** dự án đã trả giá 4 lần; biểu hiện: chọn English ở Kachi rồi mở màn ClusterNav thì màn đó **vẫn tiếng Việt**. ⇒ dùng chỗ lưu của ClusterNav (`clusternav_lang`) làm chỗ DUY NHẤT, `:core Strings.current` chỉ là **hình chiếu lúc vẽ**; vẫn uỷ quyền qua `WorkspacePrefs` để tầng UI giữ 0 lần ghi bền trực tiếp. Có bài canh đối chiếu `LangMode.code` ↔ `Lang.Choice.code` **từng ký tự**.
  - **Bảng màu SÁNG** (`KachiPalette` 54 vai × 2 bảng, chỗ DUY NHẤT được khai hex; `KachiTheme` 13 `const val` → 54 thuộc tính có getter, **tên giữ nguyên** nên 21 tệp không phải đổi cách gọi; `ThemeHost` là chủ sở hữu duy nhất). ⇒ **nút chủ đề của S1 hết là nút chết**, và **bài canh tự-đảo-chiều của S1 đã đảo đúng chiều mới** (nay đòi CÓ nút). Bảng sáng **không phải đảo ngược bảng tối**: nền sáng cần mực đậm hơn, viền rõ hơn, màu nhấn tối đi; thẻ phân biệt bằng **viền** chứ không bằng độ sáng.
  - ⚠⚠ **BÀI CANH BỎ SÓT ĐÚNG THỨ NÓ SINH RA ĐỂ BẮT — hai lần, hai lý do khác nhau**: (1) lượt soát độc lập tìm ra `"Xe"`/`"pin"`/`"WIDGET"` hiện giữa màn tiếng Anh; hai bài canh của chính phiên đều xanh vì bài quét-theo-dấu **mù với chữ Việt KHÔNG DẤU**, còn bài "mọi chữ đi qua tài nguyên" chỉ soi **9 dạng gọi của Android** mà launcher vẽ chữ qua **hàm dựng view của riêng nó** (`miniCard(`/`ring(`/`label(`) ⇒ **chính kênh chở chữ không được quét**; (2) phép chống-trùng nhãn EN bản đầu tha cả nhóm nếu *có* một mục đã trùng ở VI ⇒ phép thử phá **vẫn xanh**, phép đúng là so **theo từng cặp**. **LUẬT: thử phá phải kiểm cả BÀI CANH, không chỉ mã sản phẩm.**
  - ⚠ **BA con số kiểm kê của tôi trong spec đều sai**: "345 chuỗi ở `:app`" → trong **mã** chỉ **130** (đếm cả chuỗi trong **chú thích**, vì KDoc dự án viết tiếng Việt) và **bỏ sót chữ Việt KHÔNG DẤU** (`"Xong"`, `"khung"`) — chính lỗ hổng này về sau để lọt `"Xe"`/`"pin"` ra màn tiếng Anh · "17 mục cài đặt" → **16** · "78 hex / 17 tệp" (số của S1) → **83 / 21**.
  - ⚠ **HAI lỗi tương phản chỉ lộ khi ĐO PIXEL, không lộ khi đọc hằng**: nút ⇄ đầu ô **1.41:1** (tôi đặt scrim sáng theo phản xạ "bảng sáng thì scrim nhạt", nhưng nút đó nằm trên **pixel của app đang chiếu** ⇒ là vai DUY NHẤT không được theo chủ đề) · icon ô điều khiển đang BẬT **1.39:1** (icon trắng trên gradient **bán trong suốt**, nền sáng trộn ra (211,217,248) làm glyph biến mất, mà NHÃN vẫn đúng nên rất dễ lọt). Kèm **22 chỗ `Color.WHITE`** — không phải hex nên bài canh "0 hex" xanh trơn, mà bảng sáng có ≥9 chỗ thành **chữ trắng trên nền trắng**.
  - **Toast của ngăn kéo không giao được vì Z-ORDER, không vì toast hỏng**: [ĐO] `dumpsys window` — ngăn kéo là `APPLICATION_OVERLAY` lớp **121000** phủ kín màn, toast là `TOAST` lớp **81000** ⇒ toast nằm DƯỚI. Toast ở màn Cài đặt vẫn hiện vì bảng đó là con của cửa sổ Activity (~21000). ⇒ luật **bề mặt phủ không được nói bằng Toast**, có bài canh; sau vá dải chữ đổi **0 px → 59 363 px**.
  - **Ba mục tồn khác đã dọn**: nhãn sơ đồ ADAS mực 13→19px (cỡ chữ = `max(tỉ lệ, SÀN)`, số hàng **co theo chỗ** và `:core` quyết) · hàng nút nhóm Kính bị cắt đáy (`LinearLayout` đo con **theo thứ tự xếp** nên lưới đọc đứng trước lấy trọn trần, hàng nút tràn rồi bị cha cắt **không ném, không log**) ⇒ `onMeasure` **đo hai lượt** · nhãn ô hẹp dùng nhãn NGẮN (thêm `short`/`shortEn` cho `ControlDef`).
  - ⚠ **ĐO ĐƯỢC, CHƯA VÁ, cần owner quyết**: **đổi ngôn ngữ HOẶC chủ đề làm app trong ô bị giết và mở lại** (PID 17942→18211, màn ảo #172→#173). Cơ chế `recreate()`. Giảm nhẹ: app **về đúng ô** (không phải hình dạng P-bug2), tự phục hồi, giao diện có nói trước. Với ngôn ngữ thì `recreate()` gần như bắt buộc; với chủ đề thì về lý vá được (màu nằm trong object Kotlin) nhưng phải sửa đúng chỗ sinh ra P-bug1/P-bug2.
  - **Tiếng Anh ANH** xuyên suốt (`tyre`/`windscreen`/`colour`/`licence`), thuật ngữ ngành giữ viết tắt (VIN · SOH · ESP · MCU · ADAS · PM2.5 · SOC · HUD · DRL) khai vào `SAME_ON_PURPOSE` kèm lý do + bài canh chặn mục cho-phép không còn ai dùng. ⚠ `DEFAULT_PROFILE = "Mặc định"` **KHÔNG được dịch** — nó là tiền tố khoá lưu bền, dịch là **mất sạch cấu hình** người dùng; có bài canh.
  - **L2 phần icon**: `ic_launcher.xml` đã có từ chặng icon của G1 ⇒ còn lại của L2 là khoá ký + kênh cập nhật riêng = việc 🚗.
  - **Còn tồn**: **chưa đo gì trên xe** · ca 6 ô mỗi ô một app thật khi đổi ngôn ngữ (cần 6 màn ảo, chỉ đo được 1 ô) · tương tác `values-night/` với bảng màu launcher · bảng sáng ngoài trời phải xem bằng mắt trên xe.

- **2026-09-12 (đêm) · G1 NHÓM KHẢ NĂNG + NHỊP GIAO DIỆN + ICON SANG. DONE off-car.** Spec `docs/specs/kachi-capability-groups.html`. **[ĐO] 2960 test / 0 đỏ** (trước G1: 2797 ⇒ **+163**), APK sạch.
  - ⚠⚠ **THỨ OWNER YÊU CẦU SUÝT KHÔNG ĐƯỢC GIAO — DÙ MÃ ĐÃ ĐÚNG.** Sau 4 chặng, 12 nhóm chạy được, **2918 test xanh**, tôi đã coi mục "gom nhóm" là xong. Một lượt **kiểm toán giao diện độc lập** mở ảnh ra xem và kết luận: **ô nhóm Lốp render GIỐNG HỆT widget `w_tire` cũ** — không kết luận tổng, không màu trạng thái, đơn vị ở caption, vị trí chỉ là 2 chữ viết tắt cỡ ~60px (thứ TO NHẤT màn) trong khi **giá trị thật là gạch 70×8px**, số cách vành xe **513px**. Tức đúng ví dụ owner nêu (*"không ai xem áp suất lốp 1 lốp cả"*) vẫn **chưa cải thiện gì**, dù danh mục + bộ vẽ + test đều báo đạt. **Lần thứ BA của dự án "đúng theo mã" ≠ "đúng theo người dùng"** (trước: nút "Kính 50%" không làm 50%; 4 gói lệnh không ai chạm tới được). Khuôn ĐÚNG nằm ngay trong cùng phiên: `RadarBoardView` xếp 8 vùng quanh hình xe theo vị trí không gian ⇒ Lốp và ADAS nay dùng chung khuôn đó. **LUẬT: test xanh + mã đúng KHÔNG chứng minh đã giao được thứ owner hỏi — phải mở ảnh ra xem, và tốt nhất là để một luồng ĐỘC LẬP xem.**
  - ⚠⚠ **BỘ NIÊM PHONG T11 LÀ DẤU XANH GIẢ — đo được, không suy luận.** Sửa **một dấu cách** trong `strings.xml` ⇒ `Task :offcar-planner:test **UP-TO-DATE** · BUILD SUCCESSFUL · 0 đỏ`. Ép chạy lại thì **FAILED đúng chỗ** ⇒ phép so hash vẫn đúng, chỉ **dây nối chết**. Nghĩa là **mọi lần ghim lại layout/strings từ trước tới nay đều đi qua một niêm phong chưa hề mở ra**, trừ khi tình cờ có thứ khác làm task chạy lại. Phát hiện từ chặng icon (bài canh ở đúng module `:app` vẫn UP-TO-DATE khi đổi nội dung tệp vector, vì gradle chỉ biết đầu vào là classpath). **Bất đối xứng nguy hiểm: thêm tệp MỚI thì đỏ đúng ⇒ rất dễ tưởng đã an toàn.** Soát cả họ ⇒ **11 ca**, vá bằng `inputs.dir/files` ở **5 tệp gradle**, mỗi ca chứng minh hai chiều. Kèm `LayeringRulesTest` có **9 chỗ `?: return`** ⇒ **7 bài canh tự tắt và báo xanh** khi không giải ra cây nguồn. **LUẬT ĐẦY ĐỦ (mở rộng luật S1): đặt bài đúng module chỉ là MỘT NỬA — nửa còn lại là gradle phải BIẾT thứ bài đó quét; và bài canh không được có đường tự tắt.**
  - **Cấu trúc**: `:core CapabilityGroups` (12 nhóm · `reads`/`writes` là mã THẬT, `init{require}` đòi mọi thành viên tồn tại · tier = thành viên YẾU NHẤT · `CapabilityKind` = **READ** kể cả nhóm có nút, vì WRITE sẽ biến ô "Kính" thành một cái nút) → `:core GroupBoard` (thuần, quyết định trình bày) → `:app GroupTileViews`/`RadarBoardView`/`SideBoardView`. Nhóm **bị chặn khỏi thanh trạng thái** ở **5 lớp** (chip 24dp không vẽ được bảng 4 bánh) — [SOÁT P3-6] số này trước đây ghi "4 lớp" trong khi chỉ có **1 bài canh** đo cả bốn, nên không có gì đối chiếu con số; nay mỗi lớp một bài (`isChippable` · `choices` · `setEnabled` · `init require` · **`decode`** — lớp thứ năm trước đó KHÔNG ai canh, và nó là đường của dữ liệu sửa tay/bản cũ: không lọc thì `TopStripConfig` ném ngay lúc nạp ⇒ launcher sập khi mở). **Mục rời GIỮ NGUYÊN** (123+64+9+4) — xoá đi là làm mất khả năng; màn chọn chỉ **xếp Nhóm lên trước**.
  - **Nền đã có sẵn từ gói 2**: `WidgetShape` khai BOARD *"4 lốp, 8 zone radar"* + STRIP *"cửa/kính/đèn/dây an toàn"* nhưng **chỉ bảng lốp được dựng** ⇒ G1 là làm nốt thứ đã thiết kế mà chưa dựng, không phải phát minh khái niệm mới.
  - **Thang khoảng cách** `KachiSpace`: [ĐO] **369** chỗ dùng số tự chọn (18 giá trị khác nhau cho CÙNG vai trò "lề trong thẻ") ⇒ 337 chỗ chuyển, **167 chỗ NỚI RỘNG**. ⚠ lượt soát bắt được R4 **chưa thật đạt**: `SettingsRows.px()` là hàm đổi dp **THỨ HAI** với 4 số trần, lách hoàn toàn bài canh (nó chỉ soi tên `dp`/`dpi`) ⇒ xoá hàm + bài canh nay chặn **NGUYÊN NHÂN** (khai hàm đổi dp tên lạ là đỏ).
  - **Icon**: 9 độ dày nét → **1** (1.6) · 29/98 đường thiếu đầu nét tròn → **145/145 đủ** · khung 24×24 cho 79/79 · +8 icon mới · sửa **5 icon sai nghĩa** · bỏ **glyph dự phòng ⊞ đóng vai icon thật** (nó từng mang 3 nghĩa: cửa kính · Bảng tổng hợp · ESP) · hai nút **đối nghịch** "Mở/Đóng hết kính" từng dùng CÙNG icon. Thêm bài canh **NGHĨA** (`CapabilityIconMeaningTest`) vì bài cũ chỉ canh **phong cách** nên không bắt được họ lỗi này. Luật mới: nhóm nào ≥3 mục phải dùng chung icon ⇒ **bỏ icon**, thay bằng **sơ đồ xe có vị trí**.
  - **5 lỗi UX nặng khác kiểm toán tìm ra, đã vá**: nhãn mục đọc tương phản **2.33:1 → 6.09:1** + cao nét 10px → 15–16px (gốc là **làm mờ CẢ Ô** khi thiếu dữ liệu ⇒ nay chỉ mờ **giá trị**, nhãn luôn đọc được) · hàng đọc phình **1162×474px → 1160×94px** (mực 0.31% → 2.92%, chiếm 64% → 14.6% chiều cao ô) · nút "Đặt" **cuộn là mất** (điểm sáng 7242→83→**0**) ⇒ ghim đáy (nay 6216 qua 4 lần cuộn) · **trần 8 mục chặn IM LẶNG** (bấm thêm không một lời nào — đúng họ lỗi `DockConfig.setEnabled` cũ) ⇒ nói ra + làm mờ ô còn lại + bài canh chặn nhánh bỏ-qua-im-lặng mọc lại · badge "chưa kiểm trên xe" thả nổi cách icon 268px trông như bụi ⇒ về góc thẻ.
  - ⚠ **SỐ ĐO CỦA TÔI SAI BA LẦN TRONG CÙNG ĐÊM, cùng một kiểu** (đếm bằng một mẫu tìm rồi tin luôn con số): "115 lời gọi `dp(`" → thật ra **369** (mẫu tìm bỏ sót dạng `dpi(ctx, N)`, dạng phổ biến nhất) · "0 tệp khai `strokeLineCap`" → thật ra **41/68 tệp đã khai**, bệnh là khai **lẻ tẻ theo từng đường** nên phải đếm theo `<path>` · "23 tô đặc / 48 vẽ nét" → 26 và 48 (71 > 68 nên hai số đó vốn không cộng được).
  - ⚠ **Hai lời hứa đúng nhờ MAY MẮN DỮ LIỆU**: `rowsOf` không giữ giao kèo của chính nó (`rowsOf(7,3)`→`[3,3,1]` trong khi KDoc cấm "hàng cuối một ô") — 12 nhóm hiện tại tình cờ không rơi vào ca đó; và bài canh cho nó **không thể đỏ** (đổi `rowsOf` thành `emptyList()` làm mọi ô nhóm mất sạch ô con mà 0 bài đỏ).
  - ⚠ **Nhóm Đèn dựng 9 ô con mà không hiện một pixel**: `View` trơ khai `WRAP_CONTENT` **giãn hết** dưới spec `AT_MOST` (`getDefaultSize` trả trọn `specSize`) ⇒ ô chèn của hàng nút báo cao 648px, ăn hết `weight=1` của thân ô. Tôi **suy diễn sai 4 lượt** trước khi log chiều cao thật trong `post{}`, trong đó một lần "chữa" bằng `minimumHeight` — thứ **vô tác dụng** ở đây nên sẽ để lại chốt giả.
  - ⚠ **Giả thuyết của tôi về thanh trên bị số đo bác**: thanh cao thêm 42→56dp, tôi cho là do nới đích chạm 48dp. [ĐO] bỏ **cả hai** `minimumHeight = dp(TOUCH)` ⇒ thanh **vẫn 84px**; toàn bộ 14dp đến từ **avatar** (cỡ cứng 48dp). Nghĩa là nới đích chạm cho các pill tốn **0 pixel**. Giữ nguyên (mất 21px = 2.2% chiều cao vùng làm việc).
  - **Còn tồn**: **chưa đo gì trên xe** (bảng lốp mới chỉ chứng minh bằng dữ liệu **gieo tạm**, off-car mọi số lốp là `—`) · nhãn 13.5sp chứ không 16sp (16sp gần bằng cỡ số ⇒ đảo lại thứ bậc; đạt "16" theo **chiều cao nét**) · toast của ngăn kéo **không giao được** (hộp nằm sát mép dưới màn, 0 pixel chữ) trong khi toast ở màn Cài đặt hiện đủ ⇒ cần một lượt đo riêng · nhãn trong sơ đồ ADAS nhỏ (~14px nét) khi ô ở khung nhỏ · bảng màu SÁNG vẫn chưa có (OQ2 của S1).

- **2026-09-11 (tối) · S1 MÀN CÀI ĐẶT — gom mọi cấu hình về một chỗ. DONE off-car.** Spec `docs/specs/kachi-settings-screen.html`. **[ĐO] 2797 công bố / 2109 phân biệt · 0 đỏ** (trước S1: 2725 ⇒ **+72**), APK sạch.
  - **Việc đầu tiên là ĐẾM, không phải vẽ màn hình**: yêu cầu *"không cấu hình nào nằm lẻ tẻ"* chỉ kiểm được nếu biết đủ danh sách. ⚠⚠ **kiểm kê của tôi sai HAI lần, cùng một kiểu**: bỏ sót `slot_0..slot_5` (khoá **dựng động** `key("slot_$it")` trong vòng lặp) rồi bỏ sót `freeform_state` + 4 cờ `Settings.Global/Secure` (**tệp prefs thứ hai** + ngoài thư mục `launcher/`). Cả hai lần vì tôi tìm **hằng khai tường minh trong một tệp**. ⇒ phép kiểm cuối quét **3 gốc mã · 49 tệp · 4 nhánh ghi** (prefs · `Settings.*` · lệnh shell `settings put` · tệp prefs mới) + **7 mốc neo** mỗi mốc đại diện một nhánh.
  - ⚠⚠ **PHÉP KIỂM QUAN TRỌNG NHẤT CỦA S1 TỪNG KHÔNG CHẠY — họ lỗi MỚI của dự án**: hai bài chống-rữa đặt ở `:core` nhưng quét mã nguồn `:app`. [ĐO] đóng mốc `:core:test` xanh → thêm hằng khoá vào `app/.../WorkspacePrefs.kt` → `Task :core:test **UP-TO-DATE**, BUILD SUCCESSFUL` ⇒ **dấu xanh sai ở đúng ca nó sinh ra để bắt** (gradle không coi tệp `:app` là đầu vào của `:core:test`). Chuyển sang `:app` ⇒ FAILED đúng chỗ. **LUẬT: bài test quét mã nguồn của module X thì PHẢI nằm trong module X, không thì nó không bao giờ chạy lại.**
  - **Cấu trúc**: `:core SettingsCatalog` (7 nhóm · 17 mục · `groupOf/orphans/NOT_SETTINGS` + `TOP_STRIP_ALLOWED_KEYS` + `PREFS_FILES`, mọi mục loại trừ **phải** có lý do tại chỗ) → `SettingsPanel` (rail nhóm trái + nội dung cuộn riêng phải, dựng lười và **nhớ lại** nên không mất chỗ đang cuộn) + `SettingsSections`/`SettingsSectionsHome` + `SettingsRows`/`CapabilityGridSection` tách ra dùng chung. **Xoá `CustomizePanel` (418 dòng)**, pill "Tuỳ biến" + "Cài đặt" gộp còn MỘT.
  - ⚠⚠ **CHỖ LÀM HỎNG YÊU CẦU KHÔNG PHẢI CHỖ TÔI NHÌN, MÀ CHỖ TÔI KHÔNG NHÌN**: tôi kết luận thanh trên còn 2 bề mặt cấu hình được phép; **hai luồng soát độc lập cùng** tìm ra cái **thứ ba** — pill "Thanh" gọi `cycleDockEdge()` ⇒ ghi bền `dock_edge`. Nặng hơn: **bài canh tôi viết cho chính luật đó VẪN XANH** khi dựng lại pill (nó đếm pill *mở* cấu hình, không thấy pill *đổi* cấu hình tại chỗ). ⇒ bỏ pill (Cài đặt cho chọn **thẳng** từng viền, tốt hơn xoay vòng; `cycleDockEdge` thành mã chết ⇒ xoá) + đổi cách canh sang **khoá lưu bền** chứ không theo số pill (số pill lách được bằng icon/segmented/cử chỉ giữ). Cũng bỏ đường **tạo hồ sơ** ở avatar — `ProfileBar.cycle()` vẫn mở hộp thoại tạo khi chỉ có 1 hồ sơ = **mọi máy mới cài**, mà bài canh chỉ soi hàm dựng avatar nên không thấy.
  - **`launcher_autostart` nay có ô tick thật** (trước đó lưu bền mà không ai chạm tới được — cùng họ với mục (d) của RW0), đi đúng khuôn `topStrip`: field state → intent → repo thân mặc định → nạp trong `load()`.
  - ⚠ **CỐ Ý KHÔNG làm nút chủ đề ngày/đêm**: [ĐO] **78 mã màu hex viết cứng ở 17 tệp** + `KachiTheme` dùng `const val` (hằng biên dịch) + `state.themeMode` **không ai đọc để vẽ** + `isNight` 0 chỗ gọi ⇒ nút gạt sẽ **đổi giá trị mà màn hình không đổi gì** = nút chết (luật `product-team-workflow.md` cấm). Thay bằng **dòng thông tin nói thật** + **bài canh tự đảo chiều**: hôm nào có người đọc `themeMode` để vẽ thì bài đỏ và đòi làm nút. Bảng màu sáng là việc riêng cỡ U5 (OQ2).
  - **[ĐO] máy ảo (sao lưu có kiểm 1183 byte trước khi đo, phục hồi sau)**: thanh trên còn đúng `Ứng dụng` + 5 nút bố cục + `Cài đặt` + avatar (3 khối pill nửa phải, hết chữ "Thanh") · rail **7 nhóm** đổi nội dung thật (mật độ mực 12.39%/0.91%/0.68%) · chọn viền "Phải" ⇒ `dock_edge` **BOTTOM→RIGHT** (đúng giá trị chọn, không phải giá trị kế của vòng xoay) và 9 ô đi từ hàng dưới sang cột phải (**dải dưới 44.1%→6.8%, dải phải 20.2%→61.4%**) · ô tick autostart: `force-stop` (PID **9582→10420** = tiến trình chết thật) rồi mở lại **vẫn tắt** (độ sáng lòng ô 163.3→26.5→26.5) · **R1 giữ chỗ cuộn: 77 000 điểm khác 0.00%** so với lúc đã cuộn, khác 12.66% so với đầu trang · nền bảng **đục** (18,20,28), vùng sau bảng đổi 90.7% ⇒ không lọt xuyên.
  - **Thử phá 10 phép qua 3 chặng, đỏ đúng chỗ cả 10** (2 phép hoàn nguyên byte-identical có kiểm). Đáng nhớ nhất: phép dựng lại pill "Thanh" là phép **chứng minh bài canh cũ không đủ**, chứ không phải chứng minh mã đúng.
  - **Còn tồn**: bảng màu SÁNG chưa có (OQ2) · nhánh "thiếu quyền" chỉ khoá bằng test (ép thiếu quyền phải ghi `enabled_accessibility_services` — đúng chỗ dự án từng có lỗi P1 xoá quyền app khác ⇒ cố ý không làm) · `clusternav_state.xml` không tồn tại trên máy ảo nên tệp prefs thứ hai chỉ chứng minh bằng quét mã · **chưa đo gì trên xe** · U5 đa ngôn ngữ giờ đã có chỗ để đặt (nhóm Hiển thị).

- **2026-09-11 (chiều-4) · RW0 XONG CẢ 3 VÙNG.** [ĐO] **2725/0** (trước đó 2699 ⇒ **+26**), APK sạch.
  - ⚠⚠ **LỖI NẶNG NHẤT KHÔNG PHẢI LỖI MÃ MÀ LÀ "KHÔNG AI CHẠM TỚI ĐƯỢC"**: `WidgetViews` **vẽ** được ô hành động, `ActionMacros` có 4 gói lệnh, `CapabilityCatalog` tra ra được — nhưng ngăn kéo (`AppDrawer`) chỉ liệt kê mục ĐỌC ⇒ người dùng **không có đường nào** đặt hành động/gói lệnh vào ô giữa màn. Số đo *"3 gói lệnh ở ô giữa màn"* của phiên trước đạt được bằng **gieo cấu hình bằng tay** ⇒ nó KHÔNG chứng minh người dùng làm được. Cả gói W2 (48 test, spec riêng) **không giao được**. **LUẬT MỚI (đã đóng vào test `CapabilityReachabilityTest`)**: *"vẽ được" ≠ "đặt được"* — một khả năng chỉ tính là xong khi có đường **đi từ tay người dùng** tới nó; bài test đòi **mọi** khả năng phải nằm trong nguồn của màn chọn (`byDomain()` ∪ widget dựng tay), [ĐO] 200/200 mục.
  - **Thanh trên hết viết cứng**: `TopStripConfig` + `TopStripChips` (:core thuần) + `TopStripPicker` (:app, tách riêng vì `CustomizePanel` vượt trần 500 dòng) + lưu bền theo hồ sơ + intent ViewModel. **Mặc định ra ĐÚNG chuỗi bản cũ** (test khoá cả 3 chip + ca off-car "—"): nới một bề mặt owner đã duyệt KHÔNG phải giấy phép đổi mặc định. ⚠ **CỐ Ý chỉ nhận mục ĐỌC** — chip ~24dp, dưới xa 48dp cho đích chạm, chạm lệch mà bắn lệnh xe (mở khoá cửa) là không hoàn lại được; ghi rõ ở KDoc + test khoá **ý định** (nút và gói lệnh bị từ chối).
  - **Sắc thái thay cho mã màu ở `:core`**: `ChipTone` + `:app` dịch sang hex. [ĐO] bản nháp đầu của tôi viết `#37d67a` trong khi `KachiTheme.GREEN` là `#34d399` ⇒ nếu để `:core` giữ hex thì dự án có **hai bảng màu** và chúng đã lệch ngay từ dòng đầu.
  - **Nhãn ngắn cho datum (mục c)**: `TelemetrySpec.short` là **tham số mặc định** ⇒ không phải sửa 123 dòng; `shortLabel` tự lùi về nhãn đầy. Điền **22 datum** nhãn dài nhất. [ĐO] chip lốp: `Áp lốp trước-trái · 2.4 bar` → `Lốp TT · 2.4 bar`.
  - ⚠⚠ **BA LỖI TÔI TỰ GÂY, TÌM RA TRƯỚC KHI GIAO**: (1) dùng lại `CustomizePanel.tile()` cho ô chọn chip ⇒ **ba lỗi cùng lúc** — sự kiện bấm bên trong bắn vào **thanh nút** (chọn chip lại thêm nút vào thanh), `tiles[id]` bị **ghi đè** vì cùng mã có ở hai lưới, hai chỗ tô nền tranh nhau ⇒ phải dựng ô RIÊNG (bẫy "hai bản sao cùng khoá"); (2) điều kiện làm mới chỉ xét `carStatus` ⇒ đổi danh sách chip mà **màn hình không đổi gì** (off-car trạng thái xe gần như không đổi) — họ lỗi đã gặp ở nút bố cục sẵn P9; (3) chip datum lúc đầu dùng `display` (thiếu đơn vị) thay vì `displayWithUnit()`.
  - ⚠⚠ **PHÉP ĐO CỦA TÔI SAI HAI LẦN TRONG CÙNG LƯỢT — lần thứ 4-5 của dự án**: (a) `sips --cropOffset 0 0` **cắt từ GIỮA màn** (y=465) chứ không từ mép trên ⇒ tôi "đọc thanh trên" mà thật ra đang đọc bảng đơn vị; (b) `uiautomator dump` **trả cây cũ** — báo 150 node "chưa mở bảng" trong khi ảnh chụp cho thấy bảng Tuỳ biến **đang mở 88.6% màn**. Suýt kết luận "nút Tuỳ biến hỏng do tôi tách tệp". **LUẬT: ảnh chụp + prefs là nguồn đo tin được; cây giao diện thì phải đối chiếu.**
  - **Quét bảo mật trước commit nêu 1 điểm gia cố, áp luôn**: cổng "chỉ nhận mục ĐỌC" trước đó chỉ nằm ở `setEnabled` ⇒ lớp dựa vào *"mọi chỗ gọi tương lai đều nhớ đi qua nó"*. Thêm `init { require }` ngay trong lớp. ⚠ [ĐO] chốt đó **lộ ngay một bẫy thứ tự khởi tạo**: `DEFAULT` được dựng lúc nạp lớp, gọi `isChippable` đọc `BUILT_IN` khai SAU ⇒ null ⇒ **27 bài đỏ** `ExceptionInInitializerError`. Đổi thứ tự khai + ghi lý do tại chỗ.
  - **[ĐO] máy ảo (có phục hồi cấu hình gốc sau khi đo)**: ngăn kéo hiện đủ **4 gói lệnh** (`Mở hết kính`/`Đóng hết kính`/`Mở cửa + đèn đọc`/`Rời xe`) + nút, kèm gợi ý `· xem`/`· bấm` phân biệt nhãn trùng · đặt "Rời xe" vào ô ⇒ lưu `slot_0=widget:w_board,mac_leave` và **bấm chạy đủ 6 bước** (`mac_leave: không bước nào ăn`), app không sập · `top_strip=chip_energy,tyre_p_fl,chip_pm25` ⇒ thanh trên ra **đúng thứ tự đó**, chip nhiệt mất, `Lốp TT · —`, màu `#34D399` khớp theme · mục chọn chip hiện **4 ô** với bật/tắt khớp cấu hình từng ô.

- **2026-09-11 (chiều-3) · LƯỢT SOÁT ĐỘC LẬP TOÀN PHẦN (owner yêu cầu trước khi làm tiếp) + VÁ 5 NHÓM.** Phạm vi `6a18447^..HEAD` = **10 commit tính năng, 60 tệp, +6882/−259** (gói 2 → gói 3 → U1 → P8 → P9 ×3 → U4 ×2 → 2 lượt vá). 4 luồng soát độc lập ⇒ **60 phát hiện (1×P0 · 9×P1)**; tôi **tự kiểm lại 8 phát hiện nặng nhất** bằng cách đọc chính mã nguồn trước khi vá (bài học: soát viên cũng sai được — và trong lượt này chính tôi cũng có 1 dương-tính-giả khi tự kiểm: chuỗi `0.3` khớp `0.34f` là **tỉ lệ vẽ**, không phải ngưỡng lốp). **[ĐO] 2699/0.**
  - ⚠⚠ **P0 — GÓI "RỜI XE" KHÔNG KHOÁ XE.** `lock` ("Khoá xe") và `door` ("Mở cửa") khai **CÙNG** `bindingKey` `BYDAutoDoorlockDevice.setDoorLockState`, và cả hai rơi vào nhánh `else -> intArrayOf(primary)` ⇒ **cùng một byte cho hai nghĩa đối nghịch**. `mac_leave` kết bằng `MacroStep("lock",1)` = đúng byte của `MacroStep("door",1)` ⇒ bấm "Rời xe" thì kính đóng, đèn tắt, xe **KHÔNG khoá**, mà kết quả vẫn báo thành công (rc=0) và ô còn sáng như đã khoá. **Chứng minh được KHÔNG cần xe**: hai nhãn đối nghịch gửi byte y hệt ⇒ ít nhất một cái sai. Vá theo **giá trị tài liệu** (khoá=2/mở=1) + `rain_close` (ON=1/OFF=2; code cũ gửi **0** khi tắt = giá trị không có trong tài liệu). Kèm vòng 2 (quét bảo mật nêu): `door` từ TOGGLE *"Mở cửa"* (tắt = **khoá xe** mà nhãn không nói) thành **NÚT BẤM "Mở khoá cửa"** một chiều; `lock` đổi nhãn **"Khoá / mở khoá"** vì tắt nay mở khoá thật. **LUẬT MỚI (test khoá)**: *hai mã dùng chung một lệnh xe thì tham số PHẢI khác*, trừ danh sách cho phép có ghi lý do — luật này bắt thêm **3 cặp feature-id trùng** (`cam`/`camera_view` · `headl`/`headlight_mode` · `brightness_gear`/`hud_brightness`) ⇒ khai vào danh sách **nợ xe** kèm test chống mục-rữa, KHÔNG tự nghĩ giá trị (tự nghĩ giá trị chính là nguyên nhân P0 này). ⚠ đây là **lần thứ HAI** cùng họ lỗi "nhãn hứa việc A, gửi lệnh việc B" (lần 1: nút "Kính 50%"); test lần 1 chỉ chặn nhãn chứa `%` nên **không thể** bắt lần 2 — nay chặn **nguyên nhân** thay vì hiện tượng.
  - **4 lỗi P1 hành vi đã vá**: (1) **ô TRỘN** (mục đọc + nút/ảnh cùng ô) vẫn bị dựng lại mỗi nhịp trạng thái xe ⇒ mất cú bấm · chốt chống-bấm-kép **nằm trong View** nên dựng lại là mở cửa cho lượt thứ hai (hai lượt "đóng hết kính" chồng nhau) · ảnh trình chiếu đứng một tấm. Vá: `WidgetViews.refreshRead` **thay tại chỗ đúng ô con là mục ĐỌC** (mỗi ô con mang thẻ id), giữ nguyên view của nút và của widget tự-lo-nội-dung; chốt chuyển sang `ControlTileState.beginRun/endRun` theo **mã gói**. (2) **Tắt hình nền thì ảnh QUAY LẠI** — `reloadWallpaper` chỉ tăng thẻ QUÉT, lượt giải mã đang bay về sau vẫn `setPhoto` ⇒ vá **hai lớp** (tăng thẻ giải mã + kiểm lại công tắc lúc ảnh về). (3) **Cấp quyền trợ năng có thể XOÁ của app khác**: kênh shell lỗi trả **chuỗi rỗng** (không ném) mà bản cũ coi rỗng = "danh sách rỗng" ⇒ ghi danh sách chỉ-có-Kachi. Nay phân biệt *rỗng thật* (`"null"`) với *không đọc được* (rỗng/lạ) — không đọc được thì **tuyệt đối không ghi**. (4) Nhịp/lệnh nền **treo sau khi màn huỷ** ⇒ gom về `submitBg`/`cancelPending` + cờ `destroyed`.
  - **ĐÓNG BẪY HAI BẢN SAO (kiến trúc)**: `customLayout` (2 bản) · `unitPrefs` (**4 bản**) · `wallpaper` nay là field của **`HomeUiState`**; 3 đường ghi bền đi qua intent ViewModel; tầng UI **0** lần ghi bền trực tiếp; `load()` nạp cả 3 ⇒ ca **đổi hồ sơ** tự đúng (trước phải nhớ nạp lại bằng tay, và đã từng quên). Lý do tránh né cũ ("thêm field vào state là xáo trộn bộ quyết-định-dựng-lại") **không đúng**: bộ đó nhận `WorkspaceState`, không nhận `HomeUiState`.
  - ⚠⚠ **BỘ TEST TỪNG KHÔNG ĐÁNG TIN NHƯ CON SỐ NÓI**: [ĐO] **44** phép cắt vùng trong test canh dùng `substringAfter/Before`, trong đó ≥5 bài có **mốc kết không nằm sau mốc đầu** ⇒ Kotlin trả nguyên phần còn lại ⇒ bài đó quét tới **hết tệp** ⇒ assert `contains` gần như **không thể đỏ**. Nay tất cả đi qua `SourceRoots.body()` — **tự nổ** nếu mốc không tồn tại. Khi chuyển, **14 bài đỏ ngay**, chứng minh chúng từng xanh nhờ quét tràn. ⚠ chính helper của tôi cũng sai 3 lần trước khi đúng (tham số mặc định `= false` bị đọc thành thân-biểu-thức · `!=` bị đọc thành gán · mốc tự kết bằng `{`) — ghi lại vì đây là loại lỗi rất dễ lặp.
  - **Thêm test cho 3 chỗ TRƯỚC ĐÂY 0 TEST**: phép kẹp trình vẽ bố cục (tách sang `:core GridEditorLogic` — chỗ này từng có lỗi **SẬP** `coerceIn` trần<sàn mà không bài nào canh; nay quét >500 tổ hợp khung hỏng, không được ném) · **trần ô ở giá trị MẶC ĐỊNH** (mọi bài cũ truyền `cap` tường minh nên đổi mặc định thành 99 không ai đỏ) · hành vi cấp quyền trợ năng (8 ca).
  - **HAI CON SỐ TÀI LIỆU BỊ PHỒNG — đã đính chính**: (a) **số test**: `:app` chạy 2 biến thể trên **cùng 84 lớp** ⇒ 651 test đếm hai lần ⇒ từ nay ghi *"công bố 2699 · **phân biệt 2048**"*; (b) **nhịp trạng thái xe = 1 giây**, không phải "1 nhịp/giây" như tài liệu và KDoc vẫn ghi (`CarStatusRepository.fastMs = 1_000`) — đã sửa cả code lẫn tài liệu.
  - **Kiến trúc lõi GIỮ NGUYÊN** (đã kiểm): `:core` thuần (1 kết quả `import android` duy nhất, nằm trong chú thích) · **0** lần chạm display ≥ 1 · **0** lần ghi `wm size/density` · **0** `Dadb.create`/`Runtime.exec` ngoài `ShellTransport`.
  - **VÒNG DỌN NỢ cùng ngày (owner: *"đảm bảo clean hết rồi mới làm tiếp"*) — vá thêm 9 mục P2/P3**: widget trình chiếu sang **thread nền riêng** (`DECODER` dùng chung mọi ô) + không giải mã ở cỡ 1×1 · tách `ioExec`/`submitIo` để **I/O ảnh không dùng chung thread với lệnh cửa sổ dadb** (chặn ~3 s) · đọc nhạc **một lần mỗi lượt** thay vì mỗi ô mỗi nhịp · thanh trên **đổi chữ chip tại chỗ** thay vì dựng lại 3 chip mỗi nhịp · **Back đóng bảng vẽ** · trình vẽ theo **một ngón** (`activePointer`) · **3 icon MỚI** (`ic_mirror`/`ic_drive`/`ic_adas`) cho 13 nút từng lùi về icon nhóm (2 nút gương mang icon KÍNH) · nhãn ô hành động có "…" · `notice(coreOnly = true)` ⇒ hết mã chết, câu chữ về `:core`. ⚠ [ĐO] thử cho HOME gọi `notice()` không tham số thì test bắt ngay: nó nói RỘNG hơn `missingCore` ⇒ launcher ồn hơn thiết kế ⇒ thêm tham số, KHÔNG đổi ngữ nghĩa. **[ĐO] `--rerun-tasks` 2699/0 · APK debug sạch.**
  - **TÁCH TỆP về đúng trần 500 dòng**: `KachiHomeActivity` 678 → **498** dòng nhờ 3 đường cắt — `WallpaperController` (hình nền) · `HomePanels` (2 bảng phủ) · `PermissionPreflight.runAndReport` (vòng kiểm quyền về đúng lớp). Cả 3 nhận cổng vào bằng lambda như `LauncherWindows`/`DrawerController`. ⚠ [ĐO] 15 bài canh đỏ khi tách nhưng **nổ đúng chỗ** (mốc không tồn tại) thay vì âm thầm xanh — helper mới làm đúng việc. Tệp > 500 dòng còn lại đều là ClusterNav kế thừa (MainActivity 1386 đang niêm phong).
  - ⚠⚠ **TAI NẠN CỦA CHÍNH TÔI**: một phép thay bằng **regex `DOTALL`** đã **ăn mất 6 test** trong một tệp mà bộ test **vẫn xanh** (phần còn lại vẫn biên dịch). Tìm ra nhờ **đối chiếu số `@Test` với `HEAD`**; phục hồi từ git rồi sửa thủ công. **LUẬT: không dùng regex nhiều dòng để sửa test; sau mỗi lượt sửa hàng loạt phải đếm `@Test` so với `HEAD`.**
  - **CHƯA VÁ (không che)**: widget trình chiếu **vẫn giải mã ảnh trên thread chính** + lượt đầu giải mã ở cỡ 1×1 · `winExec` gộp lệnh-cửa-sổ với I/O ảnh · `MediaBridge.read()` gọi mỗi ô mỗi nhịp · Back không đóng bảng vẽ · nhiều ngón tay · 13/64 nút thiếu icon riêng (2 nút gương dùng icon KÍNH) · `LauncherRequirements.notice()/needsUser/fixedAtBoot` là mã chết.


- **2026-09-11 (đêm) · LƯỢT SOÁT ĐỘC LẬP ĐẦU TIÊN cho bố cục động + hình nền — 4 lỗi P1 đã vá.** **[ĐO] 2639/0** (trước lượt soát 2628 ⇒ **+11**). Phạm vi 9 tệp / 1233 dòng. **0 P0 · 4 P1 · 4 P2 · 4 P3**; soát viên CHỈ báo cáo, tôi tự vá + tự kiểm từng cái.
  - ⚠⚠ **MỘT TUYÊN BỐ CỦA TÔI BỊ BÁC — nhớ bài này**: tôi ghi ở spec hình nền rằng giả thuyết "ô bị dựng lại liên tục" đã **bị số đo bác**. Sai: phép đo đó chạy **off-car**, nơi `CarStatus` toàn null nên `statusChanged` **luôn false** ⇒ ca đó **không thể xảy ra khi đo**. Giả thuyết ĐÚNG: `CapabilityCatalog.kindOf` trả READ cho **mọi** widget dựng tay (xét `WidgetRegistry` trước) kể cả `w_photos` ⇒ trên xe (1 nhịp/giây) ô bị dựng lại **mỗi giây** ⇒ trạng thái quay vòng bị đặt lại (ảnh **đứng mãi ở một tấm**) + mỗi giây một lượt đọc tệp & giải mã ảnh **trên thread chính**. Vá: `hasReadContent` loại widget **tự lo nội dung** (`SELF_DRIVEN = {w_photos}`) + 3 test (có test chặn vá quá tay: widget đọc dữ liệu xe **vẫn phải** dựng lại). **LUẬT: "nghi phép đo trước" là đúng, nhưng kết luận đúng phải là *off-car không kiểm được ca này*, KHÔNG phải *code không sao*.**
  - **[P1] Hình nền lần mở đầu giải mã ở cỡ 1×1**: `reloadWallpaper` chạy trong `onResume`, mà **lượt đo cây view chạy SAU onResume** ⇒ `wall.width == 0` ⇒ cỡ cần = 1 ⇒ ảnh giảm tối đa rồi kéo lên phủ màn = **vệt màu loang**, không có gì nạp lại (chu kỳ tới 30 phút). Vá: cỡ cần lấy theo **màn hình** (`displayMetrics`). **[ĐO] chứng minh hai chiều**: có vá ⇒ biên độ **116**, **27** lần đổi sáng/tối, bước lưới **64px** = đúng 1920/900×30; hoàn nguyên ⇒ biên độ **0** (ảnh biến mất). ⚠ phép đo cũ không bắt được vì tôi dùng **ảnh đơn sắc** — ảnh 1 điểm kéo lên trông y hệt.
  - ⚠ **[P1] LỖI DO CHÍNH BƯỚC 3 GÂY RA**: `LauncherWindows.reflow` còn `for (i in 0..3)` **viết cứng** sau khi nới trần lên 6 ⇒ app ở khung 5/6 không được đặt lại khung ⇒ nằm lệch khỏi ô / cứ toàn màn che launcher = **hình dạng P-bug2**. `GridSeamGuardTest` của tôi **chặn tên hàm nhưng không chặn số cứng** nên lọt. Nay chặn cả dạng `0..N`.
  - **[P1] Đổi hồ sơ tài xế không nạp lại bố cục**: bố cục lưu theo hồ sơ (đúng) nhưng chỉ **đọc một lần lúc khởi động** ⇒ sang hồ sơ B vẫn thấy bố cục A; nặng hơn: mở bảng vẽ ở B nạp bố cục A, bấm Lưu = **GHI ĐÈ MẤT** bố cục của B. Vá qua `applyCustomLayout` + test canh chỗ **đọc**.
  - **[P2] Bố cục hỏng vào tới trình vẽ ⇒ kéo một khung là SẬP** (`coerceIn` trần < sàn khi khung ngoài lưới; dữ liệu sửa tay — mà spec bán điểm "chuỗi tự đọc được để cứu bằng tay"). Vá **hai lớp**: lọc ở cửa vào (`WorkspacePrefs.gridLayout`) + lưới an toàn trong phép kẹp. **[P2] nhịp widget vẫn chạy khi HOME bị app khác che** ⇒ vá bằng `onWindowVisibilityChanged`.
  - **PASS 2 (cùng ngày) — vá tiếp 3 mục còn tồn. [ĐO] 2652/0** (+13):
    - **Đọc/giải mã ảnh của HÌNH NỀN sang THREAD NỀN**: cả quét thư mục lẫn giải mã chạy ở `winExec` (thread nền có sẵn), chỉ đưa ảnh vào View trên thread chính. Kèm **thẻ thế hệ** `wallGen` (lượt cũ về muộn ⇒ bỏ + **nhả ảnh**, không ghi đè lượt mới) và **chốt chống nạp trùng** `wallLoading`. **[ĐO] máy ảo**: nền vẫn nét (biên độ 120, 24 vạch) + vẫn đổi ảnh (xám lệch đỏ-xanh 0 → cam +47), 0 ngoại lệ. ⚠ 2 test dây nối cũ **kiểm VỊ TRÍ dòng** nên vỡ khi đổi cấu trúc dù luật không đổi ⇒ viết lại cho **kiểm LUẬT** (nhánh tắt không được `return`; tạo thư mục chạy vô điều kiện).
    - **Cổng giảm cỡ**: thêm `scaledWidth()` hạ **đúng khung ngay trong lúc giải mã** (`inDensity`/`inTargetDensity`, không tạo ảnh trung gian vì thế cần 2 ảnh cùng sống). Lấy tỉ lệ **lớn hơn** trong 2 chiều ⇒ vẫn **trùm đủ** khung. [ĐO] ca 12000×9000: **27 MB → ~11 MB**; có test khoá bất biến "hạ xong vẫn trùm đủ" trên 5 tỉ lệ.
    - **"Hiện ngay ảnh đầu tiên"**: ca `lastChangeMs<=0` nay **giữ nguyên** chỉ số, chỉ đóng mốc. ⚠ **2 test cũ đang khoá hành vi SAI** ⇒ sửa cho khớp ý định + thêm ca thật (đang xem ảnh thứ 3, mở lại phải hiện lại ảnh thứ 3).
  - ⚠⚠ **BẢN VÁ ĐẦU CỦA PASS 2 TỰ GÂY HỒI QUY** (quét bảo mật tìm ra): dùng **MỘT thẻ thế hệ chung** cho cả quét thư mục lẫn giải mã ⇒ nhịp trình chiếu tăng thẻ trước lúc lượt quét về ⇒ lượt quét **bị bỏ oan** ⇒ ảnh mới thêm / ảnh vừa xoá / chu kỳ vừa đổi **không được nhận**, im lặng dùng dữ liệu cũ. Đã tách `wallScanGen`/`wallDecodeGen` + test khoá. Kèm dọn trường `photoPaths` đã thành **mã chết** sau khi vá.
  - **CHƯA VÁ (ghi rõ, không che)**: **widget trình chiếu VẪN giải mã ảnh trên thread chính** (P2 của soát viên chỉ nói hình nền; nói cho đủ thì widget cũng nên chuyển) · [P3] Back không đóng bảng vẽ · [P3] chưa xử nhiều ngón tay · [P3] vài điểm vụn (màu phân tích trong lúc vẽ, tay cầm đổi cỡ chiếm gần nửa khung nhỏ nhất).
  - **Phát hiện ngoài danh sách lỗi**: [ĐO] **ô trống che kín nền** — thẻ workspace và ô trống cho **0%** nền lọt qua ⇒ giữa màn gần như không thấy hình nền, chỉ thấy ở lề + mép dưới (thanh trên 17%). Là **quyết định owner** ⇒ OQ5, không tự đổi.

- **2026-09-11 (tối) · P9 BƯỚC 3 — NỚI TRẦN Ô 4 → 6, DONE off-car. P9 XONG CẢ 3 BƯỚC.** Spec `docs/specs/kachi-dynamic-grid.html` §Bước 3. **[ĐO] 2628/0** (trước bước này 2621 ⇒ **+7**).
  - **CHỨNG MINH RỦI RO TRƯỚC KHI SỬA**: bộ quyết-định-dựng-lại so số view với số ô của bố cục **SẴN** ⇒ bố cục tự vẽ 6 khung (bố cục sẵn 3 ô) làm nó trả **"dựng lại tất cả" MỌI lần render** ⇒ trên xe (trạng thái đổi **1 nhịp/giây** (đo lại 2026-09-11; tài liệu cũ ghi 2)) app đang chiếu **bị nhả/gắn 2 lần mỗi giây** + mất cú bấm = loại lỗi **P-bug1/R3**. Viết test chứng minh hành vi cũ ra đúng RebuildAll, rồi thêm tham số **`slotCount` = số ô THỰC TẾ**, **mặc định = `new.preset.slotCount`** ⇒ test tương-đương hơn 1000 tổ hợp **giữ nguyên** (có test riêng khoá chính điều đó).
  - **Trần = 6, không hơn**: bị chặn bởi **cỡ ô dùng được** chứ không bởi lưới. 1920×1080: 6 ô ⇒ ~640×360 **còn đặt được app**; 8 ô ⇒ ~480×270 app vô dụng. (Lưới 12×6 khung nhỏ nhất 2×1 về lý thuyết cho 36 khung — vô nghĩa.)
  - ⚠⚠ **[ĐO] MỘT LỖI SẬP MÀ TEST KHÔNG BẮT ĐƯỢC**: `PrefsWorkspaceRepository.DEFAULT_WORKSPACE` truyền **danh sách CỨNG 4 phần tử** ⇒ `require(slots.size == SLOT_CAP)` ném lỗi **ngay lúc nạp lớp** ⇒ **launcher SẬP ở lần chạy đầu** (khi chưa có cấu hình). Không test nào bắt vì lớp đó cần Context. **Tìm ra vì ĐỌC MÃ SẢN PHẨM TRƯỚC KHI SỬA TEST** — nếu chỉ sửa 2 test fixture đang đỏ thì đã đẩy đi một lỗi sập. Sửa **tận gốc**: thêm `WorkspaceState.of(preset, vararg)` (đệm tới trần, báo lỗi rõ nếu quá) + **đưa `WorkspaceState.DEFAULT` về `:core`** để kiểm được off-car.
  - ⚠ **[ĐO] lỗi thứ hai**: bấm nút **bố cục sẵn** khi đang dùng bố cục tự vẽ ⇒ **dựng lại TOÀN BỘ ô** (vì preset đổi) nhưng **màn hình không đổi gì** (bố cục tự vẽ vẫn thắng) ⇒ người dùng tưởng nút hỏng + app trong ô bị nhả/gắn vô ích. Sửa: **chọn bố cục sẵn = BỎ bố cục tự vẽ** (hành động tường minh phải có tác dụng; cũng là đường về bố cục sẵn không cần mở bảng vẽ).
  - ⚠⚠ **TÔI TỰ SẬP BẪY HAI-BẢN-SAO ngay tại đó**: lần sửa đầu tôi xoá bố cục ở **biến của màn chính** nhưng `WorkspaceView` giữ **bản sao riêng** ⇒ [ĐO] cấu hình đã xoá mà màn hình **vẫn hiện 6 khung**. ⇒ ép mọi thay đổi qua **`applyCustomLayout` duy nhất** + test canh **đếm số chỗ ghi** (đúng 2: đường duy nhất + lúc nạp khởi động).
  - **[ĐO] máy ảo**: bố cục 6 khung `0,0,4,3;...;8,3,4,3` ⇒ **6/6 khung ĐÚNG TỪNG PIXEL** · **0 lần** "dựng lại tất cả" trong 30 giây, số view = số ô = 6 mọi lần render · bấm bố cục sẵn 2×2 ⇒ bỏ bố cục tự vẽ, **4 ô** hiện đúng vị trí · dữ liệu cũ **tự tương thích** (đọc `slot_0..slot_5`, khoá thiếu ⇒ ô trống). **Thử phá 3 phép, đỏ đúng chỗ cả 3** (quay lại đọc số ô từ bố cục sẵn = 4 đỏ · bỏ đệm ô trống = 24 đỏ · hạ trần về 4 = 1 đỏ).
  - **Còn tồn**: ca **1 nhịp/giây chỉ khoá bằng test, CHƯA đo trên xe** (off-car trạng thái gần như không đổi — đo được 3 lần render/30 giây) · **chưa đo 6 ô mà mỗi ô chứa APP thật** (mỗi ô chứa app cần một màn ảo riêng ⇒ 6 màn ảo) · chưa đổi được **thứ tự** khung · **chưa soát độc lập** phần code.

- **2026-09-11 (chiều-2) · P9 BƯỚC 2 — TRÌNH VẼ KHUNG, DONE off-car.** Spec `docs/specs/kachi-dynamic-grid.html` §Bước 2. **[ĐO] 2621/0** (trước bước này 2602 ⇒ **+19**), **0 tệp XML** bị đụng, `:core` vẫn 0 import `android.*`.
  - ⚠⚠ **VIỆC ĐẦU TIÊN KHÔNG PHẢI VẼ, MÀ LÀ GOM NGUỒN**: [ĐO] **6 chỗ** tự suy ra số ô / khung pixel từ bố cục sẵn — 3 ở màn chính (`rebuild` · `onMeasure` · `onLayout`) và **3 ở `LauncherWindows`** (bộ sắp cửa sổ app). Bỏ sót một chỗ ⇒ **màn hình vẽ bố cục mới nhưng cửa sổ app đặt theo bố cục cũ** = app nằm lệch khỏi ô, đúng hình dạng P-bug2. ⇒ `EffectiveLayout` (:core, thêm vào `WorkspaceGrid.kt`) = **nguồn duy nhất** cho "bao nhiêu ô / ô ở đâu", + **`GridSeamGuardTest` quét mã nguồn** chặn `WorkspaceLayout.slots(` và `preset.slotCount` xuất hiện ở phía app.
  - **LUẬT LÙI AN TOÀN** — bố cục tự vẽ chỉ thắng khi **dùng được thật** (hợp lệ VÀ ≤ trần ô); 3 ca lùi về bố cục sẵn: dữ liệu hỏng/rỗng · đè nhau (lưới an toàn cho dữ liệu cũ) · **nhiều khung hơn trần ô** (ca CÓ THẬT khi bản sau nới trần rồi hạ cấp bản ⇒ không làm mất ô). `ignoredReason()` nói lý do, không im lặng.
  - **PHẠM VI CỐ Ý: giữ trần 4 khung** (`WorkspaceState.SLOT_CAP` (lúc đó 4 · **nay 6** sau bước 3)) ⇒ bố cục tự vẽ **dùng được THẬT ngay** với toàn bộ đường chiếu app / sắp cửa sổ / kéo-thả đang chạy, mà **KHÔNG chạm** phần rủi ro nhất. Nới trần = bước 3. Trình vẽ **nói thẳng** trần thay vì im lặng chặn nút.
  - `GridEditorView` (:app Canvas): kéo giữa = di, kéo góc dưới-phải = đổi cỡ, **luôn bám ô lưới**; **cho phép đè nhau rồi tô ĐỎ** (chặn giữa lúc kéo làm khung "dính" khó hiểu) + Lưu bị chặn kèm **tên đích danh cặp khung**. `LayoutEditorPanel` dựng bằng code ⇒ 0 XML. Vẽ theo **đúng tỉ lệ màn hình**.
  - **`setCustomLayout`: số ô đổi ⇒ dựng lại; số ô giữ nguyên mà chỉ đổi hình dạng ⇒ CHỈ đặt lại chỗ** ⇒ app đang chiếu trong ô **không bị nhả/gắn lại** (C5). Có test khoá.
  - **[ĐO] máy ảo**: bố cục `0,0,5,6;5,0,7,2;5,2,7,4` (không giống bố cục sẵn nào) ⇒ **3/3 khung ĐÚNG TỪNG PIXEL** cả ngang lẫn dọc, cột trái **773px** vs **1132px** của bố cục sẵn · kéo đổi cỡ ⇒ "còn **4 ô trống**" (đúng 2×2) · kéo đè nhau ⇒ "**khung 1 và khung 2 đè lên nhau**" + Lưu mờ · "Về bố cục sẵn" ⇒ về 1132px + xoá khoá lưu. **Thử phá 4 phép, đỏ đúng chỗ cả 4.**
  - **[ĐO] 2 LỖI NHÌN-THẤY-ĐƯỢC chỉ lộ khi ĐỌC ẢNH** (test xanh không bắt): (a) **nền bảng trong mờ** ⇒ giao diện launcher lọt xuyên qua — tiêu đề đè chữ ngày thanh trên, nút Đóng đè "Cài đặt", thanh nút xe nằm ngay dưới hàng nút ⇒ nền **đục** (sau sửa: 0/40 điểm sáng ở dải thanh nút); (b) **khung vẽ giật cỡ** khi dòng lỗi hiện/mất (ẩn hẳn ⇒ khung giãn rồi co, giật ngay giữa lúc kéo) ⇒ **giữ chỗ** dòng lỗi (sau sửa: nút giữ nguyên y=1003).
  - ⚠⚠ **[ĐO] BA LẦN PHÉP ĐO CỦA TÔI SAI TRƯỚC KHI CODE SAI — trong CÙNG ngày**: (1) hai khung chụp giống nhau từng byte ⇒ tưởng widget không đổi ảnh, thật ra **màn máy ảo ngủ**; (2) so khung ra "0/3 khớp" ⇒ tôi lấy bề rộng **cả màn 1920** trong khi vùng làm việc rộng **1878** lệch 21px, tính lại thì **3/3 đúng từng pixel**; (3) kiểm "đã về bố cục sẵn" ra "CHƯA" ⇒ bộ lọc bắt **khung chứa** thay vì ô. **LUẬT: số đo nói code sai thì KIỂM PHÉP ĐO TRƯỚC.** Cả ba lần code đều đúng.
  - ⚠ **Tôi tự gây một lỗi khi đo**: đọc tệp cấu hình rồi đẩy ngược lên **mà không kiểm lệnh đọc có thành công** (2 máy ảo cùng cắm ⇒ adb không biết chọn máy) ⇒ tệp rỗng ⇒ **xoá luôn cấu hình** trên máy ảo. Nay có **chốt kiểm trước khi đẩy**. ⚠ lưu ý: có thể có **thiết bị thứ hai trên mạng** cắm cùng lúc (adb sẽ không biết chọn máy nào) — **luôn dùng `-s emulator-5554`**.
  - **Còn tồn**: ô lưới **không vuông** (cao hơn rộng ~12% — hệ quả 12/6=2.0 vs 16:9=1.78, KHÔNG phải lỗi vẽ; đổi sang 16×9 sẽ **mất** tính chất 4 bố cục sẵn khớp từng pixel ⇒ giữ 12×6) · **tối đa 4 khung** · chưa đổi được **thứ tự** khung (thứ tự = thứ tự ô, quyết định app nào vào khung nào) · **chưa soát độc lập** phần code.

- **2026-09-11 (trưa-2) · U4 HÌNH NỀN TỪ ẢNH + TRÌNH CHIẾU — DONE off-car (phần nền).** Spec `docs/specs/kachi-wallpaper.html`. **[ĐO] 2580/0** (đầu phiên 2563 ⇒ **+17**).
  - ⚠ **RÀNG BUỘC XE ĐỊNH HÌNH THIẾT KẾ**: hai đường thông thường đều vướng — màn chọn tệp hệ thống **bị khoá trên xe**, và quét bộ nhớ chung **cần quyền cấp lúc chạy**. ⇒ đọc từ **thư mục riêng của app ở bộ nhớ ngoài** (`Android/data/<gói>/files/wallpapers`): **không cần quyền nào**, và app **đã dùng** lối này để xuất log.
  - `Slideshow` + `WallpaperPrefs` (:core thuần) · `WallpaperStore` (đọc thư mục + nạp **giảm cỡ** — ảnh 12MP nguyên cỡ ~48 MB, đủ làm launcher chết) · `WallView` vẽ ảnh, **nền vẽ sẵn là đường LÙI** (chưa bật / không ảnh / ảnh hỏng ⇒ byte-giữ như trước).
  - **Mặc định TẮT** ⇒ ai không dùng **không thấy gì khác**. Làm tối mặc định **45%** — **cần thiết chứ không trang trí**: chữ/ô launcher màu sáng, ảnh sáng làm chữ không đọc được.
  - **Bẫy kinh điển đã khoá test**: chưa tới hạn thì phải giữ NGUYÊN **cả** chỉ số **và** mốc thời gian — cập nhật mốc mà không đổi ảnh sẽ đẩy hạn lùi mãi ⇒ ảnh **không bao giờ đổi**, và lỗi đó **im lặng**.
  - **Dùng LẠI nhịp 10 giây** của thanh trên thay vì dựng vòng đếm riêng (thêm vòng là thêm thứ phải nhớ dừng lúc huỷ màn); sai số tối đa 10s so với chu kỳ ngắn nhất 15s.
  - **[ĐO] máy ảo 2 lượt**: (1) 3 ảnh đơn sắc ⇒ nền đổi **navy → lục** sau 22s, chữ vẫn đọc được; (2) tác nhân đọc ảnh **tự nêu giới hạn** "ảnh đơn sắc không kiểm được méo" ⇒ đo lượt 2 bằng **ảnh vuông 600×600 có lưới + vòng tròn**: bước lưới dọc **192.00** vs ngang **192.27** px (ô còn VUÔNG) · vòng tròn bán kính 800.3 px lệch chuẩn **0.53** px (còn TRÒN, không elip) · cắt trên/dưới **258/264** px (CÂN) · **0 pixel viền đen** · không lặp gạch ⇒ chế độ phủ kín **CẮT** đúng chứ không kéo méo.
  - **[ĐO] lỗi dùng-được tìm ra khi ĐO (test không bắt)**: thư mục ảnh **chỉ tạo khi đã bật**, mà muốn bật phải bỏ ảnh vào trước ⇒ **vòng lặp chết** ngay lần dùng đầu. Sửa: luôn tạo sẵn + bảng Tuỳ biến **nói rõ đường dẫn**.
  - **PHẦN (b) WIDGET TRÌNH CHIẾU XONG** (cùng phiên): `PhotoWidgetView` (:app) — widget thứ 9 dựng tay (`w_photos`, kind LOCAL nên off-car chạy đủ; chốt `WidgetRegistryTest` 8→9 kèm lý do). **Chạy ĐỘC LẬP với hình nền**: nguồn ảnh nạp **TRƯỚC** cổng bật/tắt nền ⇒ bật widget không cần bật nền (có test khoá thứ tự). **Nhịp TỰ DỌN** lúc ô bị tháo (nhịp sống lâu hơn ô = rò rỉ + nạp ảnh mãi) — nhịp = 1/3 chu kỳ, tối thiểu 5s. `setPhotoSource` dựng lại **chỉ ô widget** (không dựng ô đang chiếu app — C5). Widget **KHÔNG** làm tối ảnh (làm tối là để chữ trên nền đọc được; đây là khung ảnh). **[ĐO] máy ảo**: ảnh trong ô đổi **tím → cam → tím** qua 3 khung **trong khi nền TẮT**; phủ kín ô, 0 điểm tối ở mép. **[ĐO] 2602/0** (+22 so với 2580).
  - ⚠⚠ **[ĐO] MỘT GIẢ THUYẾT CỦA TÔI BỊ SỐ ĐO BÁC — nhớ bài này**: hai khung chụp **giống nhau từng byte** ⇒ tôi kết luận widget không đổi ảnh và **đoán** nguyên nhân là ô bị dựng lại liên tục theo nhịp trạng thái xe làm mất trạng thái quay vòng (đúng loại lỗi gói 2) — nếu tin thì đã **dựng lại kiến trúc**. Đếm thật: ô **chỉ gắn 1 lần**, nhịp chạy đều 6 lần, chỉ số **có** đổi 1→0 ở giây 20 ⇒ **lỗi ở PHÉP ĐO** (màn máy ảo ngủ nên lệnh chụp trả khung cũ), suýt sửa thứ không hỏng. **Luật rút ra**: hai lần đo giống nhau y hệt thì nghi **phép đo hỏng** TRƯỚC khi nghi code hỏng.
  - **Còn tồn**: widget + nền dùng **CHUNG chu kỳ** (tách được — OQ2) và có **chỉ số riêng** nên hiện 2 ảnh khác nhau cùng lúc (OQ4, tôi coi là đúng) · **thanh trên nền lọt 18%** (thẻ workspace 0%) ⇒ ảnh nhiều hoa văn sẽ rối ở đó, cách chữa là tăng độ đục riêng thanh trên nhưng cần owner xem ảnh thật · chưa có lượt soát độc lập phần code.

- **2026-09-11 (chiều) · P9 BƯỚC NỀN — model bố cục động 12×6, DONE off-car.** Spec `docs/specs/kachi-dynamic-grid.html`. **[ĐO] 2563/0** (đầu phiên 2547 ⇒ **+16**); chỉ **2 file MỚI** ở `:core`, **0 file cũ bị sửa**.
  - **Cố ý chia 3 bước**: bước 1 = model thuần (làm rồi) · bước 2 = trình vẽ khung · bước 3 = nối tầng vẽ + hồ sơ + nới trần ô. Lý do: tầng vẽ là chỗ **chiếu app vào ô** đang sống và vừa sửa hai lỗi khó (P-bug1, P-bug2) ⇒ đổi nền trước theo kiểu **cộng thêm, không sửa gì cũ**.
  - ⚠⚠ **[ĐO] SỰ THẬT ĐỔI PHẠM VI**: bố cục **"3 ô" KHÔNG biểu diễn được** trên lưới 12 cột — cột trái của nó `1.55/2.55 = 0.6078` bề ngang = **7.294 cột**, không phải số nguyên (ép 7 lệch **2.45%**, ép 8 lệch **5.88%**). 4/5 bố cục còn lại thì ĐÚNG (phủ 72/72 ô). ⇒ **lưới là nguồn CỘNG THÊM, KHÔNG thay thế**; `fromPreset(THREE)` trả **null** thay vì làm tròn (làm tròn âm thầm = đổi bố cục owner đã duyệt mà không ai biết).
  - **PHÉP CHỨNG MINH CHỊU LỰC**: test quét **4 bố cục × 5 cỡ màn × 5 khe hở = 100 tổ hợp**, đòi khung pixel ra **ĐÚNG TỪNG PIXEL** như `WorkspaceLayout`. Không có nó thì đổi tầng vẽ ở bước 3 là đánh cược. [ĐO] nó **bắt lỗi ngay lần chạy đầu**: tôi dùng **làm tròn**, code cũ dùng **chia số nguyên (cắt)** ⇒ lệch 1 pixel ở 2-cột/1920/khe-1 (959 vs 960). Hình học giống nhau, chỉ quy tắc làm tròn khác.
  - **Luật kiểm tra**: đè nhau / ra ngoài lưới / nhỏ hơn 2×1 ô = **LỖI** (đè thì nói ĐÍCH DANH cặp nào để trình vẽ tô đỏ); **"còn ô trống" KHÔNG phải lỗi** (lệch với cách backlog liệt kê — chặn vì lý do đó là ép người dùng phủ kín màn), vẫn báo được số ô trống.
  - **[ĐO] thử phá 5 phép: 4 đỏ đúng chỗ, 1 phép lộ NHÁNH DƯ THỪA** — nhánh "khung sát mép thì lấy trọn bề rộng" bỏ đi mà 0 test đỏ ⇒ công thức đã tự cho ra đúng mép, **chứng minh được**: `edge(count) − gap = total` (đúng cả với chia số nguyên). Đã bỏ nhánh + thay bằng test khoá **chính tính chất**. Lần thứ hai trong hai phiên thử phá tìm ra code chết.
  - **Toạ độ theo Ô, không theo pixel** ⇒ bố cục không phụ thuộc cỡ màn; chuỗi lưu **tự đọc được** (`0,0,7,4;7,0,5,6`) để cứu dữ liệu bằng tay được.
  - **Chưa có lượt soát độc lập** (tác nhân soát hỏng nhiều phiên gần đây).

- **2026-09-11 (trưa) · P8 VÒNG KIỂM QUYỀN — DONE off-car.** Spec `docs/specs/kachi-permission-preflight.html`. **[ĐO] 2535/0** (đầu phiên 2503 ⇒ **+32**), APK sạch.
  - **Vì sao làm**: backlog ghi *"làm sớm giúp giảm nhiễu khi test trên xe"* — nợ-trên-xe là nút thắt lớn nhất. [ĐO] trước đó kiểm quyền **rải rác 5 chỗ**, không có nơi tập trung ⇒ người dùng chỉ biết thiếu quyền khi tính năng **im lặng không chạy**.
  - **6 điều kiện** gom về `LauncherRequirements` (:core thuần) + `PermissionPreflight` (:app đọc/cấp): đọc thông báo · trợ năng · vẽ overlay · cờ cửa sổ tự do · kênh shell · là màn hình chính. Luật: **đủ thì IM LẶNG** · **tự xin lại** cái tự xin được (không hỏi) · **KHÔNG chặn launcher** · **KHÔNG chỉ tới màn cài đặt** ([ĐO] màn đó khoá trên xe: *"Hệ thống IVI không hỗ trợ hoạt động này"*).
  - ⚠⚠ **[ĐO] PHÁT HIỆN QUAN TRỌNG — lời giải cho lỗi lặp lại nhiều phiên** ("gán phím vô-lăng hay mất quyền sau khi khởi động lại"): cấp quyền trợ năng xong đọc lại thấy CÓ (cờ=1); **tắt hẳn app** rồi đọc lại thì danh sách về **null**, cờ về **0** ⇒ **HỆ THỐNG TỰ THU HỒI** khi tiến trình chết, không phải app tự đánh mất. Vòng kiểm **tự lành** mỗi lần mở launcher. ⚠ giới hạn: đo bằng **tắt hẳn app**, CHƯA đo bằng **khởi động lại máy**.
  - ⚠⚠ **HAI GUARD KIẾN TRÚC (từ phiên B0–B6) ĐÃ BẮT TÔI LÀM SAI** — đáng nhớ: (1) tôi cho vòng kiểm ra **lệnh thô** bật cờ cửa sổ tự do ⇒ `PersistentWindowStateWriterGuardTest` đỏ (luật **một-nơi-ghi-duy-nhất** cho trạng thái bền, phía launcher **cấm ghi trực tiếp**); (2) tôi sửa bằng cách cho **màn chính** gọi đường gieo ⇒ `KachiAutostartServiceWiringTest` đỏ (*"HOME must not seed freeform"*). **Kết luận đúng**: vòng kiểm chỉ **BÁO**, đường khởi động mới **SỬA** ⇒ thêm loại `FixBy.SELF_AT_BOOT`, mô hình **đúng hơn** chứ không phải lách guard.
  - **[ĐO] thử phá lộ ra CODE CHẾT**: phép "đủ mà vẫn hiện thông báo" không làm đỏ test nào ⇒ dòng kiểm `if (allOk) return null` là **dòng chết** (đủ hết thì hai danh sách sau vốn rỗng) ⇒ đã bỏ. Bài học: thử phá không chỉ tìm test mù, còn tìm code chết.
  - **[ĐO] máy ảo**: xoá quyền đọc thông báo → mở launcher → nhật ký `thiếu: notif_listener, accessibility | tự xin: … → sau khi tự cấp: đủ quyền`, và cấu hình hệ thống **thật sự có lại** component của Kachi · ca đủ quyền = **không thông báo gì** · launcher vẫn dùng được.
  - **Còn tồn**: chỉ kiểm lúc mở launcher (mất quyền giữa lúc đang chạy thì phải mở lại) · phép "không ghi đè trợ năng của app khác" chỉ khoá bằng test logic, máy ảo lúc đo không có app trợ năng khác · **chưa có lượt soát độc lập** (tác nhân soát hỏng nhiều lần 2 phiên gần đây).

- **2026-09-11 (sáng) · ĐÓNG LỖ HỔNG SOÁT XÉT GÓI 3 + U1 ICON — DONE off-car.** **[ĐO] 2503/0** (đầu phiên 2482 ⇒ **+21**), APK sạch, `:core` vẫn 0 import `android.*`, 0 file chiếu-cụm bị đụng.
  - **Đóng 3 điểm gói 3 chưa ai soi** (spec `kachi-action-macros.html` §10 Pass 2 — tác nhân soát hỏng **2 lần**: lỗi kết nối rồi hệ thống quá tải ⇒ tôi tự soi nhưng **bằng thực nghiệm/thử phá**, không suy luận):
    - **[P1] LỖI THẬT**: ghi trạng thái nằm trong `tile.post` ⇒ ô bị `removeView` giữa lúc gói chạy thì **trạng thái có thể mất**, ô nói sai so với xe. Kèm lỗi **thứ hai chưa ai thấy**: `ControlTileState` dùng `HashMap` **thường** mà gói lệnh ghi từ **thread nền** ⇒ tranh chấp dữ liệu. Sửa: ghi trạng thái **ra khỏi** `post` + đổi sang `ConcurrentHashMap`. ⇒ **thiết kế không còn cần biết** hành vi của `post` trên view đã tháo (tài liệu Android không nói rõ).
    - **[P3] KHÔNG phải lỗi**: test **có** khoá thứ tự enum thật (thử phá: đảo 2 giá trị ⇒ đỏ). Nhưng giao kèo chưa ghi tại chỗ khai ⇒ đã ghi vào KDoc enum `EvidenceTier`.
    - **QUYẾT ĐỊNH OWNER, không tự đổi**: gói "rời xe" khoá xe dù đóng kính có thể hỏng → **OQ5** (đã đặt ưu tiên trong spec). Nhưng phát hiện lỗi thật đi kèm: kết quả gói chỉ vào **nhật ký** mà người lái không đọc ⇒ thêm `MacroResult.notice()` (:core) + hiện thông báo ngắn; **thành công thì im lặng**.
  - **U1 icon** (spec `kachi-capability-icons.html`): [ĐO] gốc **123 mục đọc chỉ 7 icon** (lấy theo NHÓM) + **59 tệp icon mà chỉ 29 được nối**. `CapabilityIcons` (:core) tra theo **khái niệm** (khớp mã → tiền tố → lùi về nhóm, **KHÔNG sửa 123 dòng registry**) + vẽ **6 icon mới**. **[ĐO] 7 → 29 icon** (4,1×); mọi nhóm từ 1 lên 2–7 icon; ảnh máy ảo: 30 ô nhóm năng lượng dùng 6 icon, **0 ô trống, 0 icon lỗi**. ⚠ bẫy: **tiền tố dài phải xét trước** (`tyre_t_` trước `tyre_p_`), có test khoá.
  - **[ĐO] Lỗi PHÁT SINH TỪ GÓI 2 đã sửa**: gộp đọc+hành động vào MỘT lưới làm lộ **18 nhãn trùng** (hai ô đều ghi "Kính trước-trái": một để xem %, một để bấm) + **1 ca trùng CÙNG LOẠI** (widget "Tốc độ" vs mục đọc "Tốc độ") mà bản sửa đầu của tôi không xử được — **test toàn cục "0 trùng còn lại" bắt ra**. Sửa: gợi ý loại (· xem / · bấm / · thẻ) **chỉ ở chỗ trùng**, **widget dựng tay xét TRƯỚC**, nhãn GỐC không đổi.
  - ⚠ **6 tệp XML MỚI** (icon) — là tệp mới, KHÔNG phải sửa layout niêm phong; đã chạy bộ test niêm phong = **0 đỏ**.
  - **Còn tồn**: icon phân biệt theo HỌ chưa theo từng ô (tia sét dùng cho 11 ô); cặp đối lập (nhiệt cao/thấp, giờ/phút) chưa phân biệt; nhãn hàng cuối bảng Tuỳ biến bị viewport cắt ~4px. **U1 chưa có lượt soát độc lập** (tác nhân hỏng 2 lần).

- **2026-09-11 (đêm) · LAUNCHER GÓI 3 "GÓI LỆNH" (W2 phần lớp gộp lệnh) — DONE off-car.** Spec `docs/specs/kachi-action-macros.html`. **[ĐO] 2482/0** (mốc trước gói 3 = 2434 ⇒ **+48**), 0 file XML + 0 file chiếu-cụm bị đụng, `:core` vẫn 0 import `android.*`.
  - **[ĐO] 3 SỰ THẬT NỀN**: (1) trong 64 nút, **mọi nút là MỘT lệnh** — không có khái niệm gộp lệnh nào ⇒ yêu cầu #7 của owner ("mở cửa + tắt/mở đèn") trước đây **không thể có**; (2) ⚠ nút mang nhãn **"Kính 50%" KHÔNG làm 50%** — nó ghi **ĐÚNG CÙNG** lệnh với `win_lf` (`setBodyWindowCtrlState(1,state)`, chỉ đóng/mở) ⇒ đã đổi nhãn thành **"Kính cửa lái"** + test khoá cả họ (bất kỳ nhãn nút chứa `%` ⇒ đỏ); (3) **đọc** được độ mở kính theo % (`getWindowOpenPercent`, PROVEN) nhưng **KHÔNG có lệnh GHI %** ⇒ **"kính ½ tất cả" KHÔNG làm được off-car**, thiếu đúng lệnh *dừng giữa hành trình* (đường đi đề xuất ở spec §4.5).
  - **`ActionMacros` (:core, THUẦN — không luồng)**: `MacroStep(controlId, arg, waitAfterMs)` · `ActionMacro.tier()` = **THẤP NHẤT** trong các bước (dùng `maxByOrNull { ordinal }` vì thứ tự khai enum = tin cậy giảm dần, **có test khoá giả định đó**) · `MacroRunner.run(macro, emit, sleep)` — **một bước hỏng KHÔNG dừng gói** (bỏ dở giữa gói còn tệ hơn) · `MacroResult.summary()` nêu ĐÍCH DANH bước hỏng. Chờ + bơm lệnh nằm ở `:app` ⇒ test off-car không phải chờ thật.
  - **4 gói định sẵn**: `mac_win_open_all` / `mac_win_close_all` (**gộp 4 nút kính riêng tier PROVEN thay vì nút `windows_all` tier OVERDRIVE** — giá trị cụ thể của lớp gộp) · `mac_door_light` (yêu cầu #7) · `mac_leave` (đóng kính → tắt đèn đọc → khoá, mọi bước đảo lại được). **C5**: cố ý KHÔNG gộp việc không đảo lại được.
  - **Gói lệnh là một KHẢ NĂNG** ⇒ `CapabilityCatalog` tra ra được ⇒ **đặt được ở thanh tác vụ** nhờ nền RW0 (⚠ đính chính 09-11: chỉ 1/3 vùng trọn vẹn — thanh trên còn chip cứng, ngăn kéo chưa bày hành động cho ô giữa màn) gói 2, không cần code đặt-chỗ mới.
  - **[ĐO] máy ảo**: 3 gói hiện đúng tên ở thanh nút + ô giữa màn · bấm ⇒ nhật ký `mac_win_open_all: không bước nào ăn (4 bước)` (đủ 4 bước, off-car no-op = bình thường), app **không sập/không treo** dù gói mất ~1.2 s · **chống bấm kép**: 3 lần bấm trong 0.3 s ⇒ đúng **1** lượt.
  - **Nợ gói 2 ĐÃ ĐÓNG**: bảng lốp nay nói **SAI CÁI GÌ** — `TyreStatus.reason` ("non"/"căng"/"lệch") ở :core, KHÔNG rải chuỗi trong bộ vẽ (có test khoá). [ĐO] ảnh: `"TT · căng · 82°F"` · `"SP · non · 93°F"`.
  - **[ĐO] lượt soát độc lập CHẾT giữa đường (lỗi kết nối)** nhưng kịp vá 3 lỗi, tôi đã tự kiểm chứng từng cái: (1) **[P1] ô gói lệnh bắn MỌI bước qua `toggle`** ⇒ bước STEP bị nén 1/0, SELECT mất chỉ số, BUTTON với arg=0 **không bấm gì**; 4 gói hiện tại chưa lộ (toggle≡cover) = **lỗi ngủ đông tới gói thứ 5** → thêm `CarControlPort.actByKind` ở :core, `CarControlAdapter.act` uỷ quyền về đó (bảng định tuyến 1 chỗ); (2) **[P2] cờ chống-bấm-kép kẹt vĩnh viễn** nếu thread chết trước lúc hạ cờ → hạ trong `finally`, **NGAY trên thread** chứ không trong `tile.post` (post trên view đã `removeView` có thể không bao giờ chạy); (3) **[P2] một test của tôi là TRANG TRÍ** — cắt vùng quét bằng mốc CHÚ THÍCH mà bộ quét đã bỏ hết chú thích ⇒ quét sai vùng nhưng vẫn xanh. Bản vá còn sửa thêm một lỗi tôi chưa nghĩ tới: sau khi gói ghi thật phải **cập nhật bảng trạng thái dùng chung**, không thì ô "Đèn đọc" vẫn sáng sau khi gói "Rời xe" đã tắt đèn.
  - ⚠ **3 điểm CHƯA có ai soi độc lập** (vì lượt soát chết): gọi lại giao diện sau khi ô đã bị tháo · phụ thuộc thứ tự khai enum · gói "rời xe" khoá xe dù bước đóng kính có thể đã hỏng. Ghi ra để phiên sau soi, **KHÔNG tự tuyên bố đã duyệt**.

- **2026-09-10 (đêm) · LAUNCHER GÓI 2 "Ô KHẢ NĂNG HỢP NHẤT + LỐP + LẤY GIÓ + ĐƠN VỊ" (RW0 · W4 · W3 · W-unit) — DONE off-car, senior APPROVED.** Spec `docs/specs/kachi-unified-capability-tile.html`. **[ĐO] full 5-module 2434/0** (mốc trước gói 2 = 2343 ⇒ **+91**), APK debug sạch, **0 file XML** + **0 file chiếu-cụm** bị đụng, `:core` vẫn 0 import `android.*`.
  - **[ĐO] 4 SỰ THẬT NỀN (đọc code, không suy luận)**: (1) KHÔNG phải 2 mà **3** bộ đăng ký, **195 mã giao nhau RỖNG** cả 3 cặp ⇒ dùng MỘT không gian mã phẳng, **KHỎI chuyển đổi cấu hình người dùng đã lưu**; (2) ô giữa màn ĐÃ nhận datum đọc, control id vào ô thì ra ô vô dụng nhưng KHÔNG sập; (3) **cổng chặn thật = 1 dòng** `DockConfig.setEnabled` (`ControlRegistry.byId(id)==null → return this` = bỏ qua IM LẶNG mọi mã không phải nút); (4) `ControlDockView` KHÔNG nhận `CarStatus` (thanh trên thì có).
  - **RW0**: `CapabilityKind{READ,WRITE}` + `CapabilityPick` + `CapabilityCatalog{kindOf,isWrite,pick,all,byDomain,collisions}` ở `:core` (mở rộng `LauncherCatalog.kt`) · nới cổng chặn (**có test canh**: hoàn nguyên ⇒ `ControlRegistryTest` đỏ) · `ControlTileFactory` (:app) = bộ dựng ô DÙNG CHUNG 3 vùng · `CustomizePanel` đổi từ `ControlPanels.byDomain()` → `CapabilityCatalog.byDomain()` (trước đó chỉ bày NÚT ⇒ nới cổng thành vô nghĩa vì không có đường thêm ô đọc).
  - **W-unit (owner thêm giữa phiên: *"mấy cái có đơn vị thì cho user chọn, đừng ép"*)**: `Units.kt` (:core) — `Quantity` 7 loại · `UnitPrefs` (encode/decode, CHUNG mọi hồ sơ) · `UnitFormat.apply` = **bước TRÌNH BÀY RIÊNG**, KHÔNG nhét vào `TelemetryReadout.of` (vì test đang khoá `tyre_p_fl="241"` kPa ⇒ tách ra thì 0 test cũ phải sửa). Loại đại lượng **suy từ chuỗi đơn vị** đã khai ⇒ KHÔNG sửa 61 dòng registry. **[ĐO] 61/123 mục có đơn vị, 26 mục có lựa chọn** (°C×12 · km×6 · kPa×4 · km/h×2 · 3 lẻ). Mặc định áp suất = **bar** (giữ đúng thứ owner đang thấy). ⚠ **BẤT BIẾN**: đích trùng đơn vị gốc ⇒ trả view NGUYÊN VẸN, KHÔNG format lại (format lại làm "28.5"→"29" âm thầm; mutation dựng lại lỗi này ⇒ 3 test đỏ).
  - **W4**: `TyreBoard` (:core thuần) + `TyreBoardView` (:app Canvas). ⚠ **ĐÍNH CHÍNH**: widget cũ ĐÃ hiện 4 bánh riêng; cái sai THẬT là **ngưỡng cứng 2.2 viết tại chỗ** = ngưỡng THỨ BA lệch với `:core` — đã xoá cùng `tyreBars()`. Ngưỡng: non<2.0 · căng>3.2 · lệch≥0.3 bar (**số tôi tự chọn, OQ1 chưa có câu trả lời owner**, đặt 1 chỗ). Lệch chỉ đánh dấu bánh THẤP NHẤT (gắn cảnh báo lên bánh đủ hơi gây hiểu sai).
  - **W3**: `Prefs.recircOnStartEnabled` (mặc định TẮT) + `RecircApplier` (dùng lại `AppContainer.carControl`, KHÔNG gọi HAL trực tiếp) + gọi từ `BootSetupService` SAU ghế/lọc-bụi. Ô tick ở **bảng Tuỳ biến dựng bằng code** ⇒ **0 file XML** (C3, không cần xin đóng dấu lại). ⚠⚠ **mã `recirc` tier OVERDRIVE = CHƯA kiểm trên xe owner** (khác ghế/lọc-bụi PROVEN) ⇒ UI mang cảnh báo chưa-kiểm; **KHÔNG được hứa nó chạy**.
  - **[ĐO] 4 LỖI CHỈ LỘ KHI ĐỌC ẢNH máy ảo** (2434 test xanh KHÔNG bắt được — bằng chứng test không thay được việc mở app ra xem): (a) **nhiệt độ bảng lốp vẫn ghi °C** dù chọn °F (ô vẽ tự ghép ký hiệu cứng — đúng loại lỗi gói này đi dọn); (b) 2 nhãn ô đọc bị cắt thành y hệt nhau ("Áp lốp trước-t…"); (c) một bánh HAI màu cảnh báo (số đỏ + dòng phụ hổ phách); (d) hình xe gần vuông (0.85) + viền chênh nền 21/255 nên tan vào nền. Đã vá hết + khoá test. Chip thanh trên cũng từng bỏ qua lựa chọn đơn vị.
  - **[ĐO] senior review APPROVED sau 9 bản vá**, trong đó **3×[P1] tôi làm sai**: (1) **cổng chặn KHÔNG có test nào canh** — hoàn nguyên 1 dòng mà 2431 test vẫn xanh (tài liệu tôi khai "mutation đỏ" là SAI, đã đính chính); (2) **đổi đơn vị dựng lại TOÀN BỘ ô** ⇒ nhả `VdAppHost`, app trong ô phải mở lại → sửa thành chỉ dựng lại ô widget; (3) **R3 hụt ở ô giữa màn** — planner dựng lại mọi ô widget theo nhịp trạng thái xe, mà từ RW0 ô widget có thể chứa NÚT ⇒ trên xe (1 nhịp/giây) view bị tháo/gắn giữa cú chạm ⇒ **MẤT cú bấm**; off-car không lộ vì mọi field null.
  - **Nợ 🚗**: số áp suất/nhiệt THẬT để chốt ngưỡng · lệnh lấy gió có ăn không · ô đọc/ô hành động khi có kênh điều khiển thật · khoảng đứng bảng Tuỳ biến **187 ô** dựng đồng bộ trên thread chính (trước gói 2 là 64). **Còn tồn**: nút đổi-app đè đỉnh hình xe (chrome chung mọi ô, sửa khi làm P9) · bảng lốp cho biết bánh nào nhưng chưa nói SAI CÁI GÌ · nhóm khả năng dùng 1 icon cho cả nhóm (nợ U1 ~45 icon).
  - **Việc MỚI vào backlog**: **U5** = đa ngôn ngữ CHUẨN cho launcher (owner CHỐT tách riêng, không nhập gói 2). [ĐO] hiện trạng: **0** thư mục ngôn ngữ chuẩn · 24 chuỗi trong tài nguyên · cơ chế dịch hiện tại = ghi cặp Việt–Anh tại chỗ dùng + bộ quét cây giao diện (94 cặp) · **launcher dùng 0 lần** (ClusterNav cũ: 216 lần) · nhãn 195 mục khả năng chỉ có tiếng Việt.

- **2026-09-10 (tối) · LAUNCHER GÓI 1 "MỞ APP CHO ĐÚNG" (P-bug2 · U3 · U2) — DONE off-car, senior APPROVED, ĐÃ PUSH.** Backlog **tái phân nhóm theo 6 chủ đề** + **bỏ track ClusterNav** (owner chốt "dự án chỉ làm launcher"; backlog kế thừa → `docs/archive/clusternav2-backlog-inherited-2026-09-10.md`). Spec `docs/specs/kachi-open-app-correctly.html`.
  - **P-bug2 (bug ảnh hưởng CẢ XE) sửa xong**: `WorkspaceView.applyEmbedSeam(shell, inputClient, registerVd, unregisterVd, state, status)` gắn **NGUYÊN KHỐI** 4 kênh rồi tự dựng lại ô App 1 lần; 4 field đổi thành **private** ⇒ [ĐO] thử dựng lại lỗi cũ (gán rời + `render` trần) **KHÔNG BIÊN DỊCH ĐƯỢC** (mạnh hơn test đỏ). Luật render tách sang **`:core WorkspaceRenderPlanner.decide(old,new,builtSlotCount,statusChanged,embedChanged)`** (thuần JVM) — thêm ĐÚNG 1 lý do dựng lại: `embedChanged && nc is App`; kèm test **tương-đương-hành-vi-cũ >1000 tổ hợp** (khoá P-bug1).
  - **U3**: nút **"Ứng dụng"** trên `KachiTopStrip` → `DrawerController.openAppList()` → `AppDrawer(mode = OPEN_APP)` (bỏ hẳn mục widget) → `KachiHomeActivity.openAppFullscreen` → **`AppOpener`**. Mục **Gần đây** = `:core RecentApps` (MRU ≤12, lưu ở `WorkspacePrefs` khoá `recent_apps` CHUNG mọi hồ sơ; **cố ý KHÔNG dùng UsageStatsManager** vì màn cài đặt hệ thống trên xe khoá). Recents đi qua `WorkspaceRepository.recentApps()/touchRecentApp()` (**thân mặc định** ⇒ 0 churn cho fake) và **KHÔNG vào `HomeUiState`** (chỉ đọc lúc mở ngăn kéo).
  - ⚠ **SAI LỆCH SPEC ĐÃ SỬA THEO SỐ ĐO — nhớ kỹ**: [ĐO] `am start --windowingMode 1` (`FreeformLaunch.fullscreenCmd`) đưa app **từng nằm trong ô** về **khung ô CŨ** (Clock → 1132×768) = SAI; còn `ActivityOptions.setLaunchBounds(null)` phủ hết màn. App sạch thì cả hai đều đúng 1920×1080. ⇒ **đường API là CHÍNH** (`AppOpener.openByIntent`, thread CHÍNH), shell chỉ là lưới an toàn (`openByShell`, thread NỀN). Suy luận "phải ép chế độ cửa sổ 1" là SAI.
  - **U2** (mức tối thiểu): `LauncherWindows.placeApp` nhánh `fresh=false` → `moveToSlot` (resize-only); `fresh=true` giữ `force-stop`+`openInSlot`; **`reflow` KHÔNG đụng** (cố ý mở lại để nâng z-order). ⚠ [ĐO] bán kính HẸP: `embedding()` true ⇒ mọi lệnh freeform early-return ⇒ chỉ áp ca KHÔNG nhúng.
  - **[ĐO]** full 5-module **2343/0** (`--rerun-tasks`, tự chạy lại) · APK debug sạch · mutation **3 phép** đỏ/chặn đúng chỗ · **senior review APPROVED** vá 3×[P2]: `@Volatile` cho `shell` (đọc từ thread chính, ghi từ winExec) · rò rỉ cửa sổ ngăn kéo lúc `onDestroy` (đóng TRƯỚC `winExec.shutdownNow`) · chia sẻ 1 `Drawable` cho 2 `ImageView` (nhân bản qua `constantState.newDrawable`).
  - **[ĐO] emulator** (google_apis 1920×1080@240, 1 máy, `adb reverse tcp:5555 tcp:5555`): mở nguội KHÔNG chạm gì ⇒ màn ảo ô **0 → có ≤3s**, 2 task app trên màn ảo đúng cỡ ô, ảnh xác nhận **Maps render bản đồ THẬT** trong ô · mở app ⇒ **[0,0][1920,1080]** · ô + bố cục **KHÔNG đổi** · "Gần đây" hiện đúng sau lần mở đầu.
  - **Nợ 🚗**: mở nguội lúc nổ máy · HOME về được · mượt/z-order · không ảnh hưởng chiếu-cụm · U2 on-car. **Còn tồn tại đã đo**: app từng nằm trong ô thì cửa sổ phủ hết màn nhưng **lệch ~36px** (thanh tiêu đề cửa sổ); Android 10 KHÔNG có lệnh chữa sạch (thử `--windowingMode 1` · `am task resize` · `am stack remove-task`=không tồn tại) — trên xe ô dùng **màn ảo riêng** nên dự kiến không gặp.

- **2026-09-10 (chiều) · TEST EMULATOR (Play Store + mở-app-vào-ô) — cây git SẠCH, revert sửa ad-hoc.** Owner cài app qua Play Store + thử Kachi mở-app-vào-ô "như xe" → phiên đi sâu gỡ rối emulator. **[ĐO] finding**: (1) popup adb lặp = image Play Store **production khoá cứng** (`adb root` chặn, không ghi `adb_keys`) → không pre-authorize dadb → artifact emulator, KHÔNG phải xe; (2) dadb loopback CHẠY trên `google_apis` (secure=0, appops=allow); bẫy 2-emulator = `reverse tcp:5555` trỏ nhầm (emulator thứ 2 adb ở host:5557) → chỉ chạy **1 emulator** cổng chuẩn; (3) **[BUG launcher, cả xe] app trong ô không host lúc mở** (probe dadb async xong SAU render đầu + `WorkspaceView.render` diff theo nội dung → không gắn `VdAppHost`; đổi preset mới host) — diff sửa 2 dòng (`rebuildSlots()`) **đã kiểm** (VD tạo ngay khi mở) nhưng **ĐÃ REVERT** (làm lại đúng quy trình = P-bug2); (4) ~~**[GIỚI HẠN] Waze/Maps từ chối display phụ**~~ **← QUY KẾT NÀY SAI, ĐÃ BÁC BỎ 2026-09-14** (xem mục 09-14 bên dưới); (5) GMaps/YouTube trên google_apis là bản 2019 GMS cũ; ~45 nút W1 mới thiếu icon (U1); chấm cam = badge tier đúng. **Emulator tốt cho UI/widget/dữ liệu-W1/nút/Tuỳ-biến; app-vào-ô thật = xe.** Handoff `docs/_handoff/session-2026-09-10-emulator-launcher-hosting.md`; bằng chứng `docs/diagnostics/kachi-emulator-app-hosting-2026-09-10.md`. Cây `main` ahead-1 (`b2c85f7`), 0 file lệch, CHƯA push.
- **2026-09-14 · NGHIÊN CỨU "vì sao Waze không vào được ô" (không sửa mã sản phẩm).** [ĐO source `android-10.0.0_r47` + máy ảo] Gate thật = `ActivityStackSupervisor.isCallerAllowedToLaunchOnDisplay` `:1096-1106` (A12 `ActivityTaskSupervisor:1108-1116`): màn `TYPE_VIRTUAL` + **uid chủ ≠ SYSTEM ≠ uid app** + activity đích không khai **`android:allowEmbedded`** ⇒ từ chối. **KHÔNG** phải cờ riêng-tư (`isPrivate()` mãi tới `:1115`; màn **công khai** của `ActivityView` bị chặn y hệt), **KHÔNG** phải `resizeMode`/PIP (Waze khai `resizeableActivity=true`; Clock có activity khai `false`). Shell uid 2000 **CÓ** `INTERNAL_SYSTEM_WINDOW` ⇒ `am start --display <vd>` **thành công**; cái làm hỏng là **activity trung chuyển** `com.waze/.FreeMapAppActivity` → `.MainActivity` gọi từ **uid của Waze** ⇒ `Failed to put TaskRecord{…} on display N` ⇒ cả task rơi về display 0 toàn màn. **Clock VÀ Google Maps ở lại màn ảo bình thường.** [ĐO] đường lùi **freeform display 0** (`--windowingMode 5` + `am task resize`) **chạy tốt với Waze trên máy ảo**, nhưng bị `KachiHomeActivity:159` `embedding = shell != null` chặn (⇒ trên xe không có đường lùi). **CHƯA làm** — [ĐO] DashCast v1.2.72-beta: DL5 cắt `set-task-windowing-mode`, `task resize` no-op im lặng; freeform display 0 trên ROM DiLink **[CHƯA BIẾT]**. Bằng chứng: `docs/diagnostics/waze-into-slot-research-2026-09-14.md`.
- **2026-09-14 · H2 — VÒNG ĐỜI Ô CHIẾU APP (đã vá, off-car, 1.53/54).** [ĐO] trước vá `emulator-5554`: **4** màn ảo `kachi-slot-*` cho **2** ô + **4** `KachiHomeActivity` (3 cờ `f`) ⇒ giải phóng VD KHÔNG được treo vào `onDetachedFromWindow` (màn "đang kết thúc" giữ view; `removeView` chỉ tháo khi cây view đang gắn cửa sổ). Nay VD thuộc sở hữu **theo Ô** (`SlotVdLedger` :core + `SlotVdOwner` :app, log tag `KachiVd`), `VdAppHost.release()` idempotent gọi ở mọi đường thay/đóng/dựng-lại/huỷ; app trong ô chết thì `SlotLiveProbe` (1 lệnh `am stack list`/5 s cho MỌI ô) giấu mặt vẽ + hiện nhãn *"App đã đóng — chạm để mở lại"*. [ĐO] sau vá: **2** VD cho 2 ô, ổn định. Spec `specs/kachi-open-app-correctly.html` §9 · backlog **H2**. **Soát senior Pass H2 (cùng ngày)**: nhịp đo nay **ngưng khi màn khuất** (`SlotLiveProbe.pause/resume` ở `onStop`/`onStart`, đếm số màn đang hiện) — mọi lệnh shell xếp hàng trên MỘT chủ `ShellTransport`, và màn Kachi không chết khi mở app toàn màn; gỡ 2 hàm `SlotLiveness` không có chỗ gọi (§8).

- **2026-09-10 · LAUNCHER W1 — NỐI DỮ LIỆU/ĐIỀU KHIỂN XE THẬT (registry-driven) DONE off-car, senior APPROVED.** Thay `DemoCarData`/`NoCar` bằng lớp thật: `:core` capability layer (`TelemetryRegistry` 121 datum/8 domain · `ControlRegistry` mở rộng 65 nút/5 kind TOGGLE·STEP·COVER·SELECT·BUTTON · `CarStatus` 8-nested nullable · `CarCapabilities` · `HalBindingTable` named-method+feature-id-số fallback+sentinel `-2147482648` → unavailable · `CarDataAdapter`/`CarControlAdapter`/`CarStatusRepository` StateFlow 2 nhịp · `TelemetryReadout`/`LauncherCatalog`) + `:app` (`BydHalGateway` reuse `BydHal` reflection · `MediaBridge` MediaSessionManager · UI renderer generic theo `WidgetShape`/`ControlKind` + tier badge "chưa kiểm trên xe" + off-car "—" · `CustomizePanel` gom panel theo domain · wire `AppContainer`→`HomeViewModel` collect StateFlow `repeatOnLifecycle`, UDF một chiều). **GATE AN TOÀN ĐÃ BỎ** (owner "tự dùng tự chịu" — mọi control ghi thẳng, KHÔNG chặn speed/gear/permit, KHÔNG nhãn ADAS). **[ĐO]** full 5-module **2302/0** (Stage 1–5) · `:app:assembleDebug` sạch · LayeringRules(pureFilesStillInApp=0)/LayoutParity/WindowingGuard xanh · grep no-gate + no-DemoCarData-in-wire. **Committed `b2c85f7`** (local ahead-1, CHƯA push). Nợ 🚗: **~47/121 telemetry** pickable trong Tuỳ biến — Stage 5 (owner "làm tối đa") ĐÃ NỐI **118/123** (thêm 46 field `CarStatus` + đọc + case `TelemetryReadout`); còn **5 NEEDS_CAR** (4 GPS NaviInfo + target_soc — không có đường đọc, không bịa) hiện "—" → **grab-list on-car** (spec §9: dump `/system/framework/*bydauto*.jar` lấy feature-id/method theo trim + verify từng cái). Spec `docs/specs/kachi-w1-real-data.html`; template autonomous `docs/_handoff/kachi-w1-real-data.md`; handoff `w1-stage{1,2,3}-done.md`.

- **2026-09-09 · LAUNCHER: KIẾN TRÚC SẠCH (B0–B6, DONE off-car + senior APPROVED). ✅ PUSH 2026-09-10 → `dangkhoi/byd-kachi` (PUBLIC, MIT). SDD `docs/specs/kachi-architecture.html`.** Full 5-module **2202/0**; +Stage 7 SDD; danh tính scrub (token công việc cũ)→dangkhoi ở cả history (`git-filter-repo`) ⇒ SHA cũ `4918ead`..`38fdb25` đã đổi (HEAD `508e512`). Nợ trên xe: ARCH-🚗 · X1 cast-side · P2/P3/P4 🚗; off-car kế: **W1** nối BydHal thật (thay DemoCarData/NoCar).
  - **Tầng (đúng 3, phụ thuộc đi xuống)**: Domain `:core system/` (THUẦN, cấm android.*) = `WindowMutation`(sealed, render byte-stable) · `MutationPriority`(STOP/RESCUE▸NORMAL) · `DisplayOwnershipRegistry`(launcher=display0+VD, cast=display1, **reject cross-boundary**) · `AppLocationRegistry`(1 pkg=1 nơi) · `PrioritySerialExecutor` · `FreeformSeedPolicy`(single-writer, marker commit-trước-mutate, `FF_USER_REMOVED` terminal) · `inputd/`(InputWireProtocol/SlotTouchMapper/InputDaemonLaunch/TouchRouter) · `AutostartGate` · `LauncherBootPlan`. Data `:app` = **`ShellTransport`(CHỦ dadb `localhost:5555` DUY NHẤT, gộp 3→1, serialize+priority)** · `WindowCommandDispatcher`(validate ownership TRƯỚC dispatch) · `FreeformSeedStore` · `inputd/`(InputDaemonMain/Client/EventInjector/LocalAbstractChannel) · **`AppContainer`(1 DI graph thủ công, `SimpleCastRuntime` by-REFERENCE)** · `KachiApplication` · `KachiAutostart(+Service)`. UI `:app` (UDF một chiều) = **`HomeViewModel`(`StateFlow<HomeUiState>` = nguồn sự thật DUY NHẤT)** + `HomeViewModelFactory` + `PrefsWorkspaceRepository`; `KachiHomeActivity` mỏng 250 LOC (collect `repeatOnLifecycle`) + `KachiTopStrip`/`DrawerController`/`ProfileBar`/`DockAreaLayout`; `WorkspaceView` = view THUẦN (render(state)+callback, **hết giữ state**).
  - **Coexistence cluster-cast = 4 LUẬT**: (1) 1 `ShellTransport` cho MỌI lệnh cửa sổ (cả cast); (2) cổng chủ quyền (launcher **CẤM chạm display ≥ 1**); (3) 1 nơi ghi state bền (`FreeformSeedPolicy`: freeform seed + `wm size/density`); (4) trọng tài task (`AppLocationRegistry`). **Chạm đi đường RIÊNG** (input daemon `injectInputEvent` qua socket `localabstract`, fallback `input -d`) — KHÔNG qua hàng đợi lệnh; chỉ lifecycle daemon qua hàng đợi. Dep Context7 verify: coroutines 1.10.2 · lifecycle 2.9.0 · turbine 1.2.1.
  - **[ĐO]** full 5-module **~1664/0**; golden `LauncherCommand` 11 + guard `LauncherWindowing` 5/`PersistentWriter` 4/`LayeringRules` 9 (`pureFilesStillInApp`=0) + cast 48 file/557 test XANH; **0 file logic cast đụng** (chỉ `CastShell` freeform-seed delegate byte-identical). THUẦN CODE, KHÔNG đụng seal/MainActivity/appId.
  - **🚗 ON-CAR PENDING** (deferred, owner chấp nhận — cast không E2E-verify off-car + emulator dadb loopback down): cast STOP hàng đợi chung + cast-side `AppLocationRegistry`; gộp 12 geometry-writer cast vào `FreeformSeedPolicy`; input-daemon `injectInputEvent` E2E; hosting/reflow/DecorCaption; cold-boot mount VD; freeform bền sau **reboot VẬT LÝ**. Backlog: ARCH ✅ · X1 🔨 partial · `ARCH-🚗` checklist.

- **2026-09-09 · LAUNCHER: REFLOW cửa sổ + EMULATOR = XE (window mgmt) + PIXEL-PERFECT vs prototype (90/100).**
  - **Emulator mirror xe (owner: "giả lập giống y chang xe, port khác thì config")**: app đặt/di cửa sổ bằng `am --windowingMode 5` + `am task resize` qua **dadb loopback localhost:5555** — CÙNG đường cast. adbd máy ảo dùng qemu-pipe (không mở tcp guest), nên "config cổng" = **`adb reverse tcp:5555 tcp:5555`** 1 lần/boot (guest:5555→host adbd; `ro.adb.secure=0` nên khỏi auth). `DadbShell`(:app) probe → OK thì dùng `ShellAppLauncher`(:core), không thì fallback `IntentAppLauncher` (setLaunchBounds, chỉ mở). **[ĐO]** probe=true, ShellAppLauncher active.
  - **REFLOW (owner: đổi bố cục phải sắp lại app đang mở đúng ô theo thứ tự, KHÔNG reset; overflow → fullscreen nền)**: model = ô vị-trí `slots[0..3]` (khớp prototype: mảng 4 cố định + ẩn ô thừa). Đổi preset/dock → `reflowWindows()`: app ô hiện (i<slotCount) → `openInSlot` (am wm5 đưa lên trước + resize đúng khung); app tràn (i≥slotCount) → `closeSlot` (fullscreen) + `HOME_FRONT` (kéo launcher che); z-order: overflow < Kachi < app hiện. Mọi op cửa sổ trên `winExec` 1-thread (dadb blocking). **KEY**: `setLaunchBounds` CHỈ áp lúc TẠO task, KHÔNG đặt lại task đang chạy → BẮT BUỘC `am task resize` (shell). `openInSlot` POLL taskId (12×250ms)+retry (app nặng vẫn vào ô; app khoá-resize như documentsui → fullscreen, app-specific). **[ĐO] (bounds+log+sub-agent)**: Clock+Contacts QUAD(TL/TR)→2c(trái/phải full-height)→1(Clock full, Contacts overflow ẩn)→2c(cả 2 về ô, không reset); render cửa sổ THẬT.
  - **PIXEL-PERFECT (audit 1:1 render prototype vs emulator, sub-agent 90/100)**: header = bar bo góc + viền mờ (rgba(9,12,19)+#26ffffff); **slot-head** đúng prototype (chấm TRÒN màu theo ô purple/green/cyan + tên + nút **⇄ đổi app** (`ic_swap`) + **✕** (`ic_close`), fade tối); dock tile active = **gradient accent BÁN TRONG SUỐT** + viền (`KachiTheme.gradientSoft`, khớp `.dtile.on`) + container bar rõ + tile tắt xám vừa; **board** 4 ô-thẻ icon-trên + số + phụ đề (bolt 82%/418km, leaf 18µg/PM2.5 Tốt, tire 2.1–2.4, music Đang phát/Sơn Tùng); ring track tối gần vô hình (#181b21), PM2.5 center số "18/µg/m³" + caption "tự lọc đang bật", energy "≈418 km · đầy sau 40′"; **WallView** giảm glow (hết bloom tím). Icon mới: `ic_swap.xml`/`ic_close.xml`.
  - **#4 caption (chưa đạt — platform)**: app ĐÃ lọt đúng khung, nhưng cửa sổ freeform AOSP có **DecorCaptionView** (nút phóng to+✕) — app thường KHÔNG gỡ được (system/WM riêng mới bỏ). Trên WM tuỳ biến xe BYD dự kiến không có → **verify trên xe**. Không phải assumption (đã xác minh dumpsys).
  - **[ĐO]** full `./gradlew test` 5 module **BUILD SUCCESSFUL** (2m2s); `:core:test launcher.*` xanh; cài+chạy clusternav10. THUẦN CODE, KHÔNG đụng seal. Files: `DadbShell.kt`(mới)·`IntentAppLauncher.kt`·`KachiHomeActivity.kt`·`WorkspaceView.kt`·`WidgetViews.kt`·`WallView.kt`·`RingView.kt`·`ControlDockView.kt`·`KachiTheme.kt`·`ic_swap/ic_close.xml` · core `LauncherPorts.kt`(+moveToSlot)·`ShellAppLauncher.kt`(poll+moveToSlot). **CHƯA commit.** ⚠ Emulator cần `adb reverse tcp:5555 tcp:5555` mỗi boot để reflow chạy (mirror cổng xe). Kế: on-car (Kachi tự seed freeform lúc boot qua CastShell.ensureFreeformSeed + verify caption/fps/z-order trên xe).

- **2026-09-09 · LAUNCHER: ĐẠI TU GIAO DIỆN + P3/P4-UI + P2 (emulator) — Kachi bám prototype, fidelity 93/100 (sub-agent).**
  - **Đại tu nhìn (owner: "launcher UXUI phải cực đẹp, giống 100% prototype")**: `:app launcher/` thêm `KachiTheme` (bảng màu + drawable card/gradient/pill), `WallView` (nền tối + glow xanh trên-trái/tím dưới-phải), `RingView` (vòng đo Canvas). `WidgetViews` dựng lại bám prototype: ring Năng lượng(xanh)/PM2.5(cyan), bảng 2×2 kính màu, media art+progress, tốc độ + biển giới-hạn EU. `KachiHomeActivity`: thanh trên = segmented preset (ô chọn = pill gradient) + pill "Thanh"/"Cài đặt", **immersive fullscreen**. `ControlDockView`: thẻ kính trong mờ, tile bật = gradient, **icon vector** (18 file `res/drawable/ic_*.xml` line-art + `IconMap` map `ControlDef.icon`→drawable).
  - **P3/P4-UI DONE**: dock 4 viền toggle+stepper (−/+), widget trong ô, `WorkspacePrefs` lưu dock.
  - **P2 DONE (emulator E2E)**: `AppDrawer` (lớp phủ kính + lưới app THẬT `PackageManager` MAIN/LAUNCHER) → chạm app → đặt vào ô (`SlotContent.App`, `WorkspaceView.appCard` icon+tên + "Giữ để gỡ"); ô trống = viền đứt + ＋ (`emptyAdd`). Freeform CAR sau `AppLauncher` port: **`FreeformLaunch` (:core** builder lệnh `am ... --windowingMode 5` + `am task resize` + resolve/parse taskId, **tái dùng công thức AppMover/CastGeometryController proven on-car; 6 test xanh `FreeformLaunchTest`)** + `ShellAppLauncher` (:app adapter chạy qua dadb uid-shell). Emulator KHÔNG có dadb loopback → HOME dùng `NoCar` (giữ thẻ app); **cửa sổ freeform THẬT + fps/z-order verify TRÊN XE** (on-car TODO, feed bởi RE doc).
  - **[ĐO]** `:core:test FreeformLaunchTest` xanh; `:app:assembleDebug` sạch; cài+chạy AVD clusternav10 (pm clear reset); **UI automation** (uiautomator dump → input tap) lái QUAD→ô trống→drawer→chọn Settings; sub-agent verify **93/100** (HOME polish · QUAD dashed empty · drawer app thật · app-in-slot đều PASS). Screenshot `docs/prototypes/kachi-p34-styled-emulator.png` + `kachi-p2-appslot-emulator.png`. THUẦN CODE, KHÔNG đụng layout/strings seal.
  - ⚠ mũi tên xanh mép phải VẪN là artifact emulator (dumpsys: 0 overlay window `com.byd.launcher`). Nit nhỏ: gradient pill hơi nhạt ở res thấp. **CHƯA sync backlog §L (làm ở consolidate) · CHƯA commit.** Kế: **P5** (hồ sơ tài xế + theme ngày/đêm `ThemeMode` + kéo-thả) → P6 → consolidate (senior review + full 5-module test + backlog sync).

- **2026-09-09 · LAUNCHER P5 (emulator) — hồ sơ tài xế + kéo-thả + theme (model).**
  - **Hồ sơ tài xế**: `WorkspacePrefs` khoá theo tên hồ sơ (load/save + dock scoped `"<profile>__…"`) + `profiles()/activeProfile()/addProfile/deleteProfile`; thanh trên thêm **pill hồ sơ** (chạm = đổi hồ sơ; giữ = tạo mới qua dialog `EditText`); `applyProfile()` nạp lại workspace+dock+preset. **[ĐO]** sub-agent PASS: pill "Mặc định" → dialog "Hồ sơ mới" → gõ "Vo"+Tạo → pill đổi "Vo", workspace về mặc định.
  - **Kéo-thả**: `WorkspaceView` long-press → `startDragAndDrop` + `OnDragListener` ACTION_DROP → `onSlotSwap` đổi chỗ 2 ô (KachiHomeActivity.swapSlots + save). App-card thêm nút **✕** gỡ (thay long-press-clear cũ). *(Compile OK; kéo-thả E2E khó tự động bằng adb → verify tay/trên xe.)*
  - **Theme**: `:core ThemeMode` (DAY/NIGHT/AUTO + `isNight(hour)`/`next()`/`label()` + **3 test xanh**) + `WorkspacePrefs.themeMode()/setThemeMode()`. ⚠ **Bảng màu NGÀY chưa áp đủ + chưa có nút gạt UI** (deferred: phải themify hex cứng ở WidgetViews/WorkspaceView/ControlDockView/WallView) → để P6/consolidate. Dialog đổi **dark theme** + `onWindowFocusChanged`→`goImmersive` (giữ fullscreen sau dialog/bàn phím).
  - **[ĐO]** `:core:test launcher.*` xanh (thêm FreeformLaunchTest 6 + ThemeModeTest 3); `:app:assembleDebug` sạch; cài+chạy clusternav10. THUẦN CODE, KHÔNG đụng seal. Kế: **consolidate** (full 5-module test + senior review + APK + sync backlog §L) → **P6 optional** (bảng màu ngày đủ + nút gạt · AppWidgetHost · cluster-cast tie-in).

## 5.old2. Trạng thái (2026-09-07)

- **2026-09-08 · P1 LAUNCHER (emulator) — vỏ HOME Kachi CHẠY được.** `:core launcher/` mới: `WorkspaceLayout` (preset 1/2c/2r/3/4 → Rect từng ô, khớp prototype) + `WorkspaceState` + `LauncherPorts` (Port/Adapter: `AppLauncher`/`CarDataPort`/`CarControlPort` + `NoCar` off-car) + **13 JVM test xanh**. `:app launcher/`: `KachiHomeActivity` (Activity framework, landscape, `CATEGORY_HOME`+DEFAULT — MainActivity GIỮ LAUNCHER, không phá ClusterNav) + `WorkspaceView` (ViewGroup đặt ô theo Rect) + `WorkspacePrefs` (SharedPreferences) + nút bố cục + đồng hồ + "Cài đặt"→MainActivity. **[ĐO]** `:core:test` xanh; `:app:assembleDebug` ra APK; **cài+chạy AVD clusternav10 (1920×1080)**, sub-agent xác nhận render đúng (clock/preset `1·2c·2r·3·4`/Cài đặt + 3 ô big-left+2-phải), screenshot `docs/prototypes/kachi-p1-emulator.png`. THUẦN CODE, KHÔNG đụng layout/strings seal. ⚠ mũi tên xanh mép phải KHÔNG do app (dumpsys: 0 overlay window của `com.byd.launcher`) — nghi artifact emulator. Kế: P3/P4-UI (dock+widget) → P2 (freeform reuse `CastShell`/`AppMover`/`CastGeometryController`) → P5.

- **2026-09-08 · ĐỔI TÊN LAUNCHER → "Kachi" (bước L2·label của fork byd-launcher).** Owner: "vừa fork ra làm launcher, đặt tên là Kachi" (repo `github.com/dangkhoi/byd-kachi`). Đổi các bề mặt DANH TÍNH app hiện cho người dùng: (1) `strings.xml` `app_name` "Cluster Nav 2.0" → **"Kachi"** — nhãn launcher trên icon/recents/danh sách app (`android:label="@string/app_name"` cho `<application>`+`MainActivity`); (2) tiêu đề trên màn: `MainActivity` ghi `txt_app_title` lúc chạy "ClusterNav"→"Kachi" (layout `android:text` chỉ là placeholder bị GHI ĐÈ lúc chạy — KHÔNG đụng seal dọc `activity_main.xml`); (3) tiêu đề thông báo foreground `BootSetupService`+`VietMapAutostartService` → "Kachi". GIỮ NGUYÊN `nav_listener_label`/`acc_label` (nhãn bề mặt DỊCH VỤ của tính năng dẫn đường ClusterNav — README giữ ClusterNav làm NHÓM TÍNH NĂNG), nhãn `DiagActivity`/`ClusterBlackActivity` (feature), Log tag, `AccessibilityRebind` "clusternav" (KHỚP package). **namespace vẫn `com.byd.clusternav`, applicationId vẫn `com.byd.launcher`** (KHÔNG đổi package → giữ liên tục OTA/chữ ký). Seal T11 `strings.xml` re-pin `8300437c…`→**`45fa51a8…`** (LẦN ĐẦU strings.xml đổi kể từ lập seal; KDoc trace "strings.xml · lần 1", owner duyệt; hằng `activity_main.xml` `556c78e4…` GIỮ NGUYÊN). **[ĐO]** full suite **1983/0** (5 module). CHƯA build APK / CHƯA commit (chờ owner). ⚠ Git remote hiện = `dangkhoi/byd-launcher`; owner nhắc `byd-kachi` — CHƯA đổi remote (là git config, chờ owner). Files: `res/values/strings.xml` · `MainActivity.kt` · `BootSetupService.kt` · `VietMapAutostartService.kt` · `ExpansionTransportFenceTest.kt`(seal).

- **2026-09-08 · SHIP v1.38 OTA (versionCode 39) — PM2.5: nút "Lọc ngay" + vòng poll tự-lọc.** Owner báo on-car v1.37: bụi lên tới mức xe hiện popup nhắc lọc mà KHÔNG tự lọc. Gốc [SUY]: `setAutoCleanAirState(1)` một mình KHÔNG chủ động lọc trên trim owner (chỉ đặt chế độ) — thứ THẬT SỰ lọc = `setQuickCleanAirState(1)`; app cũ chỉ bắn quick-clean 1 lần lúc bật (nếu lúc đó đã bẩn). **Sửa:** (1) `Pm25FilterApplier.cleanNow` (nút `btn_pm25_clean_now` CẢ 2 layout, seal re-pin **lần 27** `556c78e4`, id 87→88) = `setQuickCleanAirState(1)` NGAY, KHÔNG gate công tắc; (2) `startPollLoop` — công tắc BẬT poll mỗi `POLL_INTERVAL_MS`=45s, bụi ≥ `Pm25Filter.HEAVY`(5) → quick-clean (ClusterNav luôn chạy nền — owner "lo gì"); guard `polling`+`pollGeneration` (token thế hệ) chống race enable↔disable. Ngưỡng auto-lọc = **HEAVY=5** (thang 1–6). GIỮ `setAutoCleanAirState`+`enablePurificationFunctionPrompt` best-effort (trim TỪ CHỐI tắt popup, biết từ v1.33). **[ĐO]** full suite **1983/0** (5 module ×2), senior review APPROVED (patch [P2] race concurrency + [P3] a11y EN). `apk/ClusterNav-1.38-release.apk` (sha256 `4f8b581d…`, bỏ 1.37 giữ 1.0), commit `Đăng Khôi <dangkhoi@…>` → push feat + fast-forward main → OTA. **CHỜ TEST XE:** bấm "Lọc ngay" lọc thật? · auto khi bụi cao tự lọc (không chỉ popup)? Files: `comfort/Pm25FilterApplier.kt` · `MainActivity.kt` · `BilingualLabels.kt` · `res/layout[-w960dp]/activity_main.xml` · `L2CockpitUiWiringContractTest.kt` · `ExpansionTransportFenceTest.kt`(seal) · spec `pm25-auto-filter.html`(journal).

- **2026-09-08 · SHIP v1.37 OTA (versionCode 38) — sửa auto-start VietMap silent (bóng KHÔNG lên) + tách FGS riêng.** Owner báo on-car v1.36: bật bóng VietMap chạy silent thì bóng không lên — phải mở VietMap bằng tay, đợi boot **vào map**, hạ xuống mới lên. Gốc [SUY]: nhánh silent-bg cũ = `launch → Thread.sleep(1500) CỨNG → hạ nền`; 1.5s quá ngắn cho cold start Flutter + map SDK + `VMBluetoothService` (service dựng bóng — runbook §10), mạng chậm càng hụt ⇒ hạ nền trước khi vào map ⇒ chưa dựng bóng. **Sửa:** (1) `VietMapAutostart.pollUntilInMap(sh)` — poll `dumpsys …ResumedActivity` mỗi `POLL_INTERVAL_MS`=500ms tới khi VietMap resumed **liên tục ≥ `SETTLE_MS`=2500ms** (đã vào map ổn định) rồi mới hạ nền; `POLL_TIMEOUT_MS`=25000ms chặn trên (thoát sớm khi mạng nhanh); PURE `isResumedActivity()` test off-car. (2) Tách auto-start ra **FGS riêng `VietMapAutostartService`** (single-worker guard: `workerActive` CAS + `latestStartId`, nhả cờ TRƯỚC `finish()` chống race teardown) — `BootSetupService` bỏ `runNow` ĐỒNG BỘ → `startForBoot`; ghế/lọc bụi/nav/voice-key KHÔNG còn đợi VietMap (owner: "tách riêng cái vietmap ra"). MainActivity toggle bóng + mở app + `BadgePlacementController` → `startForAppOpen`. (3) Xoá `ensureRunning` (dead). GIỮ NGUYÊN chống-loop (in-flight + cooldown 30s) + guard foreground + `hasActivityRecord` + nhánh cast-default + badge-only. **[ĐO]** full suite **1981/0** (5 module, `--rerun-tasks`), `:app:assembleRelease` sạch, senior review APPROVED (1 [P2] race FGS đã patch). KHÔNG đụng layout. `apk/ClusterNav-1.37-release.apk` (sha256 `bfd23877…`, bỏ 1.36 giữ 1.0), commit `Đăng Khôi <dangkhoi@…>` → push feat + fast-forward main → OTA. **✅ XE XÁC NHẬN (2026-09-08): CHẠY NGON** — bóng lên khi bật silent, không cần mở VietMap tay. Files: `VietMapAutostart.kt` · `VietMapAutostartService.kt`(mới) · `AndroidManifest.xml` · `BootSetupService.kt` · `MainActivity.kt` · `modules/clustercast/BadgePlacementController.kt` · `VietMapAutostartGateTest.kt` · `VietMapAutostartServiceWiringTest.kt`(mới).

- **2026-09-06 · SHIP v1.36 OTA (versionCode 37)** — nới spacing dọc bị chật sau v1.34 (owner báo trên xe: "padding/margin/line-height dính dính vào nhau"). v1.34 nén MỌI metric dọc ~70% mà text full-size → khe hẹp. Nay nâng padding/margin/gap dọc về ~90% gốc v1.32 (`gap` 10→13, `touch_min` 40→46, các padding row +2~3dp) + thêm **`lineSpacingMultiplier 1.2`** cho text nhiều dòng (`Cockpit.Text.Hint` + `Cockpit.Row.Subtitle`). Text giữ v1.32, KHÔNG đổi id (parity 87), narrow seal re-pin **lần 26** (`0678a33d…`). **[ĐO emulator]** spacing thở đều, hết dính, text nhiều dòng có leading. Full test **1957/0** (5 module). `apk/ClusterNav-1.36-release.apk` (sha256 `1ce32ff4…`, bỏ 1.35 giữ 1.0). Commit `Đăng Khôi <dangkhoi@…>` → OTA. **CHỜ TEST XE**: spacing trên cụm thật.

- **2026-09-06 · SHIP v1.35 OTA (versionCode 36)** — whitelist VietMap mod vào `byd_float_app_list` để bóng trên cụm hết bị "IVI không hỗ trợ": `VietMapAutostart.runNow` (gate **bóng bật** + cờ `vmFloatWhitelistApplied` **một-lần**) append `vn.vietmap.live` vào global CSV `byd_float_app_list` + `appops set vn.vietmap.live SYSTEM_ALERT_WINDOW allow` qua dadb uid-shell (CÙNG công thức AssistantLauncher, tái dùng `FloatAppList.merge` :core — KHÔNG clobber gói khác). Degrade-safe (fail KHÔNG chặn autostart, KHÔNG set cờ → thử lại lần sau). Full test **1957/0** (5 module), KHÔNG đụng layout (seal lần 25 giữ). `apk/ClusterNav-1.35-release.apk` (sha256 `ea3fddbb…`, bỏ 1.34 giữ 1.0). Commit `Đăng Khôi <dangkhoi@…>` → OTA. **CHỜ TEST XE**: bóng VietMap lên cụm + hết toast "IVI không hỗ trợ".

- **2026-09-06 · SHIP v1.34 OTA (versionCode 35)** — sửa 3 lỗi on-car v1.33: (1) **ghế mức OFF** — thêm `SeatComfortApplier.applySeat(seatIndex, level)` ghi `state=stateForLevel(level)` (0→1 Tắt, KHÔNG bỏ qua) qua `BydHal.callNamedInt(SETTING, methodFor(mode), seatId, state)`, wire vào `onSeatLevelChanged` sơ đồ ghế (mát mức 1/2 đã [ĐO] rc=0 trên xe; giờ Tắt cũng gửi state=1). (2) **Text về gốc, chỉ giảm chiều cao** — v1.33 thu cả text khó đọc; trả textSize/horizontal về v1.32, chỉ giảm ~70% CHIỀU CAO (card height, padding/margin dọc, custom-view height) cả 2 layout; narrow seal re-pin **lần 25** (`046d23b0…`), parity 87. (3) **VietMap loop** — thêm `hasActivityRecord(dumpsys)`: nhánh bóng CHỈ launch khi VietMap CHƯA có activity record (đã mở → skip), thay check RESUMED cũ (vô dụng vì ClusterNav mới là resumed app); giữ cooldown 30s + inFlight. Full test **1940/0** (5 module), release không debuggable + không probe. `apk/ClusterNav-1.34-release.apk` (sha256 `4bfd4705…`, bỏ 1.33 giữ 1.0). Commit `Đăng Khôi <dangkhoi@…>` → OTA. **CHỜ TEST XE**: ghế OFF · UI (text gốc + thấp hơn) · VietMap không relaunch khi đã mở. **Launcher**: RE launcher tham chiếu Dudu (`docs/diagnostics/dudu-launcher-hal-RE-2026-09-06.md`) — NOTE để sau (owner cài Dudu chơi thử 1 tuần).

- **2026-09-06 · SHIP v1.33 OTA (versionCode 34)** — sửa 4 lỗi on-car v1.32 + 2 hạ tầng. (1) **Ghế mát/sưởi**: đổi từ AC + raw `0x43101010` (trả NOT_PROVISIONED trên xe) sang **`BYDAutoSettingDevice.setSeatVentilatingState/setSeatHeatingState`** (seatID **1-based** 1..4, state **1/2/3** = tắt/mức1/mức2, qua `BydHal.callNamedInt`); bỏ hẳn đường AC+raw-id+HAL_VALUE. (2) **Loop autostart VietMap**: `VietMapAutostart` thêm cooldown 30s + in-flight (`tryBeginRun/finishRun`) + skip-nếu-foreground + MainActivity gate `savedInstanceState==null` (không chạy khi recreate đổi theme/ngôn ngữ). (3) **Hero nav trống** = lỗi WIRE UI (tín hiệu đã OK): `hero_road←NavRepository.state.road`, `hero_nav_icon←heroArrowRes(state.maneuver)` (thêm `navigation/HeroArrow.kt` :core + `Maneuver.toHeroArrow`), `hero_dist` gate `active`. (4) **PM2.5 popup**: `enablePurificationFunctionPrompt` best-effort (log, KHÔNG chặn `setAutoCleanAirState` đang chạy rc=0). + **UI thu ~70%** (styles/dimens/custom-view, CẢ 2 layout; narrow seal re-pin **lần 24** `1a7c90f7…`, parity 87 giữ) + **fix card 2-line** (bỏ layer top-highlight ở `card_bg`/`group_bg`/`hero_card_bg`). **Hạ tầng mới**: core điều khiển **cửa sổ/cốp** (`body/Bodywork` + `body/BodyworkControl` qua `BYDAutoBodyworkDevice.setBodyWindowCtrlState` window 1-4/state 0-1 + `setHetchDoorStatus`, CHƯA UI) + **bộ on-car HAL probe** (`HalProbeReceiver`/`HalProbePresets`, **vehicleTest-only**, release loại trừ, bắn HAL/preset/raw-fid qua `adb am broadcast` — doc `docs/diagnostics/oncar-hal-probe.md`). Full test **1911/0** (5 module), seal lần 24, parity 87, release không debuggable + không bề mặt test (aapt2). `apk/ClusterNav-1.33-release.apk` (sha256 `396e5969…`, bỏ 1.32 giữ 1.0). Commit author `Đăng Khôi <dangkhoi@users.noreply.github.com>` → OTA. **CHỜ TEST XE**: ghế (SETTING device) · loop VietMap · hero nav (street+mũi tên) · PM2.5 popup · UI 70%/card trên cụm thật. Cửa/cốp CHƯA UI (core), test qua bản vehicleTest + probe.

- **2026-09-06 · SHIP v1.32 OTA (versionCode 33)** — phiên UI lớn: 3 tính năng (ghế mát/sưởi tự động qua HAL AC · tự lọc bụi PM2.5 · chỉ báo trạng thái phím-thoại) + đại tu giao diện **Level-2 "cockpit"** (design system `Cockpit.*` khớp mockup `docs/specs/ui-visual-upgrade-l2.html`: hero 3 thẻ + board 2 cột grouped-row + custom view SeatDiagram/SpeedDial/Pm25Gauge/SegmentedControl/ClusterPreview) + **song ngữ VI/EN** (`Lang.t` + `BilingualLabels.localizeTree` id-free tree-walk + selector `seg_language` Theo-xe/VI/EN) + **light mode** (`values/` sáng + `values-night/` tối cùng bộ tên màu, `ThemeMode` ép uiMode qua `attachBaseContext` 5 activity, selector `seg_theme` Theo-xe/Sáng/Tối, 5 custom view đọc `@color`) + hero km/h THẬT (`SpeedProvider` HAL) + fix autostart bóng VietMap. Seal T11 narrow **lần 23** (`dfd93e64…`), parity **87** id cả 2 layout, `strings.xml` không đổi. Full test **1894/0** (5 module), senior review APPROVED (0 finding), release APK không debuggable/không bề mặt test. `apk/ClusterNav-1.32-release.apk` (sha256 `613798b6…`, bỏ 1.31 giữ 1.0). Commit author dangkhoi → merge `main` → OTA. **CHỜ TEST XE**: HAL ghế (values 2/3 + Han rear ids) · PM2.5 reflection · bóng autostart · phím-thoại sau reboot · UI mới (light/dark + song ngữ) trên cụm thật.

## 5.old. Trạng thái (2026-08-24)
- **Branch** `feat/speed-limit-badge-hal-hud` · **checkpoint `28d6843` (2026-08-26) đã COMMIT LOCAL** toàn bộ v1.16→v1.27 (57 file, scan CLEAN) — **chưa push** (chờ owner OK). Trước đó push xa nhất `40474af` (v1.13). **main = `f7843c0`** — không đụng khi chưa PASS on-car. Từ mốc này: **commit per-version** để bisect được.
- **APK đã giao owner**: v1.13 (mốc) → v1.14 (badge+phím thoại+gán nhiều phím) → **v1.15 code 16** `ClusterNav2.0-v1.15-hud-vietmap-waze-20260824.apk` ← **chờ owner test trên xe**.
- **Test [ĐO]**: `./gradlew test --rerun-tasks --continue` ⇒ **2126 bài, 0 lỗi** cả 5 module. `org.gradle.parallel=true` bật 08-24 ⇒ 127 s (trước 200 s). Bẫy E6 (test tự phá seal) ĐÃ VÁ — không cần `git checkout` seal nữa.
- **KIẾN TRÚC — owner duyệt 08-24** (`docs/specs/nav-input-output-architecture.html`): *nhiều cách nhận tín hiệu (noti · a11y · đọc màn hình) → **MỘT cửa vào tại một thời điểm** (`NavRepository.ingest`/`ingestContent` → `NavigationSessionCoordinator`) → các đầu ra (HUD kính lái · cụm giữa+ETA · dải làn · chip camera)*.
  - **Bước 1/3 XONG**: đường ảnh (VietMap/Waze) nay vào `ingestContent` — CÙNG cửa với notification ⇒ đi hết chuỗi chốt-phiên → cụm giữa → HUD. [ĐO] `BydHal.pushNavigation` mất hết call site; `writeNavFrame` còn **đúng 1** chủ (`NavigationHudOwner`) ⇒ đóng B3.54.
  - **Còn nợ (F7)**: dải làn + chip camera vẫn đi thẳng từ `NavOutputOwner`; sau đó gỡ file đó. Hai kênh này ghi thanh ghi RIÊNG nên KHÔNG gây lỗi — dọn kiến trúc thuần, owner hoãn tới sau khi v1.15 xác nhận trên xe.
- **CHƯA on-car**: toàn bộ B3.40→B3.56 + F1→F4 mới off-car/emulator. `CLAUDE.md §14` tầng 1 chưa xanh.
- **OPEN**: (a) v1.15 chờ đo trên xe — VietMap dẫn thì HUD kính lái có mũi tên+cự ly không; (b) vòng xuyến generic = OEM-render (C2); (c) HUD kính ZIN = gate firmware `0x38B00030` chưa bác (D6); (d) F7 dọn kiến trúc; (e) B3.52 phủ sóng glyph VietMap **63/87** ra được mã (08-25: +6 template fixture-runtime, 39→63; còn 63→87 cần gộp-đảo/hạ-ngưỡng-mực = chạm locator, để lại); (f) D4 scrub secret cũ khỏi git history (cần owner duyệt force-push).
- **08-25 off-car-B batch**: B3.50 (guard clock-skew SourceArbiter) ✅ · B3.52 (phủ sóng VietMap 39→63) 🔧 · B3.34 (Waze navBarDirection→ManeuverRegistry) ❌ BÁC BỎ bằng GPS-routing thật (crop navBarDirection lệch ManeuverRegistry 25–29 bit, NCC sai hướng) → đường đúng = thu WAZE_INK bbox-mực (method proven, left/right phủ, rest incremental) · B3.20 (chip cảnh báo/camera VietMap trên cụm) ✅ IMPLEMENT off-car (RoadAlertChipDecision :core + AlertChipView + overlay cửa sổ thứ 3 + toggle default OFF, senior review APPROVED; render owner-visual + hash camera thu-1-lần còn lại). Full suite **2184/0**. **APK v1.22** (code 23) `ClusterNav2.0-v1.22-alertchip-vmcoverage-clockskew-20260825.apk` sha256 `2be80c8e7ab3453e4a4e09f8c1983a88a2a5933b43175afd7c58509689619057`.
- **08-25 off-car-B batch 2**: **B3.34 thu WAZE_INK** — 26 pass GPS-mock (HN+HCMC+nhắm vòng-xuyến) → +3 maneuver Waze (quay-đầu/đi-thẳng/tới-đích); WAZE_INK 4→7; vòng-xuyến Waze bản 5.21 hiện bằng mũi-tên-hướng-ra (đã phủ), không ra glyph riêng ⇒ bão hoà. **B3.56 (P1 an toàn) XONG** — `NavOutputOwner.releaseStaleOwnerIfChanged`: khung đổi chủ sang gói KHÔNG-mũi-tên → nhả phiên chủ cũ (chống mũi tên stale kẹt sai-hướng qua HUD keep-alive 180s); senior review APPROVED, guard `!pushArrow` giữ arrow-handoff. Full suite **2188/0**. **APK v1.25** (code 26) `ClusterNav2.0-v1.25-b356-safety-waze-20260825.apk` sha256 `0345914d3bf9ac095d5b44acb1deb2c304844d8615997533a163df154242408e`.
- **08-25 off-car-B batch 3 (owner "dọn hết off-car")**: **E5 XONG** (13 doc mồ côi → 0; liệt kê file con dưới bullet đại diện trong `docs/README.md`, KHÔNG đụng file seal). **F6 XONG** (chống treo đường nền adb) — preset `LocalShellRetry.BACKGROUND_READ_CAP` (1 lần thử + hạn đọc 30s, không retry) opt-in 4 đường nền `VietMapAutostart`/`NavConnect`/`ClusterDiag` (qua `session(retry)`) + `UpdateChecker` (qua `installApk(socketTimeoutMs)`); mặc định NONE=bit-identical; sửa `VoiceKeyAdbApprovalWiringTest` (chốt: đường nền dùng CAP nhưng cấm retry-looping AWAIT); senior review APPROVED. **B3.55 điều tra→📋 by-design** (180s trần tuổi có [ĐO] on-car gap hầm 108s; hạ = regress anti-blank; 3/4 trigger STOP đã phủ B3.48/49/56; còn lại cần probe process-death nặng). **B3.52 điều tra→🔨 effort riêng** (63/87 [ĐO]; 24 khung hụt ~12 đúng-khi-im-lặng; 63→87 cần island-merge + sinh lại 40 template, ROI thấp vì 8/10 là arrive/depart ảnh-gương dễ bị lọc ≥37-bit). Full suite **2191/0**. **APK v1.26** (code 27) `ClusterNav2.0-v1.26-f6-hangcap-20260825.apk` sha256 `79f6327423a6a4922a50ee306dc5bb29f7671ad44f0ddafb939a3a4e8b0bd674`.
- **08-25 owner báo on-car 2 lỗi → sửa off-car (sub-agent DAG + senior review APPROVED + tự verify)**: **B3.57** status "Chưa có phiên dẫn đường" chỉ đúng GMaps → `NavSourceLabels` (:core, phân kênh đọc theo roster: GMaps=thông báo · VietMap/Waze=đọc màn hình) + MainActivity source-aware + reword `status_need_perm`. **B3.58** VietMap chiếu-lên-cụm không lên HUD: [ĐO] gốc `VietMapDescParser` KHÔNG trích hướng rẽ (chỉ cự-ly/đường/ETA) ⇒ hướng CHỈ qua pixel mũi tên. (2a-text) `NavWindowPicker.rank` display-agnostic (getWindowsOnAllDisplays) → VietMap trên cụm displayId=1 vẫn đọc a11y text; (2b-arrow) `captureDisplayForCase(CLUSTER_CAST)` chụp đúng display cụm — **pixel chụp display phụ CAR-DEPENDENT** (B3.26 emulator không chụp được). T11 seal re-pin `strings.xml` 4b068200→8300437c (KDoc lần 4, owner duyệt). Full suite **2198/0**. **APK v1.27** (code 28) `ClusterNav2.0-v1.27-status-vietmap-cluster-20260825.apk` sha256 `fc95f6d2ec65dd4ed2fd89759b6b204053b69c7acf801166151eae810d4b439a`.
- **08-26 commit checkpoint + bản DIAG lấy log**: commit local `28d6843` toàn bộ v1.16→v1.27 (giữ history; từ đây commit per-version). **Bản DIAG v1.28** (owner: log ra sdcard + bật sẵn + hướng dẫn anh em): cờ gradle `-PdiagLog=true` → `BuildConfig.DIAG_LOG` → `Prefs.navVerboseLog` default theo cờ (release thường VẪN OFF = giữ A8/D3) + `NavLogExport` copy log ra `/sdcard/Download/ClusterNavLog` qua dadb shell (khi tắt công tắc log / broadcast `EXPORT_LOGS`) — KHÔNG đụng layout seal (dùng `switch_diag_logging` có sẵn). Hướng dẫn `docs/HUONG-DAN-LAY-LOG-DIAG.md`. Senior review APPROVED (release proven default-OFF). Full suite **2210/0**. 2 APK v1.28 (code 29): **DIAG** `080a2512…` (verbose ON, cho anh em) + release thường `4bde8453…` (verbose OFF, cho xe). Mục đích: thu content-desc VietMap/Waze + ảnh mũi tên để tìm thông tin hướng rẽ còn thiếu (B3.58/REG-B). **REG-A/B/C (regression on-car v1.26) chưa fix — chờ bisect** (bản GOOD gần nhất hoặc APK bisect).

- **08-28 phiên CLOSING (off-car/emulator, UNCOMMITTED toàn bộ)** — chốt sản phẩm gọn: **GMaps noti→cụm+HUD** (đường proven) + **VietMap widget→badge tốc-độ** + **VietMap-mod bong-bóng→cụm (chỉnh được vị trí)** + **phím-thoại→trợ lý**:
  - **Gỡ HẾT nav screen-capture/a11y VietMap+Waze + chẩn đoán của chúng** (~39 file: `NavOutputOwner`/`ScreenCaptureNavSource`/`OffscreenMirrorCapturer`/`NavViewIdSource`/`VietMapDescParser`/`WazeArrowRegistry`/`NavGlyphLocator`/`NavWindowPump`/`mirror/*` + log `NavArrowLog`/`NavAccessLog`/`SegmentShotCapturer`… + test). GIỮ: GMaps noti (`NavRepository.ingest`), VietMap widget (`vietmapwidget/*`→badge), voice-key. `NavApps.ALL=GMAPS`, `DESC_ONLY=∅`, XML a11y = 2 gói GMaps. Gỡ selector nguồn + toggle diag (giữ `txt_nav_source_active` read-only).
  - **Item 4 — chỉnh VỊ TRÍ bong bóng VietMap trên cụm**: mod thêm receiver `VMBluetoothService$posrx` (broadcast `com.byd.clusternav.VM_BUBBLE_POS`) + **fix mapping 1-1** (gravity TOP\|LEFT 0x33 + FLAG_LAYOUT_NO_LIMITS 0x200 qua `b.q()` trước `b.E(x,y)`). ClusterNav: `VmOverlayPosition.kt` (prefs `vm_bubble_x/y` px tuyệt-đối góc-trên-trái, `send()` gate `castOn()`, `applyOnOpen/presetRightHalf/setAbsoluteTopLeft`), UI kéo-thả `VmBubblePlacementView.kt`, áp lại 3 nơi (onResume + nút Áp dụng + refresh loop `FloatingBubbleService`). Runbook đầy đủ: `docs/runbook-mod-vietmap-cluster.md` §10. **[ĐO] mapping 1-1 verified** emulator-5556.
  - **2 toggle behavior**: badge tốc-độ **default TẮT** (`badgeEnabled` false); thêm toggle bong bóng `vm_bubble_enabled` **default TẮT** + `switch_vm_bubble_enabled` cả 2 layout. Bật ON **lưu bền** (SharedPreferences, giữ ON các lần sau). Auto-start VietMap gate = **badge HOẶC bong bóng** (`VietMapAutostart.runNow`), dedup sẵn `pidof` ⇒ bật cả 2 chỉ start 1 lần; thêm `ensureRunning` vào cả 2 handler (chọn-là-start).
  - **[ĐO] auto-start trên emulator KHÔNG chạy**: dadb loopback `localhost:5555` không có trên emulator (ECONNREFUSED; `setprop`/`tcpip` không bật được) ⇒ auto-start (dùng dadb pidof+monkey+trả foreground) là **op phía XE** (chạy khi trên xe). Đã thử + **revert** fallback `startActivity` không-dadb (bị chặn background-activity-start Android 10 + UX xấu). Đúng nguyên tắc: op dadb/shell = xe.
  - **Seal T11 re-pin lần 9** (portrait `activity_main.xml` `7d283550…`→`6369e497…`; wide không pin; strings.xml không đổi). Full test `--rerun-tasks --continue` **5 module 0 fail** (core 786 · car-integration 44 · offcar-planner 99 · app 895).
  - **APK giao owner** ở `~/Desktop/ClusterNav-oncar-test/`: **ClusterNav2.0-v1.29-release** (code 30, bump từ 1.28/29) + **VietMap-3.4.0-mod-cluster** (universal-mod3, receiver posrx + fix mapping, ký `mod2.keystore`). **CHỜ test trên xe** rồi mới commit (chưa commit, chưa scan).

## 6. Cách làm việc — owner chốt 2026-08-24
- **Danh tính commit (dự án CÁ NHÂN — owner chốt 2026-09-06):** commit/push để NGUYÊN git config repo = **`Đăng Khôi <dangkhoi@users.noreply.github.com>`** (repo cá nhân GitHub `dangkhoi`). **KHÔNG** override bằng danh tính công việc `dangkhoi <dangkhoi@users.noreply.github.com>`; KHÔNG thêm `-c user.name`/`-c user.email`/`--author` dangkhoi. Rule global `workflow.md §0` (dangkhoi) CHỈ cho repo công việc — repo này override. (Commit `28d6843…20b77dc` lỡ mang dangkhoi; `20b77dc` đã amend về dangkhoi 2026-09-06.)
- **Báo cáo bằng ngôn ngữ người dùng**, KHÔNG trộn thuật ngữ/tên file/mã việc vào câu văn (owner: *"viết kiểu 50/50 thế này dọc không hiểu gì cả"*).
- **Tài liệu CHỈ lưu local** — cấm publish claude.ai artifact / dịch vụ ngoài, kể cả private.
- Nhiều agent ⇒ chạy **TUẦN TỰ**, không song song trên cùng thư mục dự án (tranh khoá gradle).
- Agent chạy test: lọc lớp lẻ (`--tests`) lúc lặp; chỉ agent cuối chạy đủ 5 module.
