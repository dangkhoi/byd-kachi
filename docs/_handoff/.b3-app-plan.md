# B3 :app capture/transport — build plan (scratch)

WORKING DIR: byd-cluster-2-wt-speed-limit-badge-hal-hud (git worktree — NOT byd-cluster-2)
JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home
GATE: ./gradlew :app:testDebugUnitTest :app:assembleDebug --console=plain  (GREEN)
Constraints: NO commit/push/main/keystore/docs/README.md edits (spec HTML IS allowed per deliverable 5).

## :core API (done, locked) — call, don't modify
pkg com.byd.clusternav.navigation.screencapture:
 CaptureCase{FULL_MAIN,HALF_MAIN_SPLIT,CLUSTER_CAST,NOT_ACTIVE}; CaptureTarget{ARROW,CAMERA}.forPackage(pkg)
 BoundsSource{A11Y_DYNAMIC,FIXED_CALIBRATED,NONE}; CropRect(l,t,r,b){width,height,isEmpty,offsetX,clampTo,EMPTY}
 DisplayGeometry(w,h,mainId=0,clusterId=1){fullRect}; AppLocation(pkg,displayId,isFullscreen,slotSide?,leftPercent=50,foreground=true,navFresh=true)
 CaptureBounds(rect,capturedAtMs); CapturePlan(case,target,bounds,boundsSource)
 CaptureRouter.route(loc,geom,a11y?,now,freshMs)->CapturePlan?(null=gate closed); selectCase; computeBounds; A11Y_FRESH_MS=1500
 CaptureCalibration.{WAZE_ARROW_OPENBYD,VIETMAP_CAMERA_SEED,fixedBounds}
 PixelFrameOps.crop(PixelFrame,CropRect):PixelFrame?
 VietMapCameraMatcher(templates,minScore).match(frame):CameraMatch; CameraMatch{hasCamera,score,templateName,NONE}; CameraTemplate; BUILTIN=empty; templateFrom; signatureOf; GRID=15; DEFAULT_MIN_SCORE=.62
pkg navigation: PixelFrame{width,height,argb()}; ArrayPixelFrame; ManeuverSignature.classify/classifyManeuver(PixelFrame?); Maneuver.toAmapIcon/toHudIcon
 SourceArbiter.shouldFeed(pkg,mode,now,channel=DATA)/isDataFresh/isFresh/activeSource/clear; NavChannel{DATA,IMAGE}; STALE_MS=6000
clustercast.simplified: ClusterSlotSide{LEFT,RIGHT}; CastStackParser; SimpleCastRuntime.coordinator(ctx).executeShell(cmd):ShellResult{success,stdout,exitCode}
app: Prefs.enabled(ctx)=master Nav+HUD (default false); Prefs.sourceMode(ctx); NavLog.verbose; DiagStorageCap.enforce(ctx); asPixelFrame(); NavAccessibilitySource.foreground(now)
Transport proven (SegmentShotCapturer): SimpleCastRuntime.coordinator(ctx).executeShell("fission_screencap -d N -p PATH"); fission -d0=CLUSTER, -d1=MAIN (opposite Android ids).

## New files (app: com.byd.clusternav.screencapture) — [x]=done
[ ] CaptureBoundsSource.kt      : object CaptureBoundsSource (a11y bounds holder) + object CaptureForegroundSource (fg pkg holder). PURE → JVM test
[ ] CaptureBoundsHeuristic.kt   : pure pick(target,candidates,region):CropRect? — arrow/camera node selection. JVM test. node-id=VERIFY-ON-CAR
[ ] CaptureLocationResolver.kt  : pure resolve(amOut,pkg,navFresh,fgHint,ids):AppLocation from am stack bounds. JVM test. VERIFY-ON-CAR calibration
[ ] ScreenCaptureSignal.kt      : object holder for classified arrow/camera result (seam T7 consumes). PURE → JVM test
[ ] ScreenCaptureTransport.kt   : Android — reuse fission_screencap dadb → Bitmap. cases 1/2/3
[ ] OffscreenMirrorCapturer.kt  : Android SCAFFOLD — MediaProjectionHolder + AUTO_MIRROR VD + ImageReader. case4. degrade-safe VERIFY-ON-CAR
[ ] CaptureRequestActivity.kt   : non-exported MediaProjection consent activity (R-nf6). scaffold, not auto-started
[ ] ScreenCaptureNavSource.kt   : orchestrator singleton. single-thread scheduled exec, single-in-flight, ~2Hz, gate, runCatching, verbose diag save

## Edits
[ ] NavAccessibilityService.kt  : publish fg pkg (all navPkgs) + best-effort arrow/camera bounds (throttled, additive, degrade-safe)
[ ] NavNotificationListener.kt  : start ScreenCaptureNavSource on connect(enabled), stop on disconnect/destroy (symmetric to WazeHudSource)
[ ] AndroidManifest.xml         : <activity .screencapture.CaptureRequestActivity exported=false translucent>

## Tests (app/src/test) JVM JUnit5
[ ] CaptureLocationResolverTest, CaptureBoundsHeuristicTest, ScreenCaptureSignalTest/CaptureBoundsSourceTest (pure logic)
[ ] ScreenCaptureNavSourceContractTest (SourceRoots structural: gate, single-in-flight, runCatching, off-thread, IMAGE channel, verbose diag)
[ ] NavAccess/NavListener wiring structural tests

## Spec: docs/specs/waze-vietmap-screen-capture.html §5 Tasks status + §9 Impl Log (Pass 2). VERIFY-ON-CAR honest.
## Handoff: docs/_handoff/stage-b3-app-done.md
