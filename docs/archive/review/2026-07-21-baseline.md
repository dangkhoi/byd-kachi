# Baseline kiến trúc + user story (principal engineer pass)

# ClusterNav — BASELINE MAP FOR REVIEW TEAM
**Author:** principal-engineer baseline pass · **Date:** 2026-07-21 · **Tree:** `v035-size-overscan-fallback`, working tree = **v0.42 uncommitted** (HEAD commit = v0.35; `git status` shows 17 modified + `CastShell.kt`/`CLAUDE.md` untracked)

> **Evidence grading used throughout, per CLAUDE.md §2:**
> **[P]ROVEN** = read from this repo's source, from a real on-car dump in `docs/diagnostics/`, or fetched verbatim from AOSP.
> **[L]IKELY** = inference that fits the observed phenomenon, no direct datum yet; a closing observation is named.
> **[H]YPOTHESIS** = merely plausible, explicitly unverified.

---

## 0. VERSION REALITY CHECK (do this before anything else)

| Fact | Value | Source |
|---|---|---|
| Field car ran | versionCode=36 / versionName=0.36, installed 2026-07-21 18:14 | `carlog-2026-07-21/00-state.txt:7-10` **[P]** |
| Working tree is | versionCode=42 / versionName="0.42" | `app/build.gradle.kts:22-23` **[P]** |
| Git HEAD is | versionCode=35 | `git show HEAD:app/build.gradle.kts:22` **[P]** |
| APK in repo | `apk/ClusterNav-0.42-release.apk` (untracked dir) | `git status` **[P]** |

**Consequence for every other reviewer:** the only on-car dump we have (`carlog-2026-07-21/`) describes **v0.36**. Six version-steps of code (0.37→0.42) — including `evictVd`, `autoCastPkg`, `scaled()`, the `am stack remove` removal, `setDensityIfNeeded`, `migrateDriftedRects` — have **never been observed on a car**. Do not describe any v0.37+ behaviour as "fixed in the field". The spec `docs/specs/cluster-cast-v036.html` documents v0.36 only and is now **six versions stale**; its §Tasks table (T1–T19) has no rows for anything after v0.36. **[P]**

---

## 1. THE USER STORIES (owner's terms)

Owner: Đăng Khôi (dangkhoi), Vietnamese, drives a **BYD Seal (DiLink3, Android 10)**. His group ("anh em trong hội") runs **SL6 / Han**, and a third-party launcher ("Dudu"). Cluster = an OEM **virtual display** owned by `com.xdja.containerservice`, `1920×720`, **`touch NONE`** (`05-display.txt`, Display Devices block) **[P]** — so everything on the cluster is *to look at*, never to touch.

### S1 — "Chiếu một app lên cụm" (cast an app to the cluster)
> *"Mở app + đưa về trạng thái cần xem ở màn giữa TRƯỚC (vd Maps: chọn tuyến), rồi CHIẾU. Cụm không cảm ứng nên chỉ để NGÓ."* — `ClusterCastActivity.kt:61`

**Call path (cold cast):**
`ClusterCastActivity.kt:66-69` (CHIẾU LÊN CỤM) **or** `:332-335` (CHIẾU APP NÀY) **or** `FloatingBubbleService.kt:206→138-141` (bubble tile) **or** `MainActivity.kt:101-113` (card 3) **or** `ClusterCast.autoCastOnBoot:868-878`
→ `ClusterCast.cast(ctx,pkg,log)` `:259`
→ `busy.compareAndSet` `:260` → `vdExec` single-thread `:262` → `dadb.Dadb.create("localhost",5555)` `:267`
→ `isInstalled` `:270` / `:921`
→ `restorePip` `:275` (undo a *previous* session's app-op leak — idempotent, first thing done)
→ `StackParse.parse(am stack list)` + `pick` `:277-278`; app not running → `resolveComp` + `am start -n` + `sleep(2500)` `:280-283`
→ PIP-window cleanup `:286-310` (`am start --windowingMode 1`, then re-check; **never** `am stack remove` since v0.42 `:302-306`)
→ `blockPip` `:314` → `cmd appops set … PICTURE_IN_PICTURE ignore` (`CastShell.pipCmd:104`)
→ `ClusterProfile.resolve` `:373`; **`svcName = prof.svcName` `:374`**; `activeTeardown = prof.teardownSeq` `:375`
→ read+zero animation `:379-380`; `clusterMutated = true` `:381` (arms rollback *before* first cluster mutation — correct ordering)
→ cast sequence `:384-387` — Seal = `[30,16,35]` via `sc(n)` `:239` = `service call AutoContainer 2 i32 1000 i32 <n> s16 ""`; `30` is rewritten per-app to `31` by `profileCmdFor` `:141/:385`
→ VD discovery loop, 16 × 500 ms `:390-394`, `DisplayParse.clusterDisplayId` `:89-100`
→ `lastDisplayId = vd` `:401` (set **before** placement so rollback knows what to clean — correct)
→ `placeAppOnVd` `:498`: `ensureFreeformSeed` `:500` → `evictVd` `:503` → `setDensityIfNeeded` `:507` → `placeLadder` `:508` → optional `escalateFreeform` `:515` → `applyBounds` `:518`
→ success: `setLastCastApp` + `setCasting(true)` `:404`, `autoDiag` `:405`
→ failure: `restoreFullscreenOnMain` `:412` + `rollback` `:413`

**Ladder** (`placeLadder:543-589`): **R1** `am start --display VD --windowingMode 5` (keeps session) → **R2** `am display move-stack <stack> VD` (does not go through `ActivityStarter`) → **R3** `am force-stop` + `am start --activity-clear-task` (**destroys the session**; skipped if `keepSessionApps` contains pkg `:579-582`). Each rung ends with `CastShell.landedOn` `:15-23` — bounded poll (2.5 s / 250 ms), excludes `pinned`.

**Head-unit state mutated by S1:**

| Mutation | Scope | Lives in | Undone by |
|---|---|---|---|
| `settings put global window_animation_scale 0`, `transition_animation_scale 0` | **GLOBAL, all apps** | `Settings.Global` (survives reboot) | `stop():471`, `rollback():487`, `reconcileOnStart():831` |
| `settings put global enable_freeform_support 1` / `development_enable_freeform_windows_support 1` / `force_resizable_activities 1` | **GLOBAL, all apps, effective next boot** | `Settings.Global` | **NOTHING — no undo path exists** (`CastShell.kt:43-44` admits it) |
| `cmd appops set <pkg> PICTURE_IN_PICTURE ignore` | per-app, system-wide | appops persisted state | `restorePip():910` (durable marker `pipBlocked`+`pipPrevMode` written **before** mutation `:903` ✔) |
| `service call AutoContainer 2 … 30/31/16/35` | cluster hardware | XDJA container | `activeTeardown` `[18,0]` in `stop():469` |
| `wm density <dpi> -d VD` | VD | **`/data/system/display_settings.xml` keyed by `uniqueId`** — survives reboot | `resetDisplayAll` `CastShell.kt:180-183` |
| `wm overscan l,t,r,b -d VD` | VD | same file | same |
| `wm size WxH -d VD` | VD | same file | same |
| `am display move-stack` / `am task resize` / `am force-stop` | task/stack | AM runtime | `restoreFullscreenOnMain`, `evictVd` |

**[P] AOSP:** `DisplayWindowSettings.java` (android-10.0.0_r47) writes `overscanLeft/Top/Right/Bottom`, `forcedWidth/forcedHeight/forcedDensity` into `new File(Environment.getDataDirectory(),"system")/display_settings.xml`, keyed by `displayInfo.uniqueId`. Our VD's uniqueId is `virtual:com.xdja.containerservice,1000,fission_bg_xdjaVirtualSurface,0` (`02-window-displays.txt:43`) — **stable across VD re-creation**, so a leaked `wm size` re-applies itself every time the container re-creates the cluster VD. This is exactly CLAUDE.md §5.

### S2 — "Chỉnh kích thước / DPI cho app đó" (adjust size & DPI)
`ClusterCastActivity.scalePanel:279-374`. Buttons: `100/90/80/70%` → `AppScale.scaled:99`; `Hẹp/Rộng/Thấp/Cao` → `nudgeRect:49`; `◀▲▼▶` → `nudgeMove:154`; `nhỏ/to` → `nudgeDpi:86`; `↺` → fresh `AppScale()`; `Kiểu ◠/▭` → `setRectProfile:137`.
Each press → `ClusterCast.setScale` `:147` (persist) → label refresh → `scheduleApplyLive:248` (350 ms debounce **tokened by pkg** `:252`).
→ `ClusterCast.applyScaleLive:698`: `busy` check `:705` → single-flight `scaleApplying` + `scaleDirty` re-loop `:709/:731-753` → VD by **ground truth**, not by flag `:725-726` → `StackParse.pick(..., preferDisplay=vd)` and **hard reject if `e.displayId != vd`** `:737-741` → re-adopt session `:744` → `setDensityIfNeeded` `:745` → `applyBounds` `:747` → read back real dpi `:750`.

**`applyBounds:606-657` — the three-tier sizing engine (the heart of the whole feature):**
- Guard `vd < 1` `:612` and `e.displayId != vd` `:613` — **enforcement-layer guard, exactly per CLAUDE.md §5** ✔
- **Tier 1** `am task resize` `:623`. **[P] AOSP `WindowConfiguration.java:795-797` (android-10.0.0_r47), verbatim:** `public boolean canResizeTask() { return mWindowingMode == WINDOWING_MODE_FREEFORM; }` (`WINDOWING_MODE_FREEFORM = 5`, `:167`). So on a fullscreen task this always throws — the code's claim is correct.
- **Tier 2** `wm overscan`, **verified by reading the real window frame back** `CastShell.overscanVerified:155-163` → `DisplayParse.appWindowFrame:73-83` (`mFrame=[l,t][r,b]`, **[P]** AOSP `WindowFrames.dump()` prints literally `"mFrame=" + rect.toShortString()`).
- **Tier 3** `wm size` + compensating density `CastShell.forceDisplaySize:129-146`, skipped when the requested rect is same-aspect as the VD `:646-648`, and self-verified by re-reading `cur=WxH` `:135-139` with a clean rollback if it did not take `:142-144`. **This ordering obeys CLAUDE.md §6** (new rung last, measures before climbing) ✔

### S3 — "Chuyển giữa các app chiếu" (switch cast apps)
`cast()` WARM path `:319-371`, taken when `casting==true` **and** the VD is still discoverable `:319-320`.
Sequence: evict every stack of `lastCastApp` on the VD `:324-326` → `restoreFullscreenOnMain(vd=-1)` `:328` (`-1` = *do not* reset the live cluster) → re-issue cluster style if it differs `:332-335` → re-issue `sc(16)` `:338` → **re-detect VD** because 16 may re-create it `:339` → `lastDisplayId = useVd` written unconditionally `:342` → `placeAppOnVd` `:343`.
On failure `:347-369`: the new app is put back fullscreen on the centre screen, **the old app is restored with non-destructive rungs only** (`:352-358` — never `force-stop` an app that is actively navigating), its dpi/frame re-applied `:362-364`, and `casting` is then recomputed **from `am stack list`, not from the flag** `:368`. This is the most carefully-written path in the file.

### S4 — "Tắt chiếu, lấy app về màn giữa" (stop; app returns to centre screen)
`ClusterCastActivity.kt:70-73` / `FloatingBubbleService.kt:170` / `MainActivity.kt:104-105` → `ClusterCast.stop:422`.
`vd = lastDisplayId`, and if the process was killed, **re-discover it** `:434-438` ✔ →
`StackParse.evictableOnVd(ents, vd)` `:445` — **the launcher-freeze fix**: only `displayId == vd`, only `isStandard`, never `pinned`, and `vd<1 ⇒ empty list` (`StackParse.kt:130-133`) → `untouchableOnVd` logged, not touched `:451-453` → every moved pkg *and* `lastCastApp` forced fullscreen `:456-457` → `restorePip` `:459` → `resetDisplayAll` `:461` → `activeTeardown` `[18,0]` `:467-469` → animation restored `:471` → `finally` always clears the flags `:475`.

### S5 — "Tự chiếu 1 app khi nổ máy" (auto-cast at engine start)
`ClusterCast.autoCastPkg` `:100`, set from `ClusterCastActivity.kt:307-315` (exactly one app, by design `:98`). Fired **only** from `RebindReceiver.kt:43-45` on `BOOT_COMPLETED` — deliberately **not** on `MY_PACKAGE_REPLACED` `:41-42` (installing an update mid-drive must not hijack the cluster). `autoCastOnBoot:868-878` sleeps `BOOT_CAST_DELAY_MS = 25 000` `:120`, re-checks `casting`, then calls `cast()`.

### S6 — "Nút nổi" (floating bubble)
`FloatingBubbleService` — foreground service, `TYPE_APPLICATION_OVERLAY`, idle alpha 0.35 → active 1.0 with a 2.5 s fade `:263-265`. **Tap = menu** (`:127` — long-press was removed as unusable while driving), drag = move + persist to `"bubble"` prefs `:128`. Menu (`:144-186`) = 2-column app grid + "⏹ Về màn chính" (`stop`) + "⚙ Cấu hình". State mirrored from `ClusterCast.onCastingChanged` `:57` / `ClusterCast.setCasting:114`.

### S7 — "GPS trong hầm" (dead-reckoning in tunnels)
`DeadReckonService` — foreground `type=location`, its own `HandlerThread`, never the main looper. States `REAL ↔ DEAD_RECKON ↔ COLD_SEED`.
- **REAL** `:271-312`: listens to real GPS (filters `isMock` `:121`), keeps `lastGoodBearing`, keeps a 6-fix ring for regression heading `:126-132/:469-479`, self-calibrates the steering ratio while GPS is good `:554-585`.
- **Enter DEAD_RECKON** `:285-292`: `lost` sustained 1.5 s **and** a hard veto if a fresh accurate fix exists `:277-281`, **and** a heading is known.
- **Enter COLD_SEED** `:293-312`: no fix this session, saved location valid, `speed < 2.0`, **`sats < 8`** — and the seeded position is then held **frozen** (`coldSeedStationary` `:310`, `dist = 0` `:319`).
- **Mocking** `:350-388`: `MockLoc.push` at 5 Hz (`loop:145` — 5 Hz only while mocking, else 1 Hz), accuracy inflated with seed age for COLD_SEED `:354`.
- **PEEK** `:379-387` / `:324-349`: every `PEEK_INTERVAL_DR_MS=20 000` (DR) or `PEEK_INTERVAL_COLD_MS=60 000` (cold) `:610-611`, `MockLoc.stop()` = `removeTestProvider`, wait 5–10 s `:612-613` for a real fix, then release or re-mock.
- **Failsafe** `MAX_DR_MS = 300 000` `:603`, applied to DEAD_RECKON but **deliberately not** to COLD_SEED `:365`.
- Position persisted every 5 s, **only when anchored to a real fix** `:434-442` → `LastLocationStore`.

State mutated: `addTestProvider(GPS_PROVIDER)` — **system-wide**, this is the single most dangerous mutation in the app. Undo paths: `onDestroy` `:244`, `RebindReceiver` on BOOT and on MY_PACKAGE_REPLACED `:37/:51`, `onCreate` orphan-sweep `:183`. Plus `appops set … android:mock_location allow` self-granted through dadb `MockLoc.selfGrant:114-142` (8 s → 10 min exponential backoff `:118`).

### S8 — "Lấy log mà không có WiFi" (diagnostics without WiFi)
Rationale, CLAUDE.md §11: plugging CarPlay/AA **turns the head unit's WiFi off**, so external adb is impossible exactly when data is needed — but the app runs *on* the head unit and reaches `localhost:5555` over loopback.
`DiagActivity` (`:25`) — read-only screen; `CHỤP CHẨN ĐOÁN` `:101-112` → `ClusterDiag.capture:36-76` (7 shell commands `:20-30`, writes `getExternalFilesDir()/diag/diag-<stamp>.txt`, keeps 20 `:81`) and prints a summary that directly answers "did the window shrink?" `:61-65`. `CHIA SẺ FILE` `:119-134` ships ≤120 000 chars as `EXTRA_TEXT` (no FileProvider, by explicit choice `:114-118`). Also auto-fires after every successful cast (`ClusterCast.autoDiag:852-861`).

### S9 — "Chia sẻ hồ sơ cụm với anh em" (share a cluster profile)
`ClusterProfile.export():35-38` → `id;diLink;W;H;cast;tear;hint;svc`, pasted back through `parse():92-109` with `sanitize():83-90` (strips NBSP/ZWSP/BOM, normalises en/em-dash and full-width `；`, takes the last line with 6–7 semicolons — because people copy the label line too). Validation: `id ≤32`, `diLink ∈ 1..9`, `W,H ∈ 1..8192`, sequence entries `∈ 0..255`, ≤16 entries, `svcName` against `^[A-Za-z0-9_]{1,32}$` `:70`. **This untrusted string ends up inside a `service call` shell command, and it is correctly validated** ✔ UI: `ClusterCastActivity.kt:115-142`.

---

## 2. ARCHITECTURE AS IT ACTUALLY IS

```
                     ┌──────────────── UI (display 0) ────────────────┐
 MainActivity:27 ─▶ ClusterCastActivity:32 ─▶ DiagActivity:25
 FloatingBubbleService:36 (overlay, TYPE_APPLICATION_OVERLAY)
                                  │  all call into ↓
        ┌─────────────────────────────────────────────────────────────┐
        │  ClusterCast (object, 924 LOC)  — ORCHESTRATOR + ALL STATE  │
        │  busy · vdExec(1 thread) · scaleApplying/scaleDirty          │
        │  prefs "clustercast"                                         │
        └───┬───────────────┬──────────────┬──────────────┬───────────┘
            │               │              │              │
     CastShell:10     StackParse:51   DisplayParse:7   AppScale:13   ClusterProfile:17
     (shell verbs)    (pure parser)   (pure parser)    (pure model)  (pure model)
            │
        dadb → localhost:5555 (uid 2000 shell)  ──▶ am / wm / cmd / service call / app_process64
                                                       └─▶ T3Daemon.java:34 (one-shot, reflection into IActivityTaskManager)

  INDEPENDENT SUBSYSTEM (does NOT touch the cluster):
  RebindReceiver:28 ─▶ DeadReckonService:37 ─▶ MockLoc:25 ─▶ LocationManager.addTestProvider
                                     └─▶ SpeedProvider / BydHal (reflection into BYD HAL)
                                     └─▶ LastLocationStore
```

### 2.1 Who owns which state

| State | Owner | Durability | Notes |
|---|---|---|---|
| `casting`, `lastDisplayId`, `lastClusterW/H`, `savedAnim`, `activeTeardown`, `svcName`, `CastShell.freeformSeeded` | `ClusterCast` **RAM only** | dies with process | `casting` is documented as display-only (`:104`, §5-compliant); `lastDisplayId`/`svcName`/`activeTeardown` are **decision inputs** and are *not* — see §3.2 |
| `castableApps`, `lastCastApp`, `appScales`, `t3Apps`, `keepSessionApps`, `rectProfileApps`, `autoCastPkg`, `pipBlockedPkg`, `pipPrevMode`, `insetH/V`, `clusterDpi`, `teardownProfile`, `keepKmh` | prefs `clustercast` (`save():153`, `loadPrefs():165`) | durable | one flat blob, no schema version except two one-shot migration keys `DEFAULTS_KEY`/`RECT_FIX_KEY` `:117-118` |
| profile override | prefs `clustercast/profileOverride` `ClusterProfile.kt:73` | durable | |
| bubble x/y | prefs `bubble` | durable | |
| DR runtime | `DeadReckonState` (global mutable object) | RAM | |
| last position | prefs `clusternav_lastloc` | durable | wall-clock aged, 7-day cap |
| **VD density / overscan / size** | **Android WM** | **`display_settings.xml`, survives reboot AND VD re-creation** | the app only *believes* it owns this |
| **animation scales, freeform flags** | **`Settings.Global`** | survives reboot | |
| **app-op PIP** | **appops** | survives reboot | |

### 2.2 Invariants the design intends

| # | Invariant | Enforced where | Status |
|---|---|---|---|
| **I1** | Never touch display 0 with a display- or task-scoped command | `applyBounds:612-614`, `placeAppOnVd:499`, `resetDisplayAll/Geometry:175/181`, `evictVd:190`, `StackParse.evictableOnVd:131`, `T3Daemon.java:65-71` | **holds** ✔ — guards are at the execution layer, per §5 |
| **I2** | Only ever move *standard*, non-pinned stacks; `home`/`recents`/`pinned` are untouchable | `StackParse.evictableOnVd:130-133` | **holds in `stop()` and `evictVd`; VIOLATED in two other places** — §3.1 |
| **I3** | Exactly one app on the cluster | `CastShell.evictVd:189-199` (now actually called, `:503`) | **holds on the cast path** ✔ |
| **I4** | Every external mutation has an undo that works even if the process died | `pipBlocked` marker `:903`; `reconcileOnStart:796` | **partially — 3 leaks**, §3.2 |
| **I5** | Decide from ground truth (`am stack list`, `dumpsys`), never from a RAM flag | `applyScaleLive:725-741`, `cast():368`, `landedOn:15` | **holds for placement/sizing** ✔ **violated for teardown**, §3.2 |
| **I6** | Never `force-stop` an app the user is depending on | `keepSessionApps` gate `:579`, warm-restore `:352-358` | **holds** ✔ |
| **I7** | Never gate a recovery path on data only that path can refresh | `DeadReckonService:374-378` (comment locks the lesson) | **holds** ✔ |
| **I8** | Model differences live in `ClusterProfile`, never scattered | `ClusterProfile.kt` | **BROKEN — the profile is largely decorative**, §3.3 |

---

## 3. WHERE THE CODE ALREADY VIOLATES ITS OWN RULES

### 3.1 Two remaining blind display-scans (I2) — **P0/P1**
- **`CastShell.restoreFullscreenOnMain:73`** — `StackParse.of(ents, pkg).filter { it.displayId >= 1 }` then `am display move-stack $it 0`. This is **the exact `displayId >= 1` blind-scan shape that froze the Dudu launcher** (CLAUDE.md §4). It is narrowed by package, but it is **not** filtered by `isStandard` and **not** filtered by `!isPinned` — so a `pinned` stack of that same package on a virtual display gets dragged to display 0. **[P] AOSP `ActivityStack.reparent()` (android-10.0.0_r47, ~1272-1298), order verbatim:** `removeFromDisplay(); … activityDisplay.addChild(this, onTop ? POSITION_TOP : POSITION_BOTTOM); if (!displayRemoved) { postReparent(); }` — and **[P] `ActivityDisplay.addStackReferenceIfNeeded()` (lines 1019-1072) throws `IllegalArgumentException`** when a home/recents/**pinned**/split-primary stack of that type already exists on the target display. Throwing *after* `removeFromDisplay()` and *before* `postReparent()` is precisely the orphaned-stack / never-resumed-launcher failure. Called from 7 sites including `stop():457`, `evictVd:198`, warm-switch `:328/:351`. **This is the single highest-severity item in the baseline.**
- **`ClusterCast.reconcileOnStart:827-828`** — `StackParse.of(...).firstOrNull { it.displayId == 0 && (it.isFreeform || it.isPinned) }` then routes into `restoreFullscreenOnMain`, inheriting the above. Runs at **BOOT** and after **every app update** (`RebindReceiver:81`) with nobody pressing anything.

### 3.2 Broken undo paths (I4/I5)
1. **`rollback():486`** — `adb.shell(CastShell.pipCmd(pipBlockedPkg, "allow"))`. Hard-coded `"allow"`, **ignores `pipPrevMode`**, and **does not clear `pipBlockedPkg`/`pipPrevMode`**. The spec's Reviewer Log Pass-2 claims this exact defect was fixed ("rollback() còn trả PIP bằng 'allow' cứng" — `cluster-cast-v036.html` §Reviewer Log). **It was not.** A CLAUDE.md §8 failure: the claimed patch has no call site. **[P]**
2. **`svcName` is RAM-only and only ever assigned inside `cast()`** (`:238` default `"AutoContainer"`, assigned `:374`). `stop():469`, `rollback():484`, and **`reconcileOnStart():836`** all call `sc()`. `reconcileOnStart` **never sets it at all**. On a DiLink5 (`svcName = "auto_container"`, `ClusterProfile.kt:60`) every one of those teardowns is fired at a service that does not exist — silently, exit 0, cluster stays projected. This is verbatim the failure mode CLAUDE.md §7 warns about. **[P]**
3. **`ensureFreeformSeed` (`CastShell.kt:47-54`)** writes three `Settings.Global` keys including `force_resizable_activities 1`, which after the next power cycle makes **every app on the vehicle** resizable, and the KDoc itself states there is no in-app undo (`:43-44`). No durable marker, no UI toggle. The field car already has all three at `1` (`00-state.txt:13-15`). **[P]** The spec lists "nút hoàn tác force_resizable_activities" as an unclosed Open Question.
4. **Animation scales** — `Prefs.animOpt` defaults **true**, so `MainActivity.kt:135` writes `0.5` globally on every app open. `cast():379` then reads `window_animation_scale` as `savedAnim` — which by then is **our own 0.5**, not the user's original. `savedAnimSafe:59` only protects against restoring `0`. Net: the user's original value is unrecoverable after the first launch. Field dump shows `window=0, transition=0` left behind (`00-state.txt:16-17`). **[P]**
5. **No watchdog for the cast app dying.** `ISSUES.md:30-33` asks for one explicitly. `grep` finds no polling loop while `casting==true`. If the projected app is killed or unplugged (exactly what happened during log collection), the cluster keeps projecting with `mHasContent=false` (`05-display.txt:114`), `03-vd-stacks.txt` is **empty**, and density=200/overscan=(536,224,88,336) sit on the VD (`02-summary.txt:2,7`) until the user presses TẮT. **[P]**
6. **`reconcileOnStart` cannot detect "cluster is projecting but empty."** It sends the teardown sequence only `if (onVd.isNotEmpty())` `:834`. The dirty-VD probe `:815-816` compares `logicalSize` vs `realSize` — it catches leaked **`wm size`** but **not** leaked density or overscan, which are the two things actually observed leaked in the field. So the exact state captured in `carlog-2026-07-21` would **not** be cleaned by a restart. **[P]**

### 3.3 `ClusterProfile` is decorative — the model seam is in the wrong place (I8) — **P1**
- **`vdNameHint` has zero consumers.** `grep -rn vdNameHint app/src/main/java` returns only its declaration, KDoc and `export()`. VD discovery is `sh("dumpsys display | grep -iE 'Display [0-9]+:|fission|xdja'")` **hard-coded in six call sites** (`ClusterCast.kt:319, 339, 391, 436, 726, 807`) plus `DisplayParse.kt:94` (`l.contains("fission") || l.contains("xdja")`). **[P]**
- **`clusterW`/`clusterH` have exactly one consumer** — a UI fallback (`ClusterCastActivity.kt:403`). Never used for bounds. **[P]**
- **`castSeq` `30` is special-cased by a hard-coded constant** `:385` (`if (raw == PROFILE_CURVED)`), so DL5's `castSeq = [16]` silently makes the per-app ◠/▭ button a no-op.
- **The only field that actually reaches behaviour is `svcName` and `teardownSeq`** — and `svcName` is defeated by 3.2#2.
- **Hard-coded package constants exist in `ClusterCast`**: `PKG_ANDROID_AUTO`/`PKG_CARPLAY` `:121-122`, consumed by `applyDefaultModes:191-200` which force-sets `t3Apps += com.byd.androidauto`. That is a per-app hard-code steering a *placement strategy* — a direct CLAUDE.md §7 violation, and it is *seeded from an on-car observation that ISSUES.md itself says was probably misattributed* (`ISSUES.md:10-20` argues the "AA only works at T3" report is more likely R3-force-stop, not the daemon; the spec §D1 states T3 was demoted *because it cannot place apps at all*). So the default contradicts the spec.

### 3.4 DiLink5 (Android 12) support is fiction — **P1**
Fetched from AOSP `android12-release`:
- **[P] `WindowManagerShellCommand.java` (android12-release) `onCommand()` switch has NO `"overscan"` case.** Cases present: `size, density, folded-area, scaling, dismiss-keyguard, tracing, logging, user-rotation, fixed-to-user-rotation, set-ignore-orientation-request, …, reset, disable-blur`. Compare android-10.0.0_r47 `:87-107` which *does* have `case "overscan": return runDisplayOverscan(pw);`.
  ⇒ On DL5, **Tier 2 of `applyBounds` does not exist**, `CastShell.overscanVerified:156-157`, `resetDisplayGeometry:176`, `resetDisplayAll:182`, `applyBounds:510/626/638` and `forceDisplaySize:131/144` all issue a command the shell rejects. Sizing degrades straight to `wm size` (centre-letterboxed only) and *teardown never clears overscan* — but there is no overscan to clear, so that part is benign.
- **[P] `ActivityTaskManager.RootTaskInfo.toString()` (android12-release) starts with the literal `"RootTask id="`, not `"Stack id="`.** `StackParse.RE_STACK = Regex("^Stack id=(\\d+).*?displayId=(\\d+)")` (`StackParse.kt:53`) therefore matches nothing on A12; because `if (stackId < 0) continue` `:80` gates everything, `parse()` returns an **empty list** on DiLink5. (Note `RE_TASK` `:57` already tolerates `Task id=` — someone half-anticipated this and stopped.)
  ⇒ On DL5: `pick()` → null → `cast()` decides the app is not running `:279`, launches it, still sees nothing, and aborts at `:283` **before** any cluster mutation. Non-functional but not dangerous. `stop()` on DL5 evicts nothing and fires the teardown at the wrong service (3.2#2).
- **[L]** DashCast's RE notes already recorded that DL5 ROM strips `cmd activity set-task-windowing-mode` and that `cmd activity task resize` returns 0 with no effect (`ClusterProfile.kt:53-56`). Nothing in the code branches on this.
- **Closing observation:** run `adb shell am stack list` and `adb shell wm overscan 0,0,0,0 -d 1` on any DL5 head unit. If the first prints `RootTask id=` and the second prints usage/error, DL5 support is confirmed dead and should be either implemented or removed from the profile list rather than advertised.

### 3.5 Dead code / functions with no call site (CLAUDE.md §8) — **P2**
`grep`-verified, zero production call sites:

| Symbol | Where | Note |
|---|---|---|
| `AppScale.nudgeEdge` + `enum Edge` | `AppScale.kt:137-164` | **6 unit tests still exercise it** (`AppScaleTest.kt:135-167`) — green tests on code no user can reach |
| `AppScale.fit` | `AppScale.kt:119-130` | 5 unit tests; superseded by `scaled()` in v0.42 but never removed |
| `StackParse.displayOf` | `StackParse.kt:110-111` | |
| `StackEntry.isSystemStack` | `StackParse.kt:40` | |
| `StackEntry.bounds` | `StackParse.kt:32/77/89` | parsed and stored, never read — KDoc claims it verifies resize took effect; nothing does |
| `ClusterCast.setKeepKmh` / `keepKmh` | `:66/:123`, persisted `:155/:167` | replaced by `rectProfileApps` in v0.37, still saved/loaded |
| `ClusterCast.setTeardownProfile` | `:125` | no UI; the *only* documented cure for the chronic "cụm kẹt cong" bug (`cast-bug-2026-07-20.log` §BUG C, listed **P1**) is unreachable from the app |

### 3.6 Proven on-car defect not yet in any issue list — **P2**
`02-window-displays.txt:117` — task 31 holds **10 distinct `ClusterCastActivity` ActivityRecords** stacked on top of one `MainActivity` (verified: `grep -o 'ActivityRecord{… ClusterCastActivity t31}' | sort -u | wc -l` = 10). `AndroidManifest.xml:94-97` declares `ClusterCastActivity` with **no `launchMode`**, and `MainActivity.openCastSettings():148` uses a plain `startActivity` with no `SINGLE_TOP`/`REORDER_TO_FRONT`. Each instance re-runs `loadPrefs` + `repairLegacyAnim` + `reconcileOnStart` + a background `queryIntentActivities` over every installed app (`ClusterCastActivity.kt:50-52, 101-109`) and retains a `TextView` log buffer. **[P]**

### 3.7 Smaller, still real
- **`DisplayParse.appWindowFrame:73-83` is display-blind and takes the first matching window.** It scans `dumpsys window windows` for any header containing `"$pkg/"`. Sub-windows (`SurfaceView - pkg/…`) and windows on display 0 both match. `overscanVerified` — the measurement that decides whether to escalate to the destructive `wm size` tier — is built on it. **[L]** that this misreads for any app with a window on both displays; **closing observation:** capture `dumpsys window windows` (never yet collected — `ISSUES.md:38`) while AA is on the cluster and count matching headers.
- **`overscanVerified:156-159` reads the frame back with no settle delay** after `wm overscan`. Relayout is asynchronous; a stale read returns "app ignored the inset" → escalate to `wm size` → the user's off-centre frame is silently replaced by a centred letterbox. **[L]**
- **`AppScale` stores absolute pixels** (`rectL/T/R/B`) against `clusterRef()` (`ClusterCastActivity.kt:401-404`), which falls back to the **profile's 1920×720** before the first ever cast. `boundsOn(w,h):24` does **not** rescale to the actual cluster. A profile shared with an SL6 owner (S9) carries the cluster geometry but **not** the per-app rects, and any rect adjusted before the first cast is computed against a guessed size. Per-app vs per-model is split in the wrong place.
- **`DeadReckonModule.selfTest` calls `MockLoc.start(ctx)` then `MockLoc.stop(ctx)`** (`DeadReckonModule.kt:36`) — i.e. `addTestProvider`/`removeTestProvider` on the live `GPS_PROVIDER`. Reachable from `AutotestActivity` (exported=true, launchable by any shell). If it fires while DR is mocking in a tunnel, it tears down the active mock. **[L]**
- **`SpeedProvider.bypass():49-56` returns `getPackageName() = "com.byd.dashcast"`** and calls `VMRuntime.setHiddenApiExemptions` `:59-64` — the app impersonates another package to pass BYD HAL permission checks. Note for whoever owns the security lens; not a leak, but it is a load-bearing hack with no fallback if the HAL starts checking uid.
- **`ClusterCast.kt` is 924 LOC**, over the repo's own 500-LOC guardrail (global CLAUDE.md §4.1). `CastShell.kt` was the extraction; it stopped at 200 LOC.

---

## 4. THE SEAMS — what is per-model, per-app, global, and whether it is in the right place

| Concern | Today lives in | Should be | Verdict |
|---|---|---|---|
| AutoContainer service name | `ClusterProfile.svcName` **but** copied into RAM `ClusterCast.svcName:238` set only by `cast()` | `ClusterProfile`, read at every use | **wrong place** — teardown/reconcile use the DL3 default |
| cast / teardown opcode sequences | `ClusterProfile.castSeq/teardownSeq` ✔ | same | **right**, except `30` is special-cased by constant `:385` |
| VD name hint | **hard-coded 7×** in `ClusterCast`/`DisplayParse` | `ClusterProfile.vdNameHint` (field already exists, unused) | **wrong place** |
| cluster W×H | measured at cast (`lastClusterW/H:108-110`) with profile fallback | measured; profile only as seed | **right** ✔ |
| `wm overscan` availability (A10 yes / A12 no) | nowhere — assumed universal | `ClusterProfile` capability flag driving `applyBounds` tier selection | **missing entirely** |
| `am stack list` output dialect | `StackParse` regex assumes A10 | `ClusterProfile` (or dual-dialect regex) | **missing** |
| DR constants `WHEELBASE 2.92`, `STEER_RATIO 15.5`, `SPEEDO_CORRECTION 0.93` | `DeadReckonService:614-616` (Seal-only) | `ClusterProfile` | **wrong place** — already an Open Question in the spec, still open |
| per-app frame + DPI | `AppScale` in prefs ✔ | `AppScale`, but **fractional**, not absolute px | **right container, wrong units** |
| per-app cluster style ◠/▭ | `rectProfileApps` ✔ (v0.37 replaced the global `keepKmh`) | same | **right** ✔ |
| per-app "never force-stop" ◈ | `keepSessionApps` ✔ | same | **right** ✔ |
| per-app "let the daemon force freeform" ⊞ | `t3Apps` ✔ — but **pre-seeded by hard-coded package** `:193` | measured, never seeded by package name | **right container, §7-violating default** |
| legacy artistic inset `insetH=0 / insetV=90` | **global** `ClusterCast:72-73` | per-model (it exists to clear the curved cluster bezel) | **wrong place** |
| default DPI `clusterDpi = 200` | global fallback `:74` | per-model seed, per-app override | **wrong place** |
| autoCast app (exactly one) | global `autoCastPkg:100` ✔ | global | **right** ✔ |
| animation scales | **global**, default-on, no true undo | opt-in, with a saved original | **wrong** |
| freeform / force_resizable flags | **global**, irreversible | global but with a durable marker + undo button | **wrong** |
| app-op PIP | per-app, durable marker ✔ | same | **right** ✔ (except `rollback()`, §3.2#1) |
| profile sharing payload | 8 scalar fields only | should carry the per-app `AppScale` map the group actually wants to trade | **incomplete vs. user story S9** |

---

## 5. WHAT I COULD NOT DETERMINE (say "chưa biết", per §2)

1. **Does Android Auto ignore content insets?** Still unproven. `androidauto` appears **0 times** in all four dumps (`ISSUES.md:36`, re-verified: `grep -ic androidauto` = 1 hit, in `00-state.txt` appops only). The *mechanism* is proven from AOSP; the *attribution* is not. **Closing observation is already written down** (`ISSUES.md:45-51`) and still not executed: `dumpsys window windows | grep -A8 -i androidauto | grep -E 'mFrame|fl=|pfl='` while AA is on the cluster. v0.42 does not depend on the answer (`overscanVerified` measures) — **but on DL5 that measurement path does not exist at all** (§3.4).
2. **Which AutoContainer opcode returns the cluster to the stock Seal gauge** (the chronic "kẹt cong" bug, marked **P1** in `cast-bug-2026-07-20.log:142`). `teardownProfile` is still `-1` and the setter is unreachable (§3.5). Untested candidates listed at `cast-bug-2026-07-20.log:206-210`: 31, 29, 1. Requires a human looking at the physical cluster — `screencap -d 1` captures the VD surface, not the panel (`:109`).
3. **Whether `stop()` ever ran to completion in the field.** `README.md:37-41` correctly retracts the earlier conclusion: unplugging CarPlay to get WiFi produces the same picture. Genuinely **unknown**. What *is* established from the same evidence is that nothing watches the projected app (§3.2#5).
4. **SL6 / Han cluster geometry and opcode set.** Never measured. `detectSeed:129-135` maps every `"byd"` head unit to `SEAL_DL3`.
5. **Whether `SpeedProvider.mps()` returns 0 or "unreadable" on SL6** — the `speed < 2.0` cold-seed gate (`DeadReckonService:293`) silently opens if the HAL returns 0 forever. Already an Open Question in the spec; `SpeedProvider.kt:29` degrades to `lastGoodMps = 0.0` with no distinction. Unresolved.

---

## 6. SUGGESTED DIVISION FOR THE OTHER SIX REVIEWERS

1. **Stack/display safety** — §3.1 (`restoreFullscreenOnMain` blind scan), I2, all 7 call sites. Highest severity.
2. **Undo & lifecycle** — §3.2 (all six), plus the missing cast-app watchdog and the `reconcileOnStart` detection gap.
3. **Model portability** — §3.3 + §3.4 + §4. Propose the *one* generic fix (capability-probed profile) rather than DL5 special cases.
4. **Sizing engine** — `applyBounds` tiers, `overscanVerified` read-back race, `AppScale` absolute-px units, seed/nudge/preset interaction with `insetV=90`.
5. **Dead-reckoning / mock GPS** — peek cadence vs. `PROVIDERS_CHANGED` blast radius, cold-seed gating, `DeadReckonModule.selfTest` hazard, Seal-only constants.
6. **Hygiene & regression locking** — §3.5 dead code (delete or wire), §3.6 activity leak, `ClusterCast.kt` 924 LOC, and the CLAUDE.md §10 gap: there is **no fixture-driven test** built from `carlog-2026-07-21/01-stacklist.txt`, and no test at all for `CastShell` or `ClusterCast`.