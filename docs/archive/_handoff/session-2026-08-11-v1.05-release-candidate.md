# Session handoff — 2026-08-11 · ClusterNav v1.05 release candidate

> Trạng thái: **RELEASE CANDIDATE 1.05 ĐÃ BUILD, chưa commit/push.** Chờ test trên xe → OK → commit (scope A: tất cả) → push.
> Owner: Đăng Khôi · dangkhoi. Baseline: `main@d85b9f2` + uncommitted WIP (không có commit mới trong phiên này).
> Env build: `export JAVA_HOME=/opt/homebrew/opt/openjdk@17` (+ `ANDROID_HOME=$HOME/Library/Android/sdk` cho `collectAuthorizedApk`).

---

## 1. Phiên này làm gì (tất cả OFF-CAR, chưa commit)

Batch UX/bugfix từ owner (spec `docs/specs/cast-nav-ux-release-v104.html`) + submenu polish + kênh cứu hộ DashCast + P0 + bump 1.05 + build candidate.

### 7 việc owner (spec R1–R9, 4 wave + senior review)
| # | Việc | Cách fix | Wave |
|---|------|----------|------|
| 6 | Ẩn option nav→HUD | `cb_hud`/`txt_hud_status` = gone (2 layout); gỡ wiring; ép HUD output disabled; nav→cluster giữ nguyên | 1 |
| 3 | Không cast launcher | `AppMover.isLauncher()` (:core) + query mọi `CATEGORY_HOME` ∪ blocklist dudu → toast, không cast (tap + autostart) | 1 |
| 4 | Đủ 9 tỉ lệ split + lưu size/tỉ lệ | `CastProfile(side,percent)` percent∈{10..90} = 19 profile/app; spinner 9 mức; bounds theo (pkg,side,%) | 2 |
| 5 | Lưu DPI theo tỉ lệ | root-cause: split-DPI lưu qua `CastingFull.targetPkg`=null → mất. Fix `setDensitySplit` lưu density per-slot cùng khoá profile | 2 |
| 7 | Nút nổi 1 icon | 1 arrow `ic_bubble_nav.xml`, không khung; tap=full toggle; long-press=submenu Trái/Phải/Cấu hình; drag mọi trạng thái | 3 |
| 1 | Cast VietMap kẹt "cửa sổ" | root-cause: path thật `SimpleCastCoordinator→AppMover` cast freeform(mode 5), return không reset. Fix `AppMover.fullscreenReturnCommand` (windowingMode 1 + LAUNCHER reparent) + `isWindowedOnMain` self-heal | 4 |
| 2 | Nav tới nơi không clear (500m→3.5km, kẹt "đi thẳng") | root-cause: `ClusterBroadcaster` heart-beat frame cũ 180s + thiếu distance guard. Fix `NavArrivalGuard` (:core, pure) → `NavRepository.stop` on arrival + chặn distance regression | 4 |

### Submenu polish (#7 follow-up, iterate với owner trên emulator)
- Rewrite `BubbleSubmenuOverlay`: **bám cạnh nút nổi** (`BubbleSubmenuAnchor.offset`, pure :core + test), **wrap vừa chữ (~60%)** bằng LinearLayout dividers, **row ~40dp (~80%)**, **nền trắng ~80% trong** (`0xCCFFFFFF`), icon ◀▶⚙ + line phân cách, đơn giản hơn. Owner đã duyệt trên emulator.

### Kênh cứu hộ DashCast (owner request)
- Vấn đề: xe đã cài DashCast + cài ClusterNav → 2 app dùng CHUNG API chiếm cụm (ClusterNav RE từ DashCast) → tranh projection/windowing/VD → kẹt cả 2, từng phải flash firmware.
- Fix: nút mới **"Dọn sạch cụm (gỡ kẹt DashCast)"** (`CastDeepRescueAction`) dưới "Khắc phục sự cố": (1) dừng hẳn cast + tắt nút nổi, **KHÔNG mở lại**; (2) `am force-stop com.byd.dashcast` + `com.xdja.clusterdemo`; (3) reset VD (`wm size/density/overscan reset`). Best-effort + trung thực (nhắc gỡ DashCast + power-cycle).
- **Trần thật:** chỉ verify đầy đủ trên xe; ca kẹt sâu OEM/MCU vẫn có thể phải flash. KHÔNG reproduce conflict off-car (rủi ro).

### P0 (bản APK cũ độc hại)
- Bản archive `apk/ClusterNav-1.04-v104-527589f2d16a-release.apk` (sha `b9a0259e…`) vẫn export `TEST_ADAS_*/TEST_SPEED_LIMIT` (pre-hardening). Đã thêm sha vào blocklist `scripts/vehicle/common.sh` `INVALIDATED_CANDIDATE_SHA256S` → `require_candidate` **refuse** (exit 9). Source hiện tại đã hardened từ trước (release không export test surface).

---

## 2. Release candidate 1.05 (đã build, verify)

- **APK:** `apk/ClusterNav-1.05-v105-fbd8331ffa8c-release.apk`
  - sha256 `d1d3db74f3902c4466a0ca8465fdb496a78f38cab61092b33701979eb16ab5e4`
  - versionName **1.05** / versionCode **105** · **KHÔNG debuggable** · **0** `TEST_ADAS_*`/`TEST_SPEED_LIMIT`
  - exactSourceId `fbd8331ffa8cf9b33e097d56b58a61fb75503dba2cf312cb11369b6a0cd3b5a3` · manifest `docs/_handoff/v1.05-exact-source.json` · 164 attested inputs · builtAtUtc `2026-08-11T09:43:43Z`
- **`docs/_handoff/vehicle-candidate.json`** đã regenerate → trỏ bản 1.05; `require_candidate` **ACCEPT** (exit 0). Bản 1.04 độc hại vẫn blocklisted → refuse. ⇒ **P0 fix triệt để.**
- Build lại (reproducible):
  ```bash
  export JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=$HOME/Library/Android/sdk ANDROID_SDK_ROOT=$HOME/Library/Android/sdk
  python3 scripts/evidence/gen-exact-source.py --out docs/_handoff/v1.05-exact-source.json --label "1.05" --base docs/_handoff/v1.04-exact-source.json   # in ra sourceId=...
  ./gradlew clean collectAuthorizedApk -PclusterNavVariant=release -PclusterNavSlice=v105 -PexactSourceId=<sourceId> -PexactSourceManifest=docs/_handoff/v1.05-exact-source.json
  ```
  Lưu ý bẫy đã gặp: cần `ANDROID_HOME` (aapt2 verify); cần build FRESH trong cùng invocation (dùng `clean`); xoá candidate cũ nếu task báo "Refusing to overwrite".

---

## 3. Cổng chất lượng đã qua (off-car)

- **Senior review (opus): APPROVED** — 0 P0–P1. Report: `docs/_handoff/cnux-release-1.05-review.md`; spec Reviewer Log Pass 6.
- **Tests:** core 727 + app 328 + car-integration 28 = **1083, 0 failures**. `LayeringRulesTest` xanh (:core Android-free). Mọi file ≤500 LOC. Không thêm dependency.
- **`lintRelease`:** 0 error (abortOnError=true). `assembleRelease`: 1.05/105, not-debuggable, WARN-1 hardened.
- **Security scan (pre-commit, opus): BLOCKED → fixed → CLEAN.** Report: `docs/_handoff/cnux-precommit-scan-1.05.md`. Đã redact: machine path `<home>/` trong `cast-nav-ux-release-v104-execution.md`; vehicle IP `<vehicle-ip>`→`<vehicle-ip>` (3 docs/scripts) + `<vehicle-ip>`→synthetic (1 test fixture). Không có secret/key/PII thật. Git identity commit tiếp = `dangkhoi@users.noreply.github.com`.

---

## 4. CHƯA commit/push (theo owner)

- HEAD vẫn `d85b9f2`. Commit scope đã chốt = **A (commit TẤT CẢ uncommitted thành 1 baseline `release: v1.05`)**, nhưng **để sau khi test xe OK**.
- Investigation biển báo/HUD (hud-cluster-injection, hud3, offcar-planner, seal-hud-sign, vehicleTest HudSignProbe, scripts/re, navopen-v4…) sẽ **cùng nằm trong commit A** (không lên APK release nên vô hại) — owner đã đồng ý.

---

## 5. RESUME HERE — bước tiếp (on-car → commit)

1. **Cài candidate lên xe:**
   ```
   adb -s <vehicle-ip>:5555 install -r apk/ClusterNav-1.05-v105-fbd8331ffa8c-release.apk
   ```
   Nếu `INSTALL_FAILED_UPDATE_INCOMPATIBLE` (khác chữ ký) → `adb uninstall com.byd.clusternav` trước (⚠ mất Cast durable state). Verify `dumpsys package com.byd.clusternav | grep versionName` → **1.05**.
2. **Test on-car** (checklist spec `docs/specs/cast-nav-ux-release-v104.html` §Verification):
   - #6 option HUD đã ẩn · #3 không cast Dudu/launcher · #4/#5 chỉnh size+DPI ở vài tỉ lệ (vd 20/80) → cast lại đúng · #7 nút nổi 1 icon + submenu bám cạnh/gọn · #1 VietMap cast qua lại luôn fullscreen, "về màn chính" full lại · #2 GMaps tới nơi → cụm clear (không kẹt 3.5km) · kênh cứu hộ DashCast (nếu có DashCast) → dọn sạch, cụm về đồng hồ.
   - HUD arrows/nav lên cụm cần **GPS thật** (release không có mock GPS).
3. **Test OK → báo agent:** chạy security scan lại (bắt buộc trước push) → **commit** (scope A, `release: v1.05 — cast/nav UX (7), submenu, DashCast rescue, P0 blocklist`) → **push**.
   - ⚠ **Merge vào `main`** vẫn cần on-car PASS + owner authorization (README). Push baseline OK sau test; merge/final gate riêng.
4. Nếu test FAIL → fix theo triệu chứng, rebuild candidate (mục §2), test lại. KHÔNG commit khi chưa PASS.

---

## 6. File thay đổi chính (uncommitted phiên này)

- **Waves 1–4** (nhiều file app/core): chi tiết trong `docs/_handoff/cnux-stage-{1,2,3,4}-done.md`.
- **Submenu:** `app/.../clustercast/BubbleSubmenuOverlay.kt`, `core/.../simplified/BubbleSubmenuAnchor.kt`(+test), `app/src/main/res/drawable/ic_menu_{left,right,config}.xml`, `FloatingBubbleService.kt`, `BubbleGestureContractTest.kt`.
- **Rescue:** `app/.../clustercast/CastDeepRescueAction.kt`(+`CastDeepRescueContractTest.kt`), `MainActivityCastController.kt`, `app/src/main/res/layout{,-w960dp}/activity_main.xml`, `scripts/vehicle/common.sh`.
- **Version/docs:** `app/build.gradle.kts` (1.05/105), `README.md` (→1.05), `docs/specs/cast-nav-ux-release-v104.html`, `docs/_handoff/cnux-*.md`, `docs/_handoff/v1.05-exact-source.json`, `docs/_handoff/vehicle-candidate.json`.

---

## 7. Investigation biển báo/HUD (track riêng — KHÔNG đổi phiên này)

Track "nhét số giới hạn tốc độ tùy chọn lên cluster / nav lên HUD" độc lập, chưa on-car:
- Công cụ: `apks/navopen-v4.jar` (verbs `readcfg`/`canmon`/`canreg`), script `scripts/vehicle/hud3-speedlimit-v4.sh`, kịch bản `docs/diagnostics/oncar-speedlimit-test-2026-08-11.md`.
- Findings: `docs/_handoff/hud-cluster-injection-findings-2026-08-10.md` (§1–26), STATE `docs/_handoff/cluster-hud-injection-STATE.md`.
- Không liên quan tới release 1.05; để nguyên.
