# ClusterNav v1.03 — Remaining Stages Execution Prompt

> Auto-generated from spec: `docs/specs/clusternav-v102-review-remediation.html`
> Stages: 5 (T2-remainder, T3, T4, T5, T6+T7) | Total deliverables: ~28
> Generated: 2026-08-05 10:41 +07

## TASK

Complete ClusterNav v1.03 remediation: bounded output ownership (T2 remainder), widget independence (T3), Cast core postconditions (T4), Cast UI lifecycle (T5), test rebaseline + consolidation (T6+T7). T8 (vehicle) is manual — not in scope here.

## WORKING DIR

`<project-root>`

## CONTEXT

- Spec approved: `docs/specs/clusternav-v102-review-remediation.html`
- T0+T1 DONE. T2 partial: frame shape, ingest, arrow pre-classification, global-state removal done.
- `SPEED_SIGN` already added to `NavigationOutputTarget` enum; exhaustive `when` patched.
- `TestHalReceiver` exists (debug tool, remove before release).
- Core pattern established: `BoundedNavigationOutputWorker` → `HudAdapter` / `ClusterLaneAdapter` wrapping it.
- Execution-reliability: NO large blocking sub-agent DAG. Primary agent works in direct 3–5 file batches. Sub-agents allowed only for bounded report-only reviews (≤5 files, one boundary, <90s).
- On-car finding (2026-08-05): `BydHal.writeSpeedLimit` HAL write returns rc but cluster does not render — camera ADAS override suspected. Proceed with bounded owner code regardless; vehicle validation is T8.

## CONSTRAINTS

- Every source file ≤ 500 LOC.
- No `NavRepository.state` access from physical output path.
- Each output (Lane, HUD, Speed-sign) owns its own bounded executor/queue/generation/clear.
- No feature beyond spec scope.
- Compile must pass after every batch.
- Match existing code style (Kotlin, single-expression where natural, explicit init validation).

## EXECUTION — 5-STAGE CHAIN

> ⚠️ Primary agent executes directly in small-batch mode.
> No blocking sub-agent for implementation/build/test.
> Report-only bounded reviewer allowed per boundary (≤5 files, one contract, <90s).

---

### Stage 1: T2 Remainder — Bounded HUD + Speed-Sign Owners

**Deliverables:**
- D1: `NavigationHudOwner` in app layer — bridges NavState → `HudAdapter` core pattern. Owns dedup (icon/seg/road), bydIcon mapping, lifecycle clear. Uses its own `BoundedNavigationOutputWorker` instance via `NavigationFrameDelivery` that calls `BydHal.writeNavFrame` / `BydHal.clearNavFrame`.
- D2: `NavigationSpeedSignOwner` in app layer — bridges speed-limit int → bounded worker. Owns dedup (lastLimit), generation-fence (clear invalidates queued writes), lifecycle clear. Uses own worker instance via delivery that calls `BydHal.writeSpeedLimit` / `BydHal.clearSpeedLimit`.
- D3: Refactor `ClusterBroadcaster` — remove shared `hudExec`, `lastHudIcon/Seg/Road`, `hudActive`, `lastPushedLimit`. Delegate `pushHud`/`clearHud` → `NavigationHudOwner`. Delegate `pushSpeedLimit` → `NavigationSpeedSignOwner`. Keep `emitHud`/`stopHud`/`pushSpeedLimit` public API signatures.
- D4: Persistence load corruption fails closed (try/catch in NavRepository load, log + clear, don't crash).

**Key files to read before coding:**
- `core/src/main/kotlin/com/byd/clusternav/navigation/HudAdapter.kt`
- `core/src/main/kotlin/com/byd/clusternav/navigation/ClusterLaneAdapter.kt`
- `core/src/main/kotlin/com/byd/clusternav/navigation/NavigationModels.kt`
- `app/src/main/java/com/byd/clusternav/ClusterBroadcaster.kt`
- `app/src/main/java/com/byd/clusternav/modules/hal/BydHal.kt` (writeNavFrame/clearNavFrame/writeSpeedLimit/clearSpeedLimit signatures)
- `app/src/main/java/com/byd/clusternav/NavRepository.kt` (persistence load)

**Pattern for owners (both follow this):**
```kotlin
class NavigationHudOwner(private val appContext: Context) {
    private val worker = BoundedNavigationOutputWorker(
        NavigationOutputTarget.HUD, "hud-hal-delivery",
        delivery = NavigationFrameDelivery { frame -> /* BydHal.writeNavFrame(...) */ },
        config = OutputAdapterConfig(queueCapacity = 4, deliveryDeadlineMs = 200L),
        nowEpochMs = System::currentTimeMillis,
        initiallyEnabled = false
    )
    // Dedup state (icon/seg/road) + push/clear methods
    // Lifecycle: start()/stop() toggle enabled
}
```

**Exit gate:**
- [ ] `./gradlew :app:compileDebugKotlin` — SUCCESS
- [ ] `ClusterBroadcaster` no longer contains `hudExec`, `lastHudIcon`, `lastHudSeg`, `lastHudRoad`, `lastPushedLimit`
- [ ] Each owner file ≤ 200 LOC
- [ ] NavRepository persistence load wrapped in try/catch

**Handoff:** `docs/_handoff/stage-1-t2-done.md`

---

### Stage 2: T3 — Widget Provider Independence

**Deliverables:**
- D5: Split speed/alert freshness — each `VietMapWidgetSlot` has independent `updatedAtElapsedMs`, `freshness`, `reason`, and `generation` tracking.
- D6: Preference commit reconciliation — check orphan widget host IDs on startup, delete leaked IDs.
- D7: Speed-sign consumer uses only fresh speed provider — if stale/OFF/disconnect → issue clear via `NavigationSpeedSignOwner`.
- D8: Show exact manual grant instruction on bind failure (BIND_UI_UNAVAILABLE/BIND_DENIED) — never self-grant.

**Key files to read:**
- `app/src/main/java/com/byd/clusternav/vietmapwidget/VietMapWidgetBridge.kt`
- `app/src/main/java/com/byd/clusternav/vietmapwidget/VietMapWidgetDiagActivity.kt`
- `app/src/main/java/com/byd/clusternav/vietmapwidget/` (all files in directory)

**Exit gate:**
- [ ] `./gradlew :app:compileDebugKotlin` — SUCCESS
- [ ] Speed and Alerts freshness tracked independently (grep: separate updatedAt per slot)
- [ ] Stale speed → `NavigationSpeedSignOwner.clear()` called
- [ ] Each modified file ≤ 500 LOC

**Handoff:** `docs/_handoff/stage-2-t3-done.md`

---

### Stage 3: T4 — Cast Core (Bounded Verified Operations)

**Deliverables:**
- D9: Extract `CastTransport` — bounded shell execution with typed timeout/error result.
- D10: Extract `CastOperationOwner` — one active operation + one replaceable queued; Stop never blocked behind work.
- D11: Strict stack/task parser — parse `am stack list` into typed model; reject ambiguous states.
- D12: Postcondition verifier — verify landing/resize/return; preserve prior state on rejection/unknown/timeout.
- D13: CP/AA full-only enforcement — classify protected apps, require fullscreen standard stack, block freeform.
- D14: Whitelist preference conditional on command success (not optimistic).

**Key files to read:**
- `app/src/main/java/com/byd/clusternav/modules/clustercast/simplified/SimpleCastRuntime.kt`
- `core/src/main/kotlin/com/byd/clusternav/modules/clustercast/simplified/` (all)
- `app/src/main/java/com/byd/clusternav/modules/clustercast/simplified/` (all)

**Exit gate:**
- [ ] `./gradlew :app:compileDebugKotlin` — SUCCESS
- [ ] Each extracted file ≤ 500 LOC
- [ ] No test reaches Casting when no target task exists (verified by test or static analysis)
- [ ] Optimistic commit removed — persist only after postcondition pass

**Handoff:** `docs/_handoff/stage-3-t4-done.md`

---

### Stage 4: T5 — Cast UI Lifecycle

**Deliverables:**
- D15: Extract Bubble renderer/gesture/action dispatcher from `FloatingBubbleService`.
- D16: ≥48dp touch zones, accurate action descriptions, non-destructive disabled zones, one tap token.
- D17: Extract Home autostart/geometry/status; cancel delayed work on lifecycle/state/config change.
- D18: Display-aware SharedPreferences editor; one shared DPI for split.
- D19: Replace V2 diagnostics with simplified operation/observation report.

**Key files to read:**
- `app/src/main/java/com/byd/clusternav/modules/clustercast/FloatingBubbleService.kt`
- `app/src/main/java/com/byd/clusternav/modules/clustercast/DiagActivity.kt`
- `app/src/main/java/com/byd/clusternav/MainActivity.kt`

**Exit gate:**
- [ ] `./gradlew :app:compileDebugKotlin` — SUCCESS
- [ ] `FloatingBubbleService` ≤ 500 LOC (extracted parts in separate files)
- [ ] Touch zones ≥ 48dp in layout/code
- [ ] No V2 diagnostic reference in active production code

**Handoff:** `docs/_handoff/stage-4-t5-done.md`

---

### Stage 5: T6 + T7 — Test Rebaseline + Consolidation

**Deliverables:**
- D20: Delete retired V2/legacy test contracts that reference removed architecture.
- D21: Update remaining tests to match new simplified contracts (NavigationHudOwner, SpeedSignOwner, CastOperationOwner, etc.).
- D22: `./gradlew :core:test :app:testDebugUnitTest :car-integration:test` — 0 failures.
- D23: LOC ratchet — every modified file ≤ 500 LOC verified.
- D24: Remove `TestHalReceiver` + manifest entry (debug-only, not for release).
- D25: Redact private IPs/absolute paths in tracked docs.
- D26: Security scan full diff.

**Senior Review (bounded, per-boundary, report-only sub-agents):**
- Boundary A: Navigation models/store/coordinator + HudOwner + SpeedSignOwner
- Boundary B: Widget bridge + provider independence
- Boundary C: Cast core (transport/owner/parser/verifier)
- Boundary D: Cast UI (bubble/home/diag)
- Boundary E: Build/release/test infrastructure

Each review: ≤5 files, one contract, report-only. Primary agent applies patches and reruns tests.

**Exit gate:**
- [ ] `./gradlew :core:test :app:testDebugUnitTest :car-integration:test` — 0 failures
- [ ] Senior review loop: 0 P0–P1 remaining
- [ ] Security scan: CLEAN (no BLOCK)
- [ ] All modified files ≤ 500 LOC
- [ ] No private IP/developer path in tracked docs (except raw artifacts in `docs/diagnostics/`)

**Handoff:** `docs/_handoff/stage-5-t6t7-done.md`

---

## ORCHESTRATOR INSTRUCTIONS (Variant A — Kiro CLI, respecting execution-reliability)

> **CRITICAL:** This workspace FORBIDS large blocking sub-agent DAGs.
> Primary agent executes ALL implementation directly in 3–5 file batches.
> Sub-agents ONLY for bounded report-only reviews (≤5 files, one boundary, <90s).

1. **Read spec** (`docs/specs/clusternav-v102-review-remediation.html`) section for current stage before each batch.
2. **Implement in direct batches** — read 3–5 files → edit → compile → report result.
3. **Compile after EVERY batch**: `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ./gradlew --no-daemon :app:compileDebugKotlin`
4. **Scope check** after each stage: verify every deliverable = ✅ (code exists + compiles).
5. **Tests** (Stage 5 only): `JAVA_HOME=... ./gradlew --no-daemon :core:test :app:testDebugUnitTest :car-integration:test`
6. **Senior review** (Stage 5): spawn bounded report-only sub-agents per boundary. Apply patches directly. Rerun tests.
7. **Security scan** (before any commit): mandatory per workspace rules.
8. **Handoff file** after each stage: record files changed, contracts confirmed, compile result.
9. **If compile fails** → fix immediately in same batch, do not proceed until green.
10. **If context approaching limit** → write progress to handoff file, report resume point.

## ERROR RECOVERY

- Compile fail → read error, fix in same batch, recompile.
- Test fail → read failure, fix source or test, rerun focused.
- Context limit → write `docs/_handoff/stage-N-partial.md` with progress + next steps.
- Sub-agent timeout/cancel → disable sub-agents, continue direct. Never retry same call.

## FINAL EXIT CRITERIA (from spec)

- [ ] R1–R20 each has code + test + wiring evidence
- [ ] Fresh full test suite: 0 failures
- [ ] Senior review loop exits with 0 P0–P3 findings
- [ ] Security scan CLEAN; tracked docs contain no private vehicle IP or developer path
- [ ] Every modified source/build file ≤ 500 LOC
- [ ] No V2 architecture reference in active production code
- [ ] `TestHalReceiver` removed from production source
- [ ] Candidate source ready for separate build authorization (T8 vehicle is out of scope here)
