# Handoff — ClusterNav phiên 2026-07-27

> HEAD `55ae408` trên `release/v0.60-cast-hardening`. 34 commit trong ngày. **697 test xanh** (app 262 · core 421 · car-integration 14). Cây: core 9.479 dòng · app 8.535 · car-integration 802.

## 1. Câu hỏi lớn của dự án — đã trả lời được cái nào

**Khả thi?** Có, đã chứng minh trên xe với mắt người xác nhận: đặt task lên cụm (`am start --display 1 --windowingMode 5` + `am task resize`), mở chiếu (`30,16,35` → chủ xe "vietmap lên ok"), hạ chiếu (`18,0` → "đồng hồ"). Không root, không reboot, không đụng cài đặt toàn cục.

**Q1 (observable để app tự biết cụm hiện gì) — ĐÓNG, kết quả ÂM TÍNH.** Không có tín hiệu chỉ-đọc nào của Android phân biệt "cụm hiện app" với "cụm hiện đồng hồ". Đã thử: `dumpsys display/window/SurfaceFlinger`, `am stack list`, `service list`, `getprop`, `settings`×3, `screencap -d 1` (giống 99,49%), và transaction 1–10 của `AutoContainer`. Ghim bằng `RealFixtureParsingTest` trên hai fixture `*HUMAN-CONFIRMED*`. Hệ quả kiến trúc: `ClusterAttestation` trong `:core` — xác minh chia hai hạng (đo được / người xác nhận); app **không bao giờ** tự nói "đang chiếu".

## 2. Việc CÒN CHẶN — cần buổi xe

| Việc | Trạng thái |
|---|---|
| `reissue-policy` (khi nào được phát lại `30,16,35`) | chặn đường app; luật đúng phụ thuộc kết quả trên xe. **Không sửa trước** = không đoán |
| Đưa map lên cụm QUA APP | chưa từng làm được; mọi bằng chứng tới giờ đều bằng shell |
| Xoá engine V1 (`ClusterCast/CastShell/ClusterProfile/ClusterDiag`, ~2.110 dòng, 8 điểm adb) | chỉ xoá được sau khi V2 chạy trên xe |
| Biển báo tốc độ (7 step khám phá) | 4 step đầu chỉ-đọc, chạy lúc đang lái; nếu cả bốn ra rỗng → hướng này dừng |
| Fixture `am get-current-user` | một lệnh trên xe để `observe --recorded` chạy hết off-car |

verdicts.tsv: **6/31 step** có candidate OK. `run-on-car.md` có thứ tự chạy cho phiên sau theo mức rủi ro.

## 3. UI — đã mang bản v0.57 về (bản trước khi two-track làm xấu đi)

Chủ dự án xác nhận UI đẹp là **v0.3x–v0.57** (`ctrlGroup` sống tới commit `ff01199`). Đã dựng lại:

- **Home** hai cột (thêm `layout-w960dp` — trước đó Home không có layout màn rộng nên thẻ Cast rơi khỏi màn xe 1280dp). Khối Cast: `Chiếu lên cụm · Hiện nút nổi · Cài đặt chiếu`.
- **Một công tắc "Hỗ trợ cự ly"** thay hai checkbox nội-suy + đọc-màn (chúng là hai nửa một cơ chế; tắt nội suy thì đọc-màn vô nghĩa vì `refine()` cần mốc do `anchor()` đặt).
- **Màn Cast**: hàng app một cột kiểu v0.3x — chạm = tick xanh + vào nút nổi; app đã chọn **nhảy lên khu "Đang chọn"** kèm panel `%`/Kích thước/Vị trí/**Nội dung** (nhỏ/to, không phải DPI)/Khôi phục và nút "CHIẾU <app> LÊN CỤM" ngay tại app. App chưa chọn nằm ở lưới dưới. Khung/DPI lưu **theo app**; áp qua **debounce 320ms** (bản vá lag trên xe của v0.57).
- Đã xoá nút dư: "Mở Navigation", "Chiếu/chuyển app" chung, "Điều chỉnh khung/DPI", "Chọn app cho nút nổi", hàng công tắc nút nổi trong màn phụ. Dừng/Chẩn đoán/Khắc phục xuống footer.

### Đã dọn trong ngày
Dead Reckon/mock (1.096 dòng), `vd_map` + `DadbBridge` + `PersistentDeviceShell` (297), hạ tầng module không ai mở được (`ModuleHost/ClusterModule/ModuleRegistry/ModuleUi/DashModule`, 306), dependency `jqwik`. Mã ngoài sản phẩm đích: 36% → ~11%.

## 4. Đang làm dở — NÚT NỔI

**Chưa xong.** Chủ dự án: nút nổi hiện tại xấu, cần port 1-1 từ v0.57. Nguồn: `git show ff01199:.../FloatingBubbleService.kt`. Ràng buộc: giữ hình dáng v0.57 nhưng **mọi lệnh qua façade V2**, không gọi engine V1. Đã sửa một crash init-order (`catalog` đọc trước khi gán → đổi `by lazy`) và thêm bước E2E O5b bật nút nổi qua UI.

Vị trí nút nổi trên emulator đang đè góc trái-trên do lưu từ lúc bấm thử — mặc định phải là rìa phải giữa màn.

## 5. Bài học vận hành ngày 27/7 (đã thành test hoặc luật)

- **Bảy lỗi đo của chính agent** — đếm call thay ranh giới, đếm theo package, quét sai module, đếm trong transport, chỉ đếm dòng `import`, đếm test theo tên đầy đủ, gộp nhóm sai (V1 "5.970" thực ra 2.110). Quy tắc: số từ script phân loại chưa kiểm thì chưa phải sự thật.
- **Ba lỗi "báo xong mà không xong"** — verdict ghi vào file bóng, `--note` bị cắt, E2E chạy trên trạng thái sót. Quy tắc: mọi tuyên bố thành công phải có phép đọc lại độc lập (`wc -l` cứu cả buổi xe).
- **Luật mới máy giữ**: test của `:core` phải ở `:core` (`LayeringRulesTest`), mọi trạng thái phải có đường ra (`NoDeadEndStateTest`, `NavigationNoDeadEndTest`), nhãn rủi ro candidate không được dối (`CandidateRiskLabelTest`).
- **P2 "dời thì chỉ được dời"**: tách `SpeedReading` tôi lỡ sửa hành vi (chặn `NaN`) — đã khai báo tường minh thay vì giấu trong diff.

## 6. Lệnh hay dùng
```
export JAVA_HOME=/opt/homebrew/opt/openjdk@17
./gradlew --offline testDebugUnitTest :core:test :car-integration:test   # 697 xanh
bash scripts/vehicle/carexec.sh steps|scenarios|plan <id>|run <c>|verdict <c> ok|e2e|observe --recorded
# checklist kiến trúc 5 bước: docs/refactor-car-execution/layering-rules.md §4
```
