# Rà soát tài liệu lỗi thời — 2026-09-26

> **Trạng thái**: Current · **Cập nhật**: 2026-09-26 · **Mục đích**: biên bản một đợt rà soát **toàn bộ** `docs/**/*.md` + `*.html` cho bản **2.73 (174)** — doc nào ghi `Current` mà nội dung đã bị thay, doc nào thiếu header, doc nào mồ côi (không có trong INDEX), dòng index nào trỏ sai. Ghi rõ **trước → sau → vì sao** để lượt sau không phải điều tra lại.

**Phạm vi & cách làm.** Quét **487** tệp `docs/**/*.{md,html}`, còn **475** sau khi trừ vùng loại trừ. Dòng liên kết lấy **bằng máy** từ `docs/README.md` (`](…)` → **361** đích phân biệt) rồi so với `find docs -name '*.md' -o -name '*.html'`. Phép thử header: có chữ `Trạng thái` trong 25 dòng đầu (`.md`); với `.html` phải là dòng metadata **cấp tài liệu**, **không** tính cột bảng `<th>Trạng thái</th>` (cột bảng làm phép thử pass giả).

**Vùng loại trừ (không đọc để sửa, không xét):** `docs/diagnostics/hud-sign-re/**` và `docs/specs/seal-*.html` (**byte-sealed** — hash chốt trong `:offcar-planner:test`, xem §🔒 của `README.md`) · `docs/PROJECT-BACKLOG.md` · `docs/specs/kachi-closeout-hardening.html` · `docs/CLOSEOUT-2026-09-25.md` · `docs/diagnostics/oncar-runbook-2.73.md` · `apk/README.md` · `.kiro/**` · `docs/archive/**`.

**Nguyên tắc sửa.** Chỉ đụng **dòng header**. Nội dung thân bài của một doc đã bị thay **không** được viết lại — nó là bằng chứng của thời điểm đó; việc đúng là gắn nhãn `Superseded`/`Historical` kèm **trỏ sang doc thay thế**, để người đọc sau biết ngay mình đang đọc cái gì. Một ngoại lệ có chủ ý: **câu nói sai về sản phẩm hiện tại** thì phải sửa chữ (xem §1 cuối mục). Ngược lại, **chuỗi UI được trích trong một khối `[ĐO]`** thì KHÔNG được sửa — đó là bằng chứng của lần đo đó; sự thật mới ghi thành ghi chú **ngoài** dấu ngoặc kép.

## 0. Tổng kết

| Hạng mục | Số | Ghi chú |
|---|---|---|
| (a) `Current` nhưng nội dung đã bị thay → đổi trạng thái | **24 phát hiện / 20 tệp đổi nhãn** | 3 phát hiện còn lại nằm ở **dòng index** của `README.md` (1 sửa ngay, 2 chuyển backlog); tệp thứ 21 (`icon-set-v2-inventory-2026-09-13.md`) **không phải nợ**: nó đã có header, chỉ là tên trường bằng tiếng Anh (`Status`/`Date`/`Purpose`) và giá trị đã đúng `Current` ⇒ để nguyên byte, đổi tên trường là việc riêng |
| (b) Thiếu header → thêm dòng header | **222 tệp thiếu** · **54 tệp đã thêm** | Thêm cho toàn bộ doc đang là nguồn hiện hành. **Còn 168 chưa thêm**: 112 trong `docs/archive/` (R0: archive không authoritative) + 56 báo cáo stage `_handoff/*-done.md` và `refactor-car-execution/*` (Session/Historical, không ai tra) — xem §2 |
| (c) Doc mồ côi (không có trong INDEX) → thêm dòng index | **17 → 0** | 124 tệp trong `docs/archive/` **không** tính là mồ côi: INDEX cố ý liên kết ở mức thư mục (`README.md` §Archive) |
| (d) Dòng index trỏ tệp không tồn tại | **0** | Cả 361 đích đều có thật trên đĩa |
| (f) **Ngoài đề bài** — dòng index **nói sai** về doc nó trỏ tới | **3 phát hiện / 1 sửa** | `apk/README.md` lệch 7 số (sửa ngay) · `dead-reckon-revalidation.html` nên Historical · `kachi-wake-word.html` index chậm hơn doc — xem §1 và §5 |
| (e) **Ngoài đề bài** — dòng index hỏng cấu trúc bảng | **5 → 0** | 3 dòng có ký tự `\|` chưa thoát (bảng lệch thành 5 cột), 2 dòng bảng lạc vào mục danh sách gạch đầu dòng. Xem §4 |

## 1. (a) Ghi `Current` nhưng nội dung đã bị thay

Gộp theo lý do, vì 18/20 tệp cùng một bệnh: **runbook / playbook / plan cho một bản đã ship**. Một runbook đã chạy xong là **biên bản**, không phải việc-cần-làm; để nó `Current` thì phiên sau mở ra và đi kiểm lại một bản không còn tồn tại.

| Tệp | Trước | Sau | Vì sao |
|---|---|---|---|
| `CLOSEOUT-2026-08-16.md` | `Current` | `Historical` | Đánh giá đóng dự án **ClusterNav 1.30**; bản đóng dự án hiện hành là `CLOSEOUT-2026-09-25.md`. Chính `README.md` đã ghi Historical ⇒ hai nguồn mâu thuẫn (R2.4) |
| `diagnostics/oncar-runbook-2.07.md` | `Current` | `Superseded — thay bởi oncar-runbook-2.73.md` | Runbook cho 2.07 |
| `diagnostics/oncar-runbook-2.69.md` | `Current` | `Historical` | Buổi xe 2.68/2.69 **đã chạy** 2026-09-26; kết quả ở `oncar-session-2026-09-26.md`. Để `Current` thì có **hai** runbook `Current` cùng lúc |
| `diagnostics/oncar-runbook-2.19.md` | `chờ test trên xe` | `Historical` | Giá trị trạng thái ngoài taxonomy, và index đã ghi Historical |
| `diagnostics/oncar-final-runbook-1.87.md` | `Current` | `Historical` | Đã chạy 2026-09-21; index đã ghi Historical |
| `diagnostics/oncar-playbook-kachi-1.47.md` | `Current` | `Superseded — thay bởi 1.53` | Index đã ghi Superseded; **thêm**: thân bài còn tả **Vosk** là bộ nghe dự phòng |
| `diagnostics/oncar-playbook-kachi-1.53.md` | `Current` | `Historical` | Playbook cho 1.53 — cách bản hiện hành 56 số |
| `diagnostics/oncar-playbook-kachi-1.55.md` | *(không có header)* | `Historical` + header mới | Vừa mồ côi header vừa bị index gọi là `Current` |
| `diagnostics/oncar-master/RUNBOOK.md` · `RUN-STEPS.md` · `0-PENDING.md` | `Current` (bản đích **1.83**) | `Superseded` | Cả ba ghim bản đích 1.83; runbook hiện hành là `oncar-runbook-2.73.md`, danh sách chờ-xe hiện hành ở `PROJECT-BACKLOG.md` |
| `diagnostics/oncar-master/1-hal.md` | `Current` | `Superseded — thay bởi oncar-sweep-verify-2026-09-21.md` | Bảng sweep đời 1.79 |
| `diagnostics/oncar-master/2-slot-cast.md` · `4-system.md` | `Current` | `Historical` | RE ô/cast và bảng system thời tiền-1.83 (4-system còn ghim *"xe đang 1.76"*) |
| `diagnostics/oncar-master/3-voice.md` | `Current` | `Superseded — thay bởi offcar-2026-09-26/voice-tail-fuzzy-phonetic.md` | Danh sách voice chờ-verify ở 1.79 |
| `diagnostics/oncar-runbook-hey-kachi.md` | `Current` (cần **1.79+**) | `Historical` | "Hey Kachi" đã lên xe và **đã đo** (`oncar-session-2026-09-26.md` §3 D7) |
| `diagnostics/oncar-plan-2026-08-19.md` | `Current` | `Historical` | Kế hoạch on-car đời ClusterNav 08/2026 |
| `diagnostics/VEHICLE-TEST-V2.md` | `Current` · 2026-07-26 | `Historical` | Ma trận "Stage 11" đời ClusterNav, **chưa từng chạy**, cách đây 2 tháng |
| `diagnostics/camera-panorama-RE-2026-09-22.md` · `oncar-camera-wake-2.15.md` · `seat-vietmaploop-oncar-2026-09-06.md` | `Current` / `NOTED` / `chờ side-load` | `Historical` | **Doc nói ngược index** — index đã ghi Historical ở cả ba. Lấy index (nghiêm hơn) và khớp doc lại |

**Ba phát hiện nằm ở `README.md`, sửa trong index chứ không trong doc:**

| Dòng index | Trước | Sau |
|---|---|---|
| L3 (header của chính INDEX) | `bản 2.72 (173)` · OTA `Kachi-2.72-release.apk` | `bản 2.73 (174)` · OTA `Kachi-2.73-release.apk` |
| Dòng `apk/README.md` | *"bản hiện hành 2.66 (167)"* · 2026-09-25 | *"bản hiện hành **2.73 (174)**"* · 2026-09-26 — **sửa ngay**. Đáng ghi: **chính `apk/README.md` đã đúng 2.73**, chỉ dòng index lệch **7 số** ⇒ đây là lỗi của INDEX, không phải của doc. Đúng loại lỗi mà một phép kiểm "index nói gì về doc" bắt được còn "đọc doc" thì không |
| Dòng `specs/dead-reckon-revalidation.html` | `Current · 2026-07-27` | Nên là `Historical` — đây là một **quyết định đã đóng** (gỡ hẳn Dead-Reckon 2026-07-27), hướng dẫn còn sống nằm ở `research/gps-dead-reckon-tunnel.html`. **Việc cho backlog** |

**`specs/kachi-voice-command.html` — một chỗ sửa CHỮ, và một chỗ CỐ Ý KHÔNG sửa:**

1. **Sửa** — dòng trạng thái: thêm cảnh báo *tầng NGHE trong tài liệu này là **Vosk**, đã THAY bởi sherpa-onnx* (`specs/kachi-voice-engine-v2.html`). Không viết lại thân bài: Vosk là sự thật của 1.49, có giá trị lịch sử.
2. **KHÔNG sửa** — hai chỗ ghi *"Chạm ra ngoài **hoặc bấm Back** để huỷ"*. Lượt vá đầu đã đổi thẳng hai chuỗi này cho khớp 2.73 (Back không còn huỷ) và **đã được hoàn nguyên**: cả hai nằm trong khối **`[ĐO]`** ghi *chuỗi mà UI hiện ra* ở bản 1.49 (bản ghi máy ảo 2026-09-14). Sửa chúng là sửa **lời trích của một phép đo** — đúng thứ CLAUDE.md §2/§3 cấm. Việc đúng, và đã làm: giữ nguyên chuỗi trích, gắn **ghi chú ngoài dấu ngoặc kép** nói từ 2.73 Back không huỷ nữa, trỏ sang `offcar-2026-09-26/taskbar-focus-and-shell-budget.md`. Chỗ hướng dẫn *người dùng* (không phải lời trích) đã nói đúng ở `HUONG-DAN-KACHI.md` §6 và README.

**Ba tín hiệu đi kiểm mà KHÔNG có bệnh** (ghi lại để lượt sau khỏi kiểm lần nữa):

- `mock location` / dead-reckon tả như tính năng **đang sống**: **0** chỗ ngoài `archive/`. Mọi chỗ đều nói rõ đã gỡ/đã cấm.
- Số chức năng cũ (`187` / `167`): mọi chỗ đều **đã có** câu đính chính tại chỗ (*"nay là 154 sau (V) FEATURE-FILTER"*).
- Chữ `Vosk` trong `specs/kachi-voice-engine-v2.html`, `diagnostics/vn-stt-*`, `voice-ft-*`: đều là khung **so sánh/lịch sử** — đúng, không phải nợ.

## 2. (b) Thiếu header

**222** tệp không có dòng `Trạng thái`. Đã thêm **54** — chọn theo một luật: **doc nào đang là nguồn hiện hành hoặc đang bị INDEX gọi là authoritative thì phải có header**, vì header là thứ duy nhất nói cho người mở tệp biết *"cái này còn đúng không"*.

| Nhóm | Số | Trạng thái gắn | Ví dụ |
|---|---|---|---|
| Spec/doc **HTML** không có dòng metadata cấp tài liệu | 17 | `Current` ×6 · `Historical` ×11 | `specs/kachi-voice-engine-v2.html` (Current — **đây mới là spec ASR có thẩm quyền: sherpa-onnx thay Vosk**) · `specs/pm25-auto-filter.html` · `specs/seat-comfort-auto.html` · `research/gps-dead-reckon-tunnel.html` (Current — hướng dẫn *đừng hồi sinh Dead-Reckon* vẫn còn hiệu lực) · `specs/launcher-foundation.html` (Historical) |
| Diagnostics `.md` đang được index gọi là hiện hành | 21 | `Current` ×13 · `Historical` ×8 | `diagnostics/oncar-sweep-verify-2026-09-21.md` · `byd-hal-permission-RE-2026-09-14.md` · `oncar-captest-sealion6-2026-09-16.md` · `hal-register-latch-on-identity-switch-2026-08-22.md` |
| Bảng **sinh bằng máy** (tương phản màu, datum) | 5 | `Current` + ghi *"sinh bằng máy — đừng sửa tay"* | `visual-refresh-2026-09-16/contrast-table*.md` · `p2/contrast-families.md` · `carlog-…/20-datums.md` |
| Dữ liệu thô buổi xe 26/09 | 9 | `Current` (bằng chứng thô) | cả `diagnostics/perf-oncar-2026-09-26/` |
| Doc phiên | 2 | `Session` | `ocr-review-2026-09-16.md` · `oncar-trace-2026-09-16b/README.md` |

**Còn 168 tệp chưa thêm, có chủ ý:**

- **112** trong `docs/archive/` — R0 đã nói archive **không** authoritative; header ở đó không thêm thông tin nào mà chỉ tạo 112 dòng diff.
- **56** báo cáo stage kiểu `_handoff/*-done.md`, `_handoff/re-hud-track*.md`, `refactor-car-execution/*` — Session/Historical đã đóng, đã có dòng index nói rõ trạng thái. **Việc cho backlog** nếu muốn sạch 100 %.

**Hai cái bẫy phát hiện khi thêm header — ghi lại vì lượt sau sẽ gặp lại:**

1. **4/5 bảng "sinh bằng máy" bị generator GHI ĐÈ TOÀN TỆP**, nên header vừa thêm sẽ **mất im lặng** ở lượt sinh sau (không test nào đỏ, chỉ mất): `contrast-table.md` ← `SurfaceContrastContractTest.kt` · `contrast-table-colors.md` ← `ColorChoiceContractTest.kt` · `p2/contrast-families.md` ← `scripts/design/icon-audit.py` · `carlog-…/20-datums.md` ← `scripts/vehicle/kachi/20-datums.sh`. Muốn header bền thì **generator phải tự in ra** — đó là sửa code, ngoài phạm vi đợt doc này ⇒ **việc cho backlog**. (`contrast-table-paint.md` an toàn: generator nó khai — `CarPaintContrastContractTest` — **không còn tồn tại** trong cây, 0 chỗ khớp khi grep. Cũng là một nợ đáng ghi.)
2. **3 tệp vừa thêm header nằm trong `.gitignore`** (`oncar-trace-2026-09-16b/README.md`, `carlog-kachi-20260914-2044/20-datums.md`, `session-findings.md` — khớp luật bỏ qua `carlog-*`/trace). Header có trên đĩa nhưng **không bao giờ vào commit**, và `git log` của chúng rỗng nên ngày phải mặc định 2026-09-26. Nếu muốn chúng là bằng chứng chia sẻ được thì phải quyết định bỏ chúng ra khỏi `.gitignore` — **quyết định của owner**, không tự làm.

## 3. (c) Doc mồ côi → đã vào INDEX

**17 → 0.** Chia hai loại, và loại thứ hai đáng ghi lại vì nó là một **cái bẫy của phép kiểm**:

| Loại | Số | Đã làm |
|---|---|---|
| Không được nhắc ở đâu trong INDEX | 9 | Cả thư mục `diagnostics/perf-oncar-2026-09-26/` (dữ liệu thô buổi xe 26/09) → **một** dòng index mới trong §6, **liên kết cả 9 tệp** trong ô mục đích (đủ để phép kiểm mồ côi pass, mà bảng không phình thêm 9 dòng) |
| Có nhắc trong INDEX **nhưng bằng chữ thường / `code span`**, không phải liên kết `](…)` | 8 | `oncar-master/0-PENDING.md` + `RUN-STEPS.md` (đổi gạch đầu dòng thành liên kết thật) · `visual-refresh-…/contrast-table-colors.md` + `p2/contrast-families.md` (thêm một dòng index chung) · `_handoff/stage2-done.md` + `stage3-done.md` (thêm 2 dòng §9) · `prototypes/kachi-workspace.html` + `kachi-launcher-prototypes.html` (thêm 2 gạch đầu dòng vào §Design/Evidence) |

> **Bài học cho phép kiểm mồ côi**: nhắc tên tệp trong một `code span` **không** làm nó thôi mồ côi. Phép kiểm chạy trên `](…)`, và đó là phép kiểm đúng — một cái tên không bấm được thì người đọc index vẫn không tới được tệp. 8/17 mồ côi ở đợt này thuộc đúng loại đó.

## 4. (e) Dòng index hỏng cấu trúc bảng (ngoài đề bài, tự phát hiện khi kiểm)

Đếm bằng máy số `|` mỗi dòng bảng của `README.md` (mọi dòng phải có đúng 5):

| Dòng | Bệnh | Sửa |
|---|---|---|
| `oncar-master/4-system.md` · `oncar-bugs-2026-09-15.md` · `runbook-mod-vietmap-cluster.md` | Ô *Mục đích* có ký tự `\|` **chưa thoát** (trong `grep byd.launcher\|…`, `256\|…`) ⇒ Markdown tách thành **5 cột**, hai cột `Trạng thái`/`Cập nhật` bị đẩy ra ngoài bảng | Thoát thành `\\|` |
| 2 dòng ở §Design/Evidence | Dòng **bảng** lạc vào mục dùng **gạch đầu dòng** ⇒ render ra một bảng 1 dòng không có tiêu đề | Đổi thành gạch đầu dòng cho khớp mục |
| 5 dòng (do coordinator thêm trước đó) | Thiếu hẳn hai cột cuối `\| Trạng thái \| Cập nhật \|` | Thêm ` Current \| 2026-09-26 \|` |

Sau khi sửa: **0** dòng bảng lệch cột trong `docs/README.md`.

## 5. Việc chuyển cho backlog (phiên này KHÔNG làm)

1. ~~`apk/README.md` lệch 7 số~~ — **đã sửa** dòng index (tệp `apk/README.md` vốn đã đúng 2.73).
2. Dòng index `specs/dead-reckon-revalidation.html` nên `Historical` (quyết định đã đóng 2026-07-27).
3. Dòng index `specs/kachi-wake-word.html` ghi *"Draft — CHỜ OWNER DUYỆT (chưa code)"* nhưng **chính spec** ghi `APPROVED — owner chốt 3 OQ 2026-09-18` **và tính năng đã ship**. Đây là lệch **ngược hướng** với §1 (index chậm hơn doc) ⇒ phải sửa dòng index.
4. ~**70** dòng spec dùng giá trị trạng thái **ngoài taxonomy** (`Draft / chờ duyệt`, `DONE off-car`, `Đã ship 2.23`…). Gốc: chính `specs/_template.html` ship sẵn `Trạng thái: Draft / chờ duyệt`, nên **mọi** spec sao từ template đều lệch từ lúc sinh ra. Sửa template trước, rồi mới quét.
5. `README.md` §🔒 còn cảnh báo *"BYTES ĐÚNG HIỆN CHỈ NẰM Ở INDEX + CÂY LÀM VIỆC, CHƯA VÀO HEAD (2026-08-23)"* — cần kiểm lại xem còn đúng không (vùng byte-sealed, ngoài phạm vi đợt này).
6. **56** tệp `_handoff/*-done.md` + `refactor-car-execution/*` chưa có header (§2).
7. **4 bảng sinh bằng máy** sẽ mất header ở lượt sinh sau — generator phải tự in header (§2 bẫy 1). Kèm: `contrast-table-paint.md` khai một generator (`CarPaintContrastContractTest`) **không còn trong cây** ⇒ tệp đó nay không dựng lại được.
8. **3 tệp bằng chứng bị `.gitignore`** (§2 bẫy 2) — owner chốt có đưa vào repo hay không.

## 6. Bằng chứng không bị phá

Doc byte-sealed **không** bị đụng. Chốt bằng `:offcar-planner:test` (bộ test giữ hash từng byte của 13 tệp cha + 12 tệp tái sinh): xem kết quả ghi ở §Verification của phiên trong `PROJECT-BACKLOG.md`. Nếu test báo `SEALED PARENT FILE(S) DRIFTED` thì khôi phục **bytes** (`git checkout <commit> -- <path>`), **không bao giờ** đi hướng ngược lại là sửa hằng số hash.
